# 基础批判档案（CRITIQUE）

> 本文档回答「DESIGN.md 的基础主张哪里可疑」。只收针对公理、身份声明与核心断言的概念质疑；实现缺陷与工程笔记归 ARCHITECTURE.md。
> 每条质疑只有两种闭案方式：**(a)** 提炼出本质并据此修订 DESIGN.md 的表述或机制；**(b)** 显式接受为权衡——写明放弃了什么、为何仍然值得。含糊共存不算闭案。
> 裁决纪律沿 DESIGN §9：按门控推进，未测量不动手；但本文的「测量」多为概念澄清与小实验，不等 village 规模。

## 组织轴

DESIGN.md 在三层借用了逻辑与信息科学的权威，而未构造对应的理论。各质疑标注所属轴；三轴各有理论落位之日，即「本质」问题有答案之时：

| 轴 | 缺失的理论 | 关联质疑 |
|---|---|---|
| 真 | 叙述的真理理论：说什么算真、谁有权说 | C1, C4 |
| 蕴含 | 规则网的推理理论：顺序耦合何时可见、冲突何时报警 | C3, C8 |
| 信道 | 信息记账：带宽、冗余、出处、噪声各归哪一层 | C2, C5, C9 |

## C1. 「可证伪」是修辞，机制只做实体接地 【真】

- **DESIGN 主张**：§2「表达可证伪……幻觉无处藏身」。
- **质疑**：declare 校验的是名称提及权（涉及实体 ⊆ touched 集），从不校验陈述内容。校验带宽 ≈ log₂|touched| 位/条，载荷（statement + 散文正文）数百位——**核对的是地址标签，不是信件**。正文无机械检查是自觉选择（§5-10 实证），那么实际承诺的只是「实体级接地 + touched 边界」，不是可证伪性。借用真理理论的权威而无真理条件的构造。
- **出路候选**：
  - (a) 改口径：§2 降级为「实体级接地；谓词层一致性由法则 + 声明契约 + 提示约束分层承担」。成本 ≈ 0。
  - (b) 构造谓词层的正式片段：结构化 Fact 已是雏形，把高频谓词（持有/位置/存活）逐步纳入可校验形态。与「词表永不回归」不冲突（结构校验 ≠ 词表扫描）；代价是表达自由度下降、作者负担上升。
  - (c) 接受权衡：实测玩家侧残余幻觉率低于注意阈值即闭案。
- **判定所需**：量化「未核验占比」——每回合玩家读到的叙述中，多少命题内容处于一切校验之外；附 e2e 幻觉率基线。
- **状态**：闭案（(a)）：DESIGN §2/§5/本质清单已降级为「实体级接地 + 分层一致性」；(b)(c) 不再构成欠债，留作增强候选；残余幻觉率基线转为可选测量项，非闭案条件。

## C2. 冗余被删除而非被不信任；情节记忆整类缺席 【信道】

- **DESIGN 主张**：「语言不入账」「叙述不回流——幻觉不固化」。
- **质疑**：香农：冗余是纠错容量，不是浪费。散文是状态的第二份含噪编码；引擎将其从记忆层级整体切除（防漂移正确），账单转嫁作者——一切需叙事存续的线索须手工物化为 props/relations，负担随故事复杂度超线性。且系统只剩工作记忆（近况窗口）与语义记忆（世界状态）：对话说了什么、发生过什么事件，无处安放。§9 社交记忆只是此洞的特例形态。
- **出路候选**：
  - (a) 隔离记录通道（§9 记录方案之推广）：AI 写「回合纪要」入隔离命名空间；映射/表达可读作上下文，规则不可读、不构成状态。把「不信任」落实为「带出处的旁路」，而非切除。代价：token 预算；须证明纪要不经上下文间接固化幻觉。**原料已现成：声明契约每回合产出的已校验 `{entities, statement}`——AI 语言、实体接地、从未入账、校验后即被丢弃——正是记录通道的合格条目，立项时无需新机制；命题真伪未经校验，导入须带出处标记并受本条自身的固化风险门控。解域澄清：纪要只解决叙事连续性；言语的机械后果（承诺的咬合、谎言的代价）仍须由 speech-act 动词物化为状态——那是创作协议的职责，不是记录通道能还的债。**
  - (b) 维持切除，把物化负担承认为品类创作成本（era 作者本就手写事件脚本）。
  - (c) 折中：仅高显著性事件（不变式拒绝、despawn、守恒触碰）自动入纪要。
- **判定所需**：对话密集对照实验（§9 已门控）；另加「物化负担」度量——一个叙事向游戏要多少 props/relations 才撑得起十回合连贯回溯。
- **状态**：开放。

## C3. 形式层正以运行时瘢痕分期赎回 【蕴含】

- **DESIGN 主张**：数据法则是类别错误（ARCH §5-11），规则即代码；core 不内嵌形式层。
- **质疑**：杀死 AST 后，形式机制正逐件赎回：probe 多卫命中报告（运行时模型检查）、PropDef 值域（分期到位的类型系统）、计划中的注册表闭包检查（静态分析转世）、dryTick（有界前瞻）。每件都是被教义禁止整体承诺之物碎片化重建，以运行时成本支付。本质：形式化水平应一次选定、由类型静默承担，而非按疼痛分期、由探测承担。**词汇看守不是待研究问题，是缺失的类型纪律的经验发现。**
- **出路候选**：
  - (a) 一次到位的最小类型纪律：props 注册表已有 type 字段，升级为构造期约束（规则经具名访问器读属性）。词汇看守闭案为设计约束。代价：与「games 层怎么写都行」原则的张力需划界（类型访问器是 core 工具，不是吸收游戏形态——可辩）。
  - (b) 维持分期赎回，承认总成本更高，但每步有实证门控。
  - (c) 仅做 lint：静态扫描规则代码中的属性键字符串，对照注册表报未知键。零运行时成本、零 API 变更。
- **判定所需**：在 village 上估 (a)/(c) 的漏报率与作者负担差；(c) 近乎免费可先行。
- **状态**：开放（(c) 已落地：`sim lint` 静态扫描 GameDef 全部闭包的属性键对照注册表，首跑即发现 village 未注册 grain/water 并修正；见 ARCHITECTURE §5-12；(a)/(b) 仍待 village 规模门控）。

## C4. Fact 无认识论：感知与世界之言不分 【真】

- **DESIGN 主张**：fact-only 系统合法（梦中低语）；facts 进表达输入与声明契约。
- **质疑**：`Fact{text, entities}` 把世界状态之真与世界之言（氛围话语）并为同一证据代币；声明契约对二者同等放行——模型可合法地把低语断言为事实。痴迷可证伪的系统没有出处类型学：知觉事实 / 世界发出的话语 / 作者钦定，许可各异而机制无法表达差异。梦核里混淆即美学，成立；但那时此通道是引语通道，不该冒用认识论名词。
- **出路候选**：
  - (a) `Fact.kind: percept | utterance`：utterance 只授权关于话语的断言，不授权关于状态的断言。一处字段，成本极低。
  - (b) 接受混淆并改名（whisper/quotation），承认其为话语通道。
- **判定所需**：纯概念裁决。除非有人辩护「低语理应成为玩家的信念材料」，(a) 直接做。
- **状态**：闭案（(a)）：`Fact.kind: percept | utterance` 落地——percept（缺省）实体进声明契约 touched 集，utterance（世界之言）不进：话语只许被转述，不许据以断言状态；yume 低语系统改标 utterance；(b) 弃。

## C5. 确定性在错误的边界上被声称 【信道】

- **DESIGN 主张**：公理 4 随机是世界纯函数；§2「操作始终保持精确」「玩家行为有精确重量」。
- **质疑**：内核确定，界面随机——同一意图文本跨 run 映射到不同动词（ARCH §4-2 自证）。玩家经验到的性质「这些话 → 这些后果」不成立；**重量住在裁决层，不住在解释层**。「精确重量」是产品口吻包装的内核保证。
- **出路候选**：
  - (a) 改口径：明确区分「裁决重量」（确定）与「解释保真度」（概率），§2 措辞对齐。
  - (b) 高频意图确定性直通（DESIGN §4 已列为未来优化），解释层方差收敛于长尾。
  - (c) 映射层 temperature=0 + 结构化输出约束：压低不消除。
- **判定所需**：同意图集 × N run 的映射一致率基线；若一致率高且分歧集中于等价动词，「概率解释」可辩解为特性而非缺陷。
- **状态**：闭案（(a)）：DESIGN §2/§7 区分裁决重量（内核确定）与解释保真度（映射概率）；一致率基线归入 DESIGN §4 未来优化轨道，作为可选测量而非欠债。


## C8. 承重墙是荣誉制：两形态的纪律无声明、无执法 【蕴含】

- **DESIGN 主张**：§3.1「承重墙：法则网络形态下规则必须按属性组合键控，禁止特判实体 id」；「core 不感知形态差异」。
- **质疑**：形态（法则网络 vs authored）是 GameDef 的隐含属性——不声明于 def、不可被任何工具识别。承重墙因此只能靠 review 维持：法则网络游戏里一条 `p.entity === "x"` 的 id 特判不触发任何报警，组合爆炸的防线系于作者自律；「规则随新实体自动泛化」同样无法验证——probe 报 denyAll 缺口，但不报「规则读取了写死的 id」。按本项目自身的标准（约束是结构性的，不是提示词请求），无执法的设计约束应补执法或降级。
- **出路候选**：
  - (a) GameDef 增 `regime: "lawnet" | "authored"`（仅工具消费——lint/probe 读它，core 裁决路径不读，不违「core 不感知形态」）：lint 对 lawnet 游戏扫描规则闭包中的 id 字面量比较并报警。成本 ≈ 一个字段 + 一条 lint 规则。
  - (b) 降级表述：承重墙改称「书写纪律」，明示无执法，把组合爆炸防线的责任显式交还作者。
- **判定所需**：在 village 上估 (a) 的误报率（id 字面量出现在非比较位置的频率）。
- **状态**：开放。

## C9. 映射保真靠提示词请求；语义强制转换无守卫 【信道】

- **DESIGN 主张**：§4「能解析的动作直接提交，由规则层裁决」；§2「解释保真度是体验参数而非内核保证」。
- **质疑**：映射层的激励偏向「凑合成功」——玩家意图不适配任何动词时，模型要么诚实 refusal、要么把意图**强制转换**为最近似的合法动作。规则层只审机械合法性（可达/持握/属性），不审意图保真：village 里「送米」被转换为 sell 时，一次合法买卖会完整执行（铜币移动），世界欣然做了玩家没要求的事，且无任何拒绝可供学习边界。这是系统性风险（bias），不是 C5 已闭的跨 run 方差（variance）：方差说「同一文本可能命中不同动词」，此处是「不适配的文本可能被合法动词吞掉而无报警」。整个引擎的一致性约束都是结构性的（schema、契约、硬墙），唯独映射保真——意图与世界后果间的语义对齐——靠映射 prompt 的一句「无法解析 → refusal」维持，是设计里唯一接受提示词请求承担核心保真职责的位置。
- **出路候选**：
  - (a) 接受为品类代价并显式化：era 极的立场是「操作即语义」，玩家学习动词边界是被设计的学习曲线；在 DESIGN §7 把 coercion 从无名状态升格为 named trade-off。
  - (b) 结构性缓解：act 提案增可选保真信号（模型自报低置信），低置信时世界以世界腔先确认再裁决（引入确认回合的交互协议变更）。
  - (c) 测量先行：e2e 对照（意图 → 人工标注预期动词 vs 实际提案动词）建立 coercion 率基线，按门控决定 (a)/(b)。
  - (d) 映射面预检：act 之外增只读 check 工具（同一裁决瓶颈、预算限制、不翻 one-shot 门闩、不进叙述源与 touched 集），模型解读不确定时先验算再提交——把事后惊讶变为事前发现，让模型能选诚实 refusal 而非被合法动词吞掉的强制转换。代价：对「协议已完成且应冻结」立场的首次破例请求；「何时预检」仍是提示词请求（结构改进只提高分辨路径的质量上限，不消除决策点的概率性）。与 refusal.considered（ARCHITECTURE §2.5 未来优化）同族。
- **判定所需**：coercion 率基线（现有 loop transcript 可回放标注）。
- **状态**：开放。

## 与既有文档的关系

- 修订 DESIGN.md 时同步更新本文件索引与状态；ARCHITECTURE.md 只在裁决产生技术动作时新增条目。
- 本文不收实现缺陷（roll 键碰撞检测、提交快照成本、菜单去重碎片化等归 issue / ARCHITECTURE）。
