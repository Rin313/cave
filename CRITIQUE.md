# 基础批判档案（CRITIQUE）

> 本文档回答「DESIGN.md 的基础主张哪里可疑」。只收针对公理、身份声明与核心断言的概念质疑；实现缺陷与工程笔记归 ARCHITECTURE.md。
> 每条质疑只有两种闭案方式：**(a)** 提炼出本质并据此修订 DESIGN.md 的表述或机制；**(b)** 显式接受为权衡——写明放弃了什么、为何仍然值得。含糊共存不算闭案。
> 裁决纪律沿 DESIGN §9：按门控推进，未测量不动手；但本文的「测量」多为概念澄清与小实验，不等 village 规模。

## 组织轴

DESIGN.md 在三层借用了逻辑与信息科学的权威，而未构造对应的理论。各质疑标注所属轴；三轴各有理论落位之日，即「本质」问题有答案之时：

| 轴 | 缺失的理论 | 关联质疑 |
|---|---|---|
| 真 | 叙述的真理理论：说什么算真、谁有权说 | C13 |
| 蕴含 | 规则网的推理理论：顺序耦合何时可见、冲突何时报警 | C3, C8 |
| 信道 | 信息记账：带宽、冗余、出处、噪声各归哪一层 | C2, C9 |

## C2. 情节记忆缺席：公理交集处的精确分解 【信道】

- **DESIGN 主张**：「语言不入账」「叙述不回流——幻觉不固化」；记忆层只剩语义记忆（World）与工作记忆（近况窗口）。
- **质疑**：香农：冗余是纠错容量，不是浪费。散文是状态的第二份含噪编码；引擎将其从记忆层级整体切除（防漂移正确），账单转嫁作者——一切需叙事存续的线索须手工物化为 props/relations，负担随故事复杂度超线性；对话说了什么、发生过什么事件，无处安放。§9 社交记忆只是此洞的特例形态。
- **精确分解（裁决）**：原表述把三个不同性质的缺口混为一个「洞」。按「记忆内容 × 用途」拆开，每格的公理地位不同：

  | 内容类型 | 作裁决输入（入账本） | 作表达上下文（记忆） | 作权威存档（审计） |
  |---|---|---|---|
  | 当前事实（props/relations） | ✓ 账本本身 | ✓ digest | ✓ state |
  | 机械事件史（Change/Fact/Denial，带 src） | ✗ 公理 1 的形状闭包——需历史者须物化 | ✓ **无公理障碍**：近况窗口已是其最小实现（6 回合内核裁决行 + 意图 80 字符），扩容是调参非设计变更 | ✓ transcript；重放裁决不可得（快照线：决策是代码、代码不序列化，存档兼容须钉住整个 def） |
  | 语言史（对话内容、玩家原话全文、逐回合散文、utterance） | ✗ 公理 3 | 唯一合法形态 = 带出处的隔离记录通道（(a)）；散文全文回流被切除（防固化，正确） | ✓ session 文件全量累积 |

  三个分立裁决：
  1. **物化税——部分闭案：显式接受为公理代价。** 历史要影响裁决必须重演为当前事实，这是公理 1（账本形状闭包）与公理 3（语言不入账）的定义性代价，与 C9 同一条接受逻辑：机械可执行的真相只有「当前事实」一种形态。era 作者手写事件脚本本就是品类创作成本，不再列为质疑。
  2. **机械事件史进上下文——澄清：从来不是公理问题。** 账本形状只约束裁决输入，不约束记忆层；近况就是机械事件史进上下文的最小形态，字段取舍（6 回合、80 字符意图）是工程量决策。曾经的归因（「状态即唯一真相挤出了记忆」）不成立。
  3. **语言史进上下文——保持开放：唯一真正的公理空白。** 公理 3 禁其入账、防固化禁其回流，系统内无合法居所。注意公理 3 的管辖域是账本而非上下文：回合内上下文已合法居住三类语言（玩家意图、通道策展、内核裁决行），记录通道是第四类（模型自撰、带出处），不入账故不违公理。
- **出路**：隔离记录通道——AI 写「回合纪要」入隔离命名空间；映射/表达可读作上下文，规则不可读、不构成状态、不产生后果（记录 ≠ 后果，软通道的静默假授予 bug 在此结构上不可能复现）。把「不信任」落实为「带出处的旁路」，而非切除。**原料现成：声明契约产出的已校验 `{entities, statement}`（AI 语言、实体接地、从未入账、校验后即弃）、完整意图文本、（如需）内核事件行——立项无需新机制；条目须带出处分层（内核背书 / 玩家原话 / 模型自述）。解域澄清：纪要只解决叙事连续性；言语的机械后果（承诺的咬合、谎言的代价）仍须由 speech-act 动词物化为状态——创作协议的职责，不是记录通道能还的债。**
- **记录通道的新风险面（门控实验的精确对象）**：今天的回合内上下文是**零模型自撰持久文本**（叙述被剪除；近况除 refusal.label 一枚短标签外全为内核行与玩家原话）。记录通道首次引入「模型自述层」，固化风险的准确形状 = 自述偏差经上下文跨回合自我放大——账本无害（规则读不到），风险只在表达连续性。判定所需：有/无自述层的对话密集对照实验（跨回合错误率与空心感的净变化）；另加「物化负担」度量——一个叙事向游戏要多少 props/relations 才撑得起十回合连贯回溯。
- **状态**：部分闭案（物化税显式接受为公理代价）；语言史记录通道开放（门控如上）。

## C3. 形式层正以运行时瘢痕分期赎回 【蕴含】

- **DESIGN 主张**：数据法则是类别错误（ARCH §5-11），规则即代码；core 不内嵌形式层。
- **质疑**：杀死 AST 后，形式机制正逐件赎回：probe 多卫命中报告（运行时模型检查）、PropDef 值域（分期到位的类型系统）、计划中的注册表闭包检查（静态分析转世）、dryTick（有界前瞻）。每件都是被教义禁止整体承诺之物碎片化重建，以运行时成本支付。本质：形式化水平应一次选定、由类型静默承担，而非按疼痛分期、由探测承担。**词汇看守不是待研究问题，是缺失的类型纪律的经验发现。**
- **出路候选**：
  - (a) 一次到位的最小类型纪律：props 注册表已有 type 字段，升级为构造期约束（规则经具名访问器读属性）。词汇看守闭案为设计约束。代价：与「games 层怎么写都行」原则的张力需划界（类型访问器是 core 工具，不是吸收游戏形态——可辩）。
  - (b) 维持分期赎回，承认总成本更高，但每步有实证门控。
  - (c) 仅做 lint：静态扫描规则代码中的属性键字符串，对照注册表报未知键。零运行时成本、零 API 变更。
- **判定所需**：在 village 上估 (a)/(c) 的漏报率与作者负担差；(c) 近乎免费可先行。
- **状态**：开放（(c) 已落地：`sim lint` 静态扫描 GameDef 全部闭包的属性键对照注册表，首跑即发现 village 未注册 grain/water 并修正；(a)/(b) 仍待 village 规模门控）。


## C8. 承重墙是荣誉制：两形态的纪律无声明、无执法 【蕴含】

- **DESIGN 主张**：§3.1「承重墙：法则网络形态下规则必须按属性组合键控，禁止特判实体 id」；「core 不感知形态差异」。
- **质疑**：形态（法则网络 vs authored）是 GameDef 的隐含属性——不声明于 def、不可被任何工具识别。承重墙因此只能靠 review 维持：法则网络游戏里一条 `p.entity === "x"` 的 id 特判不触发任何报警，组合爆炸的防线系于作者自律；「规则随新实体自动泛化」同样无法验证——probe 报 denyAll 缺口，但不报「规则读取了写死的 id」。按本项目自身的标准（约束是结构性的，不是提示词请求），无执法的设计约束应补执法或降级。
- **出路候选**：
  - (a) GameDef 增 `regime: "lawnet" | "authored"`（仅工具消费——lint/probe 读它，core 裁决路径不读，不违「core 不感知形态」）：lint 对 lawnet 游戏扫描规则闭包中的 id 字面量比较并报警。成本 ≈ 一个字段 + 一条 lint 规则。
  - (b) 降级表述：承重墙改称「书写纪律」，明示无执法，把组合爆炸防线的责任显式交还作者。
- **判定所需**：在 village 上估 (a) 的误报率（id 字面量出现在非比较位置的频率）。
- **状态**：开放。

## C9. 映射保真靠提示词请求；语义强制转换无守卫 【信道】

- **DESIGN 主张**：§4「能解析的动作直接提交，由规则层裁决」；§2「解释保真度是体验参数而非内核保证」。
- **质疑**：映射层的激励偏向「凑合成功」——玩家意图不适配任何动词时，模型要么诚实 refusal、要么把意图**强制转换**为最近似的合法动作。规则层只审机械合法性（可达/持握/属性），不审意图保真：village 里「送米」被转换为 sell 时，一次合法买卖会完整执行（铜币移动），世界欣然做了玩家没要求的事，且无任何拒绝可供学习边界。这是系统性风险（bias），不是 C5 已闭的跨 run 方差（variance）：方差说「同一文本可能命中不同动词」，此处是「不适配的文本可能被合法动词吞掉而无报警」。整个引擎的一致性约束都是结构性的（schema、契约、硬墙），唯独映射保真——意图与世界后果间的语义对齐——靠映射 prompt 的一句「无法解析 → refusal」维持，是设计里唯一接受提示词请求承担核心保真职责的位置。
- **裁决（已闭：显式接受为权衡——信任 LLM）**：映射保真整体托付模型的原生语义能力，映射面零守卫。(b) 置信信号与确认回合、(d) check 预检工具，均以「增加摩擦与成本」否决；(c) 撤销——取舍不依赖测量，测量只能改变语气、不能改变立场。放弃了什么：不适配的意图可能被最近的合法动词吞掉并完整执行（coercion），世界做了玩家没字面要求的事、无拒绝可供学习边界。为何仍然值得：
  1. 映射是概率机器，这是品类的定义性事实而非缺陷；概率误差在概率层面修复（模型能力、提示设计），用确定性机制补概率误差是以运行时摩擦购买幻觉性的安心。
  2. 重量住在裁决层：被执行的动作永远真实、自洽、可复现——即使不是玩家字面所求，也是世界里的真事件；「操作即语义」，玩家经落差学习动词边界，与「尝试 → 被世界拒绝」是同一条学习曲线。
  3. (d) 自证不成立：「何时预检」仍是提示词请求；结构守卫只改判哪一次提交发生，保证不了提交的是玩家所想。
  4. 摩擦与成本是硬通货：多一次调用、多一个决策点、多一层协议面，都是对「单 pass、双工具、语言即操作面」身份的稀释。
- **教义修订（随本条闭案）**：「约束是结构性的，不是提示词请求」的适用域收窄为裁决与表达（两扇门、硬墙、声明契约）；映射保真是唯一显式托付给模型能力的位置，已由 DESIGN §4/§7 命名为赌注。防御机制的准入判据由此二分：**结构性保证（可执行、可证伪）进协议；只能降低概率的守卫交还模型能力与提示。**
- **状态**：已闭。

## 与既有文档的关系

- 修订 DESIGN.md 时同步更新本文件索引与状态；ARCHITECTURE.md 只在裁决产生技术动作时新增条目。
- 本文不收实现缺陷（roll 键碰撞检测、提交快照成本、菜单去重碎片化等归 issue / ARCHITECTURE）。
