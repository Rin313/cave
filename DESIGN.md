# 语言游戏引擎设计方案

## 0. 公理

core 的全部承诺分三层：**公理一**陈述世界的形状，**公理二**陈述门的管辖；公理之下是**协议不变量**（公理一/二的直接推论，对一切协议变体强制）；不变量之上是**回合协议**——本引擎选定的缺省协议实例。**指不出推导来源或选择理由的引擎形态即设计警报**，未来任何特性提案必须注明触碰了哪条公理（或推论/协议不变量）、或改写了哪项协议选择，归入哪个声明面；无法归类的提案默认拒绝。

1. **世界即账本。** World = 实体（稳定 id + 名字 + 属性包）× 关系边 × 一个整数时钟，此外无物。状态侧不得有账本外的隐藏状态，动态侧不得有裁决中的隐藏变量。账本的唯一职责是让真相**可说、可 diff**：凡产生玩法后果者，其后果必可说——变更说为 Change、氛围与理由说为 Fact、时间流逝说为经裁决归因的刻数（步记录承载）；中间状态（隐藏的原因、调度、计数）可不可说，经 `internal` 属性合法存在。**名字是引擎自有词汇**：普在于一切实体、spawn 保证在场、integrity 钉住非空字符串契约，经 rename delta 可变。指称与键控的锚是 id 与裁决时读态：协议指称恒为 id，规则键控恒读当前态，名字只承担呈现。
2. **一切后果过唯一的门。** 一切状态变异经过提交硬墙（快照 → 提交 → 不变式校验 → 违反则原子回滚）。门的管辖有二：**必要性**——墙审世界，任何 bug 无法绕过；**出处**——后果只能由确定性代码产出，语言只从两端进出：意图文本进（映射层翻译为被 schema 收口的动作提案），世界腔裁决文案与表达散文出；映射/表达侧无机械门，完全托付模型能力
3. **协议不变量与回合协议。** ：**提交全序**——回滚与出处都以「之前的状态」为锚，交错的提交使二者失义（门原子性的推论）；**钟随门走**——账本二分：实体与关系是**内容**（被排序的变异对象，门的原子域——快照、回滚、保真翼、不变式皆以内容为对象），钟是**坐标**（排序本身，单向非负整数，不产生 Change、不在回滚域）。钟不设墙——墙审规则/系统产出的内容，core 自审无义；其保证是结构性的：唯一写者是落钟循环（与 commit 机械同信任级，单写者位置可机械审计），形状由 integrity 钟契约钉住；不变式的对象同样是内容——时间条件属于规则/系统谓词，坐标否决不可回滚，只会造成永久冻结。授予与执行分离：verdict 的 ticks 是授予的权威记录、随 verdict 生死（被拒授予只余尝试时价），执行与授予的一致由同一落钟循环按构造成立。推论一：**凡裁决必耗时**——门被调用即裁决事件发生，被拒的尝试同为世界事件，至少耗时价。推论二：「提交失败不回退时间」与「系统被墙否决时刻照走」（坐标不因过程损坏停摆）皆是坐标域的定义性结果；**提案者可说**——提案的取舍是后果之源，取舍者必须可归属：单意志下恒定退化为省略（ActionStep.action 即可说），多意志下须每意志一根居所引用、提交者入步记录；**感知参数化**——投影是状态的函数，函数必须带体验者参数（grounding/digestExtra/summarize 均带 player），单意志只是把参数钉在 def.playerId。**回合协议**是本引擎在不变量之上选定的缺省实例：
   - **一回合、一意志**：act 是言语行为通道，每回合恰好一个说话人；单意志是体验者参数的缺省取值。多意志不触任何公理与不变量，是未实例化的协议变体。
   - **one-shot 门闩 + 通道次序**：act 一次性提交先行，裁决先于叙述进入通道。这是表达侧唯一的机械保障，与「表达侧无语义门」配套：内容校验不可得，就按构造切断散文的两个失锚通道——回合内（锚不存在于散文之先）由相位次序封死，跨回合（散文为后续提案与叙述播种）由叙述不回流封死；二者合拢，锚定纪律在通道上按构造成立，散文对账本的唯一因果路径经过玩家——「语言只从两端进出」的机械兑现。门闩封调用而非提案：一次提交内的动作数与忠实同属语言赌注，世界逐动作裁决、刻随动作流逝。
   - **裁决驱动落钟**：「无裁决即无流逝」是触发源的选择。选裁决驱动的理由：映射是 LLM 在环，秒级延迟使实时成为另一种体验形态而非同型变体；提案对其自身结果不可见，决策是陈述不是搜索，操作重量不被稀释；近况与审计以回合为自然单位。时长是行为的后果维度：动词声明尝试时价（成败皆消耗），规则可在授予中改写实际刻数，语言可经类型化参数提案时长、由规则裁决，模型永不直接提案时间数值；钟不可 delta 寻址、只按裁决授予——「时间作为成本」的机制化，同时堵住规则顺手拨钟的通道。刻的推进是裁决边界的定义部分：apply 裁决 → 提交 → 按授予刻数逐刻落钟运行 systems；墙否决回滚 deltas 但不回退时间，授予刻数在裁决入口校验形状（非负整数，违约走不变式通道），执行与授予的一致由同一落钟循环按构造成立。刻是世界的因果步（每刻 systems 照常运行），不是回合的别名；事件流中刻步与动作裁决独立成形。

> **语言的居所**（公理二出处轴的词汇面）：**世界语言**：玩家可见的一切文字（法则理由、不变式 message、回退摘要等）与表达契约（叙述者人格、世界读法、写作纪律等），由游戏注入，永不成为状态；表达散文由模型在契约下生成，对已锚定内容的转写与渲染一律自由。**通道语言**：引擎↔模型的 prompt 脚手架、act 工具描述与校验反馈、结果视图的协议锚（未解析行、新见 id 卡）——协议基础设施，不进玩家视野；结果视图的骨架行由回合骨架（spineLines）承担：符号结构（✓/✗/⏱/×n）+ 游戏注入的原子（理由 / 事实 / label）。**言语对象**：经裁决入账的言语原文，是世界里的惰性数据，不是呈现，是状态。选用哪种自然语言是自由决策。

**推论**：

- **随机是世界的纯函数**（由公理一）：无隐藏变量——apply / 存档恢复对同一状态天然一致。机械形态：roll 是 `time#key` 的纯派生；key 需同 tick 唯一属工程纪律。
- **感知是注入的投影**（由公理一）：感知谓词不是世界的真相，是账本向消费者的投影；投影是状态的函数，函数住 def 不住账本，core 不内嵌任何投影语义，只留 core 机器（视图装配/可见性门/新见/事件投影）消费的空槽：grounding / digestExtra。缺省全见是诚实零：未声明认识论语义即账本自然态，恒真谓词不含属性语义。**施动前提（可达/可持握）不是感知**——消费者只有规则卫语句，规则侧谓词住 games 层构件与模块助手。违反现实规则的世界由 authored 形态承载。
- **动力学三分**（由公理二与 P1）：同一扇门、同一产出原语（Delta / Fact / Denial），确定性代码只有三个可运行的时机——动作裁决时（规则，管此刻的离散因果）、tick 内（系统，管此后的连续因果）、提交审计时（不变式，管永远）。
- **言语是惰性对象**（由公理一与公理二）：言语要成为世界的部分只有一条路——意图经映射层分类为类型化参数，规则对参数裁决，原文作为惰性数据随裁决存入属性 / 关系边。意义处理只发生在模型侧——映射层分类与指代、表达层引用与转述。言语的机械后果只能由 speech-act 动词物化为状态——创作协议的职责，不是任何记录通道能还的债。物化税由此显式接受为公理一/二的定义性代价：凡需在裁决中存续的叙事，须物化为 props/relations。

> **语言史的地籍**（记忆层的闭合裁决）：语言史不是公理空白——按「内容 × 出处 × 用途」各有居所。**玩家原话**：近况全量承载（映射层的指代与续接锚）+ 会话审计；作为玩法后果时经 speech-act 动词物化为账本惰性记录。**机械事件史**（内核：裁决行、刻步、拒绝标记）：近况即其最小形态，字段取舍是工程量决策；进裁决输入的形态只有账本本身（公理一的形状闭包——需历史者须物化）。**逐回合散文**：会话文件全量累积作审计，永不回流。**模型自撰纪要**：拒绝为机制——其独有贡献（无出处的叙事自传）恰是出处纪律要排除的东西。

## 1. 身份：协议、基底、作者界面

| 层 | 内容 | 身份 |
|---|---|---|
| **协议** | `Action{verb,params}` 进 → `Delta / Change / Fact / Denial` 出；提交过不变式硬墙 | 引擎的身份所在，已完成且应冻结 |
| **基底** | Entity / props / relations / clock 这套 World 形状 | 实用主义的事实账本，可用但不可神圣化 |
| **作者界面** | verbs 集合 + 四个声明面 | 受钩子准入原则约束的生长区 |

「本体论 + 规则代码」的结合是放弃三个替代方案后的幸存点：纯数据法则无明显收益（AST 解释器）；完全自由的代码沙盒丢掉可序列化、可 diff、可核验的边界；重本体（DF/CDDA 式专用结构）只在领域固定时成立，与谱系目标矛盾。「薄的可说基底 + 代码裁决」的代价是词汇看守问题。

## 2. 定位与核心主张

一种新的游戏类型引擎：**以确定性状态机为重量锚点、以 LLM 为映射与表达引擎**的交互式语言游戏。

- **操作比文字更加核心。** 玩家操作的「重量」由确定性裁决保证，AI 只负责让每次操作的反馈有文学生命力。重量住在裁决层，同一动作提案的裁决确定可复现。
- **AI 不创造规则，AI 不产生系统后果。** 数值与后果只能由规则 / 系统产出（公理二），映射层只提案、表达层只叙述。
- **忠实是赌注。** 提案是否忠实于意图、散文是否忠实于状态，判定同属语义任务——机械执行即引入第二个被信任的语义引擎（循环信任），任何机械校验都只能覆盖模型自选路径的窄面。映射不准、错位提案、一次提交任意多动作皆合法，世界照常逐动作裁决。
- **自由度上限由作者协议决定。** 引擎每少承担一分职责，作者协议的可能性上限就更高；语言的开放性决定逼近上限的体验摩擦，不构成机制层面的无限自由。

## 3. 模拟层

### 3.1 规范面：动词 + 卫语句规则

- `GameDef.verbs` 是动词表：每个动词含 `schema`（TypeBox，生成 act 工具参数校验并推导规则参数编译期类型）、`cost`（尝试时价：成败皆消耗的刻数；时间律的动词面）、`entityParams`（实体参数，供可见性校验）、`rules`（卫语句式规则函数，按序裁决首个表态即判决）。动词集由各游戏声明。
- **主体性两概念：意志与视角**：core 在主体性上只持有两件事，机械形态 = 一条协议不变量 + 一根引用。**意志**（玩家）：act 通道的说话人，每回合恰好一个（one-shot 门闩执行，无需 id）——意志不在世界里，账本里没有玩家，只有 def.playerId 这根指向普通实体的引用；它是意志在世界的全部足迹（居所），居所上的状态全是身体态，意志自身无状态。**视角**：感知函数的现值——游戏经 grounding 注入函数（感知推论），世界态决定现值；grounding 圈定「意志能点名什么」，状态视图的实体索引与之构造等价（装配线保证；可感知而不可指名的纹理走 digestExtra 的无 id 通道）。居所承担两个角色：墙的保护对象（引用自身的必要性——凡被解引用者必须可解），与体验者推导的根与兜底。**无主语动词的缺省主语是体验者，不是地址**；感知谓词的缺省视点、叙述「你」的缺省指称同属体验者一侧。体验者不是 core 概念，其正确居所是包含拓扑：地址建模为魂时以 `in` 居于当前器皿，体验者 = 链上最近器皿——视角与门随链自动跟随，无需逐钩子推导。地址之外皆非 core 概念：动词的语义主语是普通参数（entityParams）。第一人称（感知锚定居所 + 无主语动词）、上帝视角（感知全开 + 主语显式入参，神力动词照常过提交硬墙——神是动词表更大的玩家）、附身（占据 = 地址的空间迁移，视角与门随链跟随）都是这对概念的取值模式。视角以居所为缺省锚点是惯例（第一人称的机制来源），不是必然——上帝视角与多视角幽灵无视该参数。人称与受话人是呈现投影的取值：core 只保证每回合把「谁在看」以可投影形态交给呈现层——账本 name 是协议锚而非时变真相（人称不是真相，name 无变更 delta），投影可动态呈现「你」；规则理由串内联人称，动态人称由规则从世界态计算或以无主语句式交表达层补足。切换扮演同理是取值而非协议：占据与脱离即地址的 `in` 迁移、一条 delta 过提交硬墙（附身是动词，其 deltas 过门）；def 重绑是不经裁决的旁路，永不提供；角色会死会换的游戏，居所须建模为魂/叙述者等 meta 锚（居所不朽，器皿可生灭）。占据必须建模为 id 属性而非关系边。
- **规则即代码**：普通函数接收只读上下文 `Q`（world/player/time/params + `rel/relNum/roll/visible/name` 等引擎自有语义唯一入口），返回授予（Delta 列表 + 世界腔理由 + facts）、结构化拒绝（Denial）或 null（不表态交由后续规则）。按序裁决，首个表态即判决。
- **承重墙**：键控形态下规则必须以属性组合为键，禁止特判实体 id——检查双方的 `material` 硬度来裁决，不关心具体是哪个实体。泛化的另一半是数据载荷：量级与效果参数存于属性，一条规则覆盖一切可食之物——键控管「何时对谁」，载荷管「多少」，二者都是属性键控的合法形态。规则随新实体自动泛化，这是组合爆炸的唯一出路。
- **分辨率即链**：每动词一条卫语句链——顺序即优先级、弃权是唯一组合算子；优先级是作者知识（拒绝的诊断顺序即玩法文案）。键控的泛化拓扑只在 probe 的裁决地图（按法则分组的法则×动词矩阵）上被看见，运行时没有第二种裁决结构。
- **法则身份纪律**：同一概念一个 law id、一份世界腔理由。横切法则住 games 层构件：法则是 Rule 工厂（施法主语＝动词参数名，显式绑定），接线是一行可见调用、位置即优先级、例外法则插在其前。
- **两种创作形态**，共用同一套裁决瓶颈与提交硬墙，core 不感知形态差异：
  - **键控形态**：规则按属性组合键控、随新实体自动泛化；
  - **authored 形态**：互动逐实体书写（每条一个卫语句子句 + 兜底），实体生灭、动态拓扑、持有物改写互动结果。
- **拒绝**：结构化 `Denial`（law/reason/debug），理由以世界腔内联在规则文本里，缺省回落 `messages.noResponse`。拒绝不携带涉及实体：指称的居所是 action 参数（结构化）与法则理由（世界腔），第三方指称可由 law 在裁决时世界态上重算。`deniedBy: "rule" | "invariant"` 法则否决是玩法（世界性理由），墙否决是必要性拦截；可见性门归 rule（感知是世界真相，拒绝耗尝试时价）。二者是探测与审计的语义基础，不依赖理由字符串匹配；全部规则未表态时由引擎闭合回落（law `action.unanswered`）。覆盖面没有机械判据：`sim probe` 穷举可见域产出按法则分组的裁决地图供人判读兜底落点，缺口判断属创作者，core 不承载覆盖语义。
- **前置门两站点**：静态形态（动词存在、schema）错在裁决之外——工具边界由 pi 校验拒绝（错误回模型、可重试、门闩未耗），内核收到同类动作即调用方违约，前置条件违约即抛`ProtocolViolation`；世界真相在裁决之内——可见性门（感知是世界真相，拒绝为世界性、耗尝试时价；门的世界腔只依赖公开状态：幻觉 id 与隐藏实体同一文案，未入域的指称在尝试行原样回显、不解析名字）→ 规则。施动前提等语义前提一律写成第一条卫语句规则，谓词由 games 层构件/模块助手供给

### 3.2 动力学面：系统与时间

- `systems` 是按序执行的纯函数注册表（每 tick 运行，产出 deltas/facts），承载连续因果；fact-only 纯氛围输出合法，零状态变更的氛围事实同样进入表达输入。
- **时长是裁决授予的后果维度（时间律的机制面）**：`VerbDef.cost`（缺省 0）是尝试的时价（成败皆消耗）；规则可在授予中以 `ticks` 改写实际流逝；时长可经动词 schema 的类型化参数由语言提案、由规则裁决。apply 是完整的裁决边界：裁决 → 提交 → 按授予刻数逐刻落钟运行 systems，产出并入表达输入，世界能在一次提交的多个行为之间反应；墙否决回滚 deltas 但不回退时间——尝试本身仍消耗动词时价。无动作入裁决即无流逝，空提案与未解析回合世界静止；刻是世界的因果步，不是回合的别名。授予的刻数必须可说：静默刻（零产出）在结果视图与近况中仍须表达时间流逝——动作步的 ticks 是授予的权威记录。事件流中刻步（TickStep：时刻 + 变更/事实）与动作裁决（ActionStep）是两种条目形态——刻是世界的因（提交失败不回退时间），不是意志的果。
- 随机经 `Q.roll(key, sides)`（随机推论；key 需同 tick 唯一，可含实体 id；同 tick 内重试同一判定得同一命运——时间流逝是命运变化的唯一来源）；时钟由行为驱动（时间律）；多时间尺度（刻/日/月）由游戏自持计数器 + systems 内判定。

### 3.3 必然性：不变式硬墙

- 提交受两翼审查，任一违反即**原子回滚整个提交并拒绝**。**执行翼**审「裁决被如实执行」（见次条）；**状态翼**审「执行后的世界成立」：core 默认恒挂引用完整性与注册表类型契约（id 唯一、卡片契约——id 与名字为非空字符串、钟契约——time 为非负整数、id 型属性——标量或引用数组——与关系端点指向存在的实体；注册属性值与声明类型一致，number 拒非有限值——NaN/Infinity 经 JSON 序列化即静默变 null，是账本腐蚀通道；null/缺席为缺省惯例放行，any 显式豁免；词汇闭合——实体属性键 ⊆ 注册表（type 层封闭、token 层开放，与动词表同构：未声明词汇不入账；动态键值对走关系边、自由值形状走 any 与 tags））；游戏追加领域不变式；守恒类种子读 `InvariantCtx.genesis`（实际起点世界的冻结快照，存档恢复与变体开局同样成立）。
- **执行校验（fidelity，公理一「后果必可说」的执行侧形态）**：每条 delta 在其应用时刻必须可执行（逐条校验而非提交前预检——同一授予内 spawn 后 set 是合法书写）——目标实体与关系端点必须存在、spawn 的 id 必须未占用、rename 的名字须为非空字符串、inc/relInc 的现值必须是有限数（缺席/null 按 0 的既定语义，这是承重墙泛化的承重约定；非数现值拒绝而非静默改写类型）、一切数值后果必须有限（NaN/Infinity 无法经 JSON 存活，序列化即静默变 null 的腐蚀通道在提交侧封死）。幂等跳过的唯一判据是「目标状态已成立」：relSet 删不存在的边成立（无边即状态，悬空端点之间本不容边）；set 同值与增量零效果以目标存在为前提——主语不存在的「已成立」不可判定，存在性拒绝在前，永不回落为跳过。推导：裁决理由与变更流必须互证——静默丢弃 delta 即叙述对账本说谎；probe 将核心级不变拒绝（无世界腔理由）报为 bug。
- **不变式可见提交（两种形态，同一接口）**：`InvariantCtx` 除 `genesis` 外携带 `changes`（本次提交的全部变更，含 spawn/despawn 与 `src` 产出方标识）。**状态不变式**只读世界——守恒类，防总量漂移；**过渡不变式**读提交——provenance 类，防错误再分配。每条规则/系统的提交独立过墙，过渡不变式的粒度即单次提交。类型契约管词汇（注册属性值的形状），过渡不变式管语义（这一次提交是否必要）。
- **`invariant` 是独立的否决来源**：墙的否决与法则否决是不同的语义来源——法则否决是玩法（世界性理由），墙否决是必要性拦截（规格违反信号或戏剧性必然），审计与探测应能区分。
- 游戏不变式的 message 是游戏撰写的世界腔，直接作玩家文案；core 完整性违反只留 debug 诊断，回落通用兜底文案。
- **意志的居所在墙的管辖内**：`playerId` 是 def 指向世界的唯一数据引用（意志的居所：墙的保护对象 + 体验者推导的根与兜底；感知钩子以它为缺省首参但可无视——缺省全见全达即上帝视角），integrity 不变式恒查其可解；初始世界同样过墙（构造期校验，genesis = 自身、changes = 空）——「不变式管永远」包括 t=0。由此显式声明两个立场：**指针不可重绑**（居所引用在 def 层恒定；附身/换躯不改指针——占据是地址的 `in` 迁移）；**居所不朽，器皿可生灭**——墙保护的是意志的居所而非任何身体：终局是状态不是删除。占据建模为 id 属性时，器皿之死被 id 引用契约拦下——意志的迁出必先作为 delta 入账（迁移可说，静默回退不可说：公理一「凡后果必可说」由墙机械兑现）；推导兜底只覆盖「未入链」态（hostOf 缺省地址自身），不豁免迁移的可说性。占据期间地址迁移的合法性由过渡不变式钉住。

### 3.4 生灭与拓扑

- Delta 原语：`set / inc / relSet / relInc / rename / spawn / despawn`（relSet 值 null 即删边——拓扑收缩与生长对称，存储边永不持 null）。提交产出按基底类别同构分形的变更记录 `Change`（kind: prop/rename/rel/spawn/despawn——rel 携带完整边端点，next null 即删边；rename 携带 prev/next 名字）。生灭原语配合 relSet 建径/删径让世界拓扑生长与收缩；despawn 级联清理关系边，悬空 id 引用由完整性硬墙回滚。
- **生长与收缩对称**：硬墙对悬空引用 fail-closed ；引用清点原语 `refsTo(def, world, id)`（按注册表 `type:"id"` 枚举指向该实体的 (entity, prop)，标量与引用数组同覆盖，语义无关的机械操作），清理策略（置空、转移、级联生灭）是游戏语义，由规则决定；容器级 despawn 同理由此表达（先迁散子女再 despawn）。
- 关系边表 `world.relations` 表达社会 / 叙事状态（信任、记忆、派系），表达层格式化为世界腔文本。

### 3.5 感知面

- **状态视图是唯一装配线**：prompt 的状态视图——可见实体（grounding）× 注册表过滤（internal 不进 prompt）× 关系端点可见过滤；游戏派生纹理 `digestExtra`（出口、随身清单等）入独立 `extra` 键。参照域契约由构造保证：视图实体索引 ≡ 可见性门的权威集——模型看得见的才可指名，可指名的必看得见；`digestExtra` 无 id 承诺，携带可指名 id 时配合非 entityParams 参数消费（地点恒可指名，由法则层回答）。act 结果的新见段是同一条装配线的回合内增量：本回合新进入参照域的实体（规则/系统生灭、移动揭晓）以状态视图同形的实体卡（含 id）承载——纹理与指称锚在裁决当回合闭合，不欠下一回合的 digest 重注入。
- **事件投影（感知推论在回合协议上的闭合）**：act 结果视图与回退摘要是世界对体验者的应答（受话人是体验者参数的现值），与状态视图消费同一个 grounding（回退摘要指缺省骨架投影；声音覆写 summarize 读原样 steps，与理由/Fact 同属作者特权）。变更行按「全部指称在场」投影：主语、关系端点、一切解析为实体的取值（与 fmtValue 的解析对齐），任一缺席即整行沉默。判定跨度是步的参照域 {步前态, 步后态}（感知分辨率继承提交边界的离散化；任一端在场——单端谓词在「进入/离开感知域」的原型知觉上自相矛盾），由 Simulation 产出步时计算（动作步前态复用可见性门，刻步按系统提交逐个取），随步携带——与 Change.prev/next 同一本体：提交后不可重算、只存在于记录。被投影的 despawn 不铸造合法名字，引用隐藏离场者的行随之沉默；describeAction 的机械指称解析同受参照域管辖（域外 id 原样回显——模型自己的词不是新信息）。沉默行的净效果由状态投影承接（新见/离场），模糊知觉的唯一通道是作者 Fact。理由与 Fact 不过投影：规则铸造的世界语，作者特权是唯一合法越权口——与「拒绝不携带涉及实体只约束门」是同一条纪律在时间维的落点。模型认识论由此闭合：Π(S_now) ⊕ 被感知事件 ⊕ 作者 Fact。
- **投影按消费者分类**：进协议通道（约束裁决可行集）的投影，与真相的一致性必须机械保证；只服务工具与呈现的投影（裁决地图、意图菜单）永不进协议通道，形态自由。
- **感知面的准入门**：入 GameDef 感知面的资格 = core 机器在协议通道内消费它，容器包含树等空间语义是游戏侧自愿接入的构件。

## 4. 映射层

玩家输入自由文本；LLM 职责是解析为动作提案列表 `[{ verb, params }]`，实体命中可见索引，动词取自已声明动词表。

- **纯语义解析**：无关键词匹配、无别名表、无语言限制——动词与实体由 LLM 从 `name + attrs + 上下文` 纯语义接地；选中文本作为实体接地锚点。
- **one-shot 门闩**：模型先调 `act` 一次性提交动作提案（或以空提案拒绝），门闩封闭变异窗口；`act` 工具结果是世界回应的唯一载体，叙述只跟随它。
- **拒绝契约**：模型不撰写拒绝理由。拒绝的唯一判据是**构造性**：构造不出合法提案（动词表承载不了意图，或意图指称落不到可见实体）时提交空 actions；能构造的动作一律直接提交，由规则层裁决——「预计被拒」不是拒绝的理由而是提交的理由，「是否合理」的判断属于世界、不属于映射。

## 5. 表达层

AI 读状态视图（core 装配，回合 prompt 注入）+ 本回合变更（act 工具结果承载，经 props 注册表 label 做语言无关线性化）+ 法则 facts，输出该状态的文字化。

- **表达契约住 def（`voice`，出口侧的作者供稿面）**：入口侧的作者指令已机制化（动词表、schema 描述），出口侧`GameDef.voice` 补齐：静态世界语言文档，原样注入 system prompt；协议块（映射契约、保真与指称纪律）是引擎机械的说明书，不随游戏漂移。缺省即裸契约。
- **保真义务统一陈述，不逐属性划线**：叙述不得发明无锚内容（锚 = 状态/变更/法则事实）、不得与状态矛盾；对已锚定内容的转写与渲染一律自由。
- **内部属性隔离**：`internal: true` 的属性不进序列化 / 变更线性化——模型根本看不到（公理一的不可说逃生舱）。
- **回合骨架（spineLines）**：事件流的规范单行渲染——符号承担结构（✓/✗/⏱/×n）、语言词全部来自 messages/label/规则文案，internal 变更恒滤。结果视图（模型）、近况（compact 投影）、控制台与玩家回退摘要共用同一线级机械；段头等通道语言只存在于模型视图，玩家兜底路径结构上不含通道语言。回退摘要缺省 = 骨架投影（空步回落 noResponse），`summarize` 是可选的声音覆写——文学化兜底是选择，不是结构义务。
- **叙述不回流**：回合输入只有当前状态 + 变更（act 工具结果）+ 近况窗口；会话文件仍累积全量消息作审计。
- **场景呈现服务**：表达层的场景模式可独立调用——无意志、无 act 通道、无时间流逝，不是回合。正当调用时刻的判据：账本中存在可说而未被说过的状态内容。

## 6. 状态机谱系

创作者自选落点，引擎不预设复杂度：

```
完全无状态机 ←────────────────→ 极复杂状态机
(纯AI聊天)      (Era/DoL级别系统)
 灵活性最大                        确定性最强
 游戏性最弱                        游戏性最强
 AI全权负责                        AI只做表达
 玩家行为无重量                    玩家行为有裁决重量
```

这张图是品类史的修辞，不是本引擎的参数空间。真实自由度至少是三个不共轴的旋钮：**裁决重量**（法则与不变式密度，决定玩家行为的可复现约束）、**动作空间广度**（动词 × 实体 × 规则授予面，决定可尝试的操作集）、**叙事依赖度**（须物化为 props/relations 才能存续的连续性，决定作者为故事连贯所付的税）。

身份下界：门的**在场**是身份，门的**通过量**是分级参数。**出局测试（「另一品类」的严格用法）**：一个交互形态出局，当且仅当违反公理一/二或协议不变量——引用时必须指名违反哪条、在哪一点；没有指名的出局判决无效。纯 AI 聊天无门可过（违反公理二），多意志、实时、开放提案不触公理与不变量，不是另一品类，而是**未实例化的协议变体**。重量锚点的强度由游戏声明的法则与不变式密度决定，不来自 core 的原语集合——开放世界极的「一致」就是一小撮领域不变式，不是法则穷举。

## 7. 交互模式：文本即操作面

玩家用任意文字表达操作意图，可选中场景散文片段作实体接地锚点。快捷键 / 菜单只是「输入意图文本」的快捷方式，本质仍是自由文本。

- 玩家定义交互面：阅读深度直接转化为操作空间广度。
- 裁决保持精确：意图与实体命中状态机边界后才产生后果。

## 8. 引擎接口：四个声明面

GameDef 的字段按四个声明面组织（概念分类）。**钩子准入原则：一个钩子要有资格存在，必须命名某种内核既无法推导、又无法在不破坏某类游戏的前提下取缺省的信息；且必须归入某一声明面、注明触碰的公理或推论。跨栏的重新设计。** 

**核心增长准入：只有「无法表达」（而非「表达痛苦」）有资格驱动 src/core 的机制增长；表达痛苦是游戏侧构件与库层的信号。侵蚀度量：每新增一个复杂场景记录 src/core 的 diff——预测为零，首个以「无法表达」立项的非零 diff 即设计警报。**

```ts
const game: GameDef = {
  // —— 规范面：何为可能、何为必然
  id, title,                 // 实例元数据
  verbs: { ... },            // 动词表：label/description/schema/cost/entityParams/rules
  invariants: [...],         // 提交后硬墙（core 默认恒挂引用完整性）
  playerId,                  // 意志的居所：def 指向世界的唯一数据引用（integrity 墙管辖；体验者推导的根与兜底——现宿主是包含拓扑读数，不在此钉死）
  world,

  // —— 动力学面
  systems: [...],            // 每 tick 的连续因果（时长由裁决授予：VerbDef.cost / Verdict.ticks）

  // —— 感知面
  grounding,                 // 可见实体索引（状态视图实体索引 ≡ 此集，装配线保证）
  digestExtra,               // 状态视图派生纹理（无 id 承诺）

  // —— 词汇面：世界的语言
  messages,                  // 必填：core 产出的用户可见文案
  voice,                     // 表达契约：叙述者人格/世界读法/写作纪律（世界语言，core 原样注入 system prompt，缺省裸契约）
  props: {...},              // 属性注册表：type/label/internal
  memoryLimit,               // 近况窗口回合数
  summarize,                // 可选：回退摘要的声音覆写（缺省=引擎装配的回合骨架投影 spineLines）
};
```

## 9. 检查的边界：墙、探针、审计、赌注

公理一/二承诺两种必要性（后果必可说、唯一门），但不是一切保证都能机械兑现。一个检查的权力由三问钉死：**判据能否从检查点合法可见的数据上判定**（可判定性）、**误报能否零化**（恰好性）、**成本能否与提交同阶支付**（时机）。

| 站点 | 时机 | 信任来源 | 成本上限 |
|---|---|---|---|
| **墙** | 每次提交，必然 | 机械、恰好（零误报；拒绝是世界事件——世界不道歉） | O(提交) |
| **探针** | 离线，可枚举域 | 机械枚举 + 人判读（发现是提问，误报由人豁免） | O(域)，截断显式 |
| **审计** | 随事件流持久化 | 记录完整性（不判定，只保真） | 只录重推导不出者 |
| **赌注** | 运行时语义属性 | 两端构造约束损害面（无检查） | 显式命名赌什么、在哪被人读 |

归站程序（按序判）：判据不可从合法数据判定 → **赌注**（语义属性）或**审计**（历史事实）；可判定、恰好、且违反绝不可入账 → **墙**；可判定但存在合法例外、或成本 O(域) → **探针**。

### 各站点的纪律

**墙审事实，不审意义。** 墙的对象恒为状态侧恰好性质：有效性（integrity）、执行保真（fidelity）、守恒与出处（游戏不变式）、ticks 形状、以及**可说性——世界在裁决入口与出口之差必须被本次裁决的 deltas 完全解释**。语义门（风格检查、禁词扫描、散文断言校验）永不做：引入第二个被信任的语义引擎，违反公理二表达侧立场。

**探针不冒充墙。** 探针的发现是证据不是执行：凡每次提交都必要的违反必须落为不变式晋升进墙，探针永无运行时否决权。探针可以不完整，不可以谎称完整——穷举域与截断显式声明，漏报域随发现一同报告。

**审计只记不可重推导者。** 决策是代码，代码不序列化：凡可从已记录输入 + def 重推导的不入审计流——双真相源必然漂移。反之，人类判读所必需且无他处可寻的必须全量。审计的失败模式是选择性记录：审计一旦有立场即不再是审计。

**赌注只构造损害面。** 映射与表达的忠实不可判定，一切输出校验都退化为模型自选路径的窄面（循环信任）。赌注的纪律是显式命名：赌什么属性、兑现在哪里被人读（e2e 会话阅读；编译与 verify 通过是伪信号，不构成兑现）。

## 10. 开放问题与非目标

**非目标**（显式不做）：

- 多 actor 离屏自治（NPC 由 LLM 驱动的 RimWorld 式野心）
- 词表式语言约束（扫描器 / forbiddenTerms 类）：语言是 LLM 的原生能力
- 表达侧逐属性忠实分界（stylistic 类润饰旗标、散文断言检查器）
- 模型自撰的回合纪要通道：自述偏差经上下文跨回合自我放大。

## 附录：灵感来源

| 游戏/类型 | 贡献的正向参考 | 本方案拒绝的负面参考 |

|---|---|---|
| MUD | 命令解析复杂度即技能曲线；trigger/别名编程 = 玩家在编程，元游戏层 | 固定命令 DSL 让操作固化；社群穷举最优序列后游戏死亡 |
| NetHack | 穷举交互空间的探索模式；物态/物品/怪物互动的操作空间广度 | 输出端用 ASCII 字符而非自然语言，浪费了输入端的交互复杂度 |
| era 系 | 基于状态机的交互模拟器；所有操作映射到庞大数值系统；可重玩性来自系统深度 | 交互脚本是预编码的，操作被固化 |
| Degrees of Lewdity | 多资源轮耦合的风险决策设计 | 回合制+固定选项制造决策节奏，但操作集被预设 |
| A Dark Room | 极简操作中"造火"式的 gameplay reveal（操作中发现而非 lore dump） | 放置式点击循环，可用鼠标连点器，太蠢 |
| Dwarf Fortress | material system：交互规则降维为属性运算 | 仅覆盖物理/化学属性，覆盖不了社会/叙事状态 |
| Visual Novel | — | 固定分支 = 翻书，AI+随机分支只是加噪音，无法深挖 |
| Lifeline 系列 | 真实时间流逝制造"陪伴在场感"——时间节奏可剥离出来作为真实决策压力（接上状态机） | 本质是"带时钟的 VN"：选择落在固定分支，真实感靠拖延内容制造而非生成内容。LLM 已使该类别整体退役 |
| 文字遊戲 (Word Game) | "文字作为可交互实体"的直觉有吸引力 | 把 NetHack 的"字符构建视图"推向极端：世界 = 汉字瓦片（墙是一排"墙"字，玩家是"我"字），字形既是视图又是谜题机制；文字沦为退化版图形引擎，文学性与操作重量双失。且谜题为手工设计固定关卡、无状态机，属"固化操作"的另一形态 |
| AI chat/跑团 | — | 无系统约束的即时幻觉，玩家行为没有实际重量；"不是游戏" |
| 风之旅人 / What Remains of Edith Finch | 文本表达应向电影级渲染靠拢的标准 | — |
| Minecraft | 方块即原子：世界由可组合的最小元素构成，任何交互都在元素层面成立，玩家自由度来自系统组合而非预编码交互 | 3D 方块空间表达不了叙事/社会状态；"破坏-放置"覆盖一切，无因果链 |
| Terraria | 发现式合成：大量可交互物品 + 探索中揭晓组合配方，玩家考古游戏系统 | 配方仍是预编码集合，穷举后操作固化（同 MUD 死结） |
| Don't Starve | 生存资源耦合 + 时间压力：饥饱/理智/血量互锁，季节昼夜驱动决策节奏，饥饿把"时间"变成真实资源 | 行为树/预设配方使行为固定；"活着"本身有重量但可穷举 |
| RimWorld | AI 叙事生成：pawns 的性格/关系/事件与模拟耦合，故事从系统状态中涌现而非手写分支 | 事件是预设叙事碎片，触发式而非涌现；操作大多是菜单指派 |
| Kerbal Space Program | 真实规则即重量：玩家用物理零件自由组装，结果由物理模拟裁决，轨道力学本身成为技能曲线；系统组合产出意料之外的结果 | — |
| Mount & Blade | 开放世界沙盒：玩家在世界中自由定位自己，经济/战争/声望由系统模拟，无主线 | 战斗与治理是预设操作层，系统耦合浅 |
| Kenshi | 系统驱动世界状态：派系战争、城市兴衰、经济由模拟产生，玩家行动有真实后果且无角色预设 | 角色成长是"做事累积数值"的浅系统，无深交互 |
| Cataclysm: Dark Days Ahead | 极端深度的物品交互：一切皆可拆解/改造/组合（车辆系统），时间流逝是持续压力，纯文本的交互复杂度巅峰 | — |
