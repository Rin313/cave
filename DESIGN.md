# 语言游戏引擎设计方案

## 1. 定位

一种新的游戏类型引擎：**以确定性状态机为重量锚点、以 LLM 为映射与表达引擎**的交互式语言游戏。

核心主张：

- 文字不是核心，操作才是。
- AI 不创造规则，AI 不产生系统后果。
- 玩家操作的"重量"由状态机保证，AI 只负责让每次操作的反馈有文学新鲜感。

## 2. 设计依据（对既有文字游戏的分析结论）

1. **信息提取本身是游戏性**——从文字噪音中定位关键信息的能力替代图形游戏的"反应时间"成为技能度量。
2. **操作成本 = 决策权重**——文字操作比点击慢，每个操作更重，玩家决策更审慎。
3. **系统透明度差异**——文字能描述图形游戏无法表现的复杂度，代价是玩家必须心理建模整个系统。
4. **元游戏层**——MUD 的 trigger 编程、NetHack 的 wiki 考古是文字游戏独有的 meta 玩法；本方案中对应物是"重量边界测绘"（§3 原则 4）。

## 3. 核心原则

1. **模拟层是唯一真相源。** 一切游戏状态、后果、数值由确定性状态机决定，不接受 AI 篡改。
2. **状态机是硬边界。** 触及状态机的操作必须严格可预测；不匹配的输入，AI 拒绝模拟。
3. **状态机之外的一切，AI 可以自由发挥，但不产生系统层面的重量。**
4. **拒绝也是游戏的一部分，且理由必须来自世界法则。** 玩家通过试错测绘"什么行为改变世界"，这本身就是元玩法；拒绝的理由是线索（"铜太软，撬不动石门"告诉你材质有意义），不允许用实现术语（"不是容器""不可见"）回应。
5. **没有通关公式。** 状态空间由模拟层保证复杂度，AI 不制造数值不平衡。对既有文字游戏，"固化操作"（社群穷举出最优序列后游戏死亡）是真正的死结；本方案通过情境化、随状态动态变化的操作集与表达层的修辞新鲜度破局。
6. **表达可证伪。** 玩家看到的一切文字都必须能从状态真相核验：叙述只引用状态派生的实体与变更，幻觉无处藏身。表达层是强制约束（schema + 后置校验），不是提示词请求。

## 4. 三层架构

```
模拟层 → 映射层 → 表达层
```

### 4.1 模拟层（确定性）

- NetHack / DoL / C:DDA 级别的法则网络：实体、属性、关系、法则组成的约束网络。
- 接受**结构化操作**（op），返回新状态与结构化结果。
- 统一模式：**原子操作 + 法则层裁决**
  - 只实现最原子的基础规则：`apply(a, b)`（用 a 作用于 b）/ `move(e, dest)`（拿取、放置、装入）/ `set(e, p, v)`（请求改变属性）。
  - 每条法则（law）对操作做裁决：`granted` + 世界性理由 + 变更列表；多条法则按声明顺序短路，全部否决时落到 denyAll（状态不变）。
  - **操作的组合由 LLM 自由提出（提案），世界的回应由法则层确定完成，LLM 不参与任何状态变更。**
  - `解下铜戒` → LLM 提案 `set(ring, attachedTo, null)` → 法则 `detach` 裁决返回变更 `[attachedTo→null, in→player]`。
  - 拒绝理由必须来自法则（世界性的）；拒绝本身就是重量边界测绘的线索。
  - 代价：创作者负担从"写交互"转为"声明法则网络"；未覆盖的操作用世界性 denyAll 回应。
  - **法则必须按属性组合键控，不能按实体键控**（承重墙）：法则只读实体的属性/关系来裁决，禁止特判实体 id。这让法则随新实体加入自动泛化（`pry` 检查双方的 `material` 硬度，不关心是不是铜戒），也是组合爆炸的唯一可行出路。属性组合系统（DF material system 风格：实体属性集 + 操作作为属性解析器）是天然实现手段。
  - **法则无洞**：完整性检查工具穷举可见实体的 apply/move/set 组合，报告会落到 denyAll 的操作，作为作者预警——世界法则的洞就是模型幻觉的诱因。denyAll 是必要的终端兜底（未覆盖的操作用世界性理由回应），不追求零洞；对模型**真会试**的操作给高信号理由，denyAll 本身保持世界腔、不泄漏实现术语。
  - 意图表降级为可选"规则捷径"（高频明确操作的确定性直通），不再作为交互的主要出口。

### 4.2 映射层（LLM，双出口 + 结构化拒绝）

玩家输入是两段自由文本：**意图文本**（任意语言、任意表述，可含完整句子）与可选**选中文本**（场景散文里选中的片段）。LLM 的职责是把它们解析为**操作提案列表**（`[apply/move/set]`），实体必须命中可见实体索引。

可靠性实现手段：

- **Function calling**：把 `act(ops)` 暴露为 tool schema；LLM 提案原子操作，实体 id 只能取自已可见实体。
  合法性校验（可见性、条件、可访问性）发生在 tool call 内部，由法则层逐条裁决。
- **纯语义解析**：操作与实体完全由 LLM 基于自由文本 + `name + attrs + 上下文` 判断，**无关键词匹配、无别名表、无语言枚举限制**。
- **选中文本可选**：有选中则作为实体接地锚点；无选中时从意图文本本身解析实体（如「解下铜戒」「把蜡烛放进木箱」）。
- **双 pass 分离（与表达层彻底分离）**：
  - 映射 pass 只产结构化结果：`(操作提案列表)` 或 `(结构化拒绝：标签 + 世界性理由)`，**不产散文**。
  - 表达 pass 在状态变更后独立运行（见 §4.3），面向玩家；玩家的唯一信息来源是表达 pass。
  - 这切断两条幻觉路径：模型不调工具却叙述动作、模型叙述未发生的后果。
- **规则捷径**（未实现，未来可选优化）：`take X` / `open door` 等高频明确操作可用确定性解析直通，不绕 LLM，降成本与延迟。
- **拒绝生成**：无法解析（实体不存在、语境荒谬）或法则否决时，映射 pass 返回结构化拒绝，理由由法则给出，状态不变。

### 4.3 表达层（LLM，可证伪的文本生成）

- AI 读结构化状态 + 本回合变更列表（changes），输出该状态的文字化表达。
- **忠实性约束（强制，非提示词）**：
  - 输出 schema：`entities_used: [可见实体ID]` + `facts: [从 (状态 + changes) 派生的断言]` + `text`。
  - **后置校验器**：`text` 中出现的实体名必须 ⊆ 可见实体名；`text` 不得断言状态中不存在的实体/属性、不得与状态矛盾。**changes 是唯一的新事实来源**——状态蕴含的旧事实（陈设、属性、位置）允许描述，新的事实只能来自本回合 changes（幻觉无处藏身）。
  - 校验失败 → 带错误反馈重生成一次；仍失败 → 回退为结构化摘要（强制可证伪）。
- **叙述不回流**：表达 pass 的输入只有当前状态 + changes，无旧叙述全文（幻觉不固化）；长期记忆用结构化摘要（落地 §9 上下文可裁剪）。
- 生成即时、可流式输出，覆盖表达层延迟。

## 5. 状态机谱系

创作者自选落点：

```
完全无状态机 ←────────────────→ 极复杂状态机
(纯AI聊天)      (Era/DoL级别系统)
 灵活性最大                        确定性最强
 游戏性最弱                        游戏性最强
 AI全权负责                        AI只做表达
 玩家行为无重量                    玩家行为有精确重量
```

- 允许纯 AI 聊天端点存在（完全无状态机 = 自由度最大的普通模拟应用）。
- 引擎不预设状态机复杂度，由每个游戏自己声明。

## 6. 交互模式：文本即操作面

- **意图文本**：玩家用任意文字表达想执行的操作（「拿起」「点燃」「解下铜戒」「把蜡烛放进木箱」），任意语言、任意表述。
- **选中文本（可选）**：玩家可额外选中场景散文中的片段，作为实体接地锚点。
- **执行**：两者连同当前状态提交映射层，LLM 解析出操作提案（apply/move/set）后交法则层裁决。
  - 意图「解下铜戒」+ 无选中 → `set(ring, attachedTo, null)` → 法则 `detach` 裁决
  - 快捷键/菜单只是"输入意图文本"的快捷方式，本质仍是自由文本。

关键特性：

- 玩家定义交互面——阅读深度直接转化为操作空间广度。
- 解析失败也是游戏的一部分（意图「拿起蜡烛光」→ "无法抓取"，玩家学到"光不是实体"）。
- 操作始终保持精确——意图与实体命中状态机边界后才产生后果，LLM 不创造系统后果。
- 与 AI chat 的本质区别：无系统约束；与 MUD/roguelike 的本质区别：操作集是情境化、动态生成的，不是预设热键。

## 7. 游戏循环

```
1. 表达 pass 将当前状态写成场景文本 → 展示
2. 玩家阅读 → 可选中文本任意片段（也可不选）
3. 玩家输入意图文本（任意语言/表述；快捷键/菜单只是快捷输入方式）
4. 引擎将 (意图文本, 选中文本?, 当前状态) 提交映射层
5. 映射 pass：LLM 解析出操作提案（apply/move/set）
   a. 法则层逐条裁决 → 状态变更 + changes 列表
   b. 无法解析 / 法则否决 → 结构化拒绝（世界性理由），状态不变
6. 表达 pass 基于 (新状态, changes) 生成新文本 → 展示
7. 回到 1
```

## 8. 引擎接口（创作者只需实现状态机）

```python
class StateMachine:
    def init(self): → state                  # 初始状态
    def apply(self, state, action): → (new_state, result_desc)  # 执行操作
    def validate(self, state, action): → bool  # 操作合法性
```

引擎自动处理：LLM 上下文管理、选中文本→实体语义映射、快捷键绑定、场景文本生成、拒绝/文学回应生成。

## 9. 关键工程性质

- **上下文可裁剪 + 叙述不回流**：模拟层是唯一真相源，LLM 无需全量历史——上下文只需"当前状态 + changes"，旧叙述不回流（幻觉不固化），长期记忆可裁剪或结构化摘要。这是相对 AI chat 方案的根本工程优势。
- **成本/延迟**：每次操作 = 1 次映射调用 + 1 次表达调用（双 pass，流式）。规则捷径可削减高频操作成本。回合制游戏可容忍该延迟。

## 10. 原型验证计划

先验证完整闭环（表达→选取→映射→执行→再表达），核心指标是**接地一致性**——不测映射层孤立准确率，因为映射成功率依赖表达层接地质量：

1. 最小种子状态机：一个房间、若干带属性实体 + 基础法则网络（基线场景已覆盖：基础流程 / 火蔓延与燃尽 / 撬门拒绝 / 楔门与解卡 / 铁钎撬木箱 / 火把入箱引燃）。
2. Function-calling 映射层（`act(ops)`）+ 实体接地 + JSON schema。
3. 跑 50 条刁钻交互用例：否定、跨句指代、隐含宾语、怪异措辞、歧义实体。
4. 量出指标：
   - **接地一致性**——表达 pass 散文中的实体能否 100% 回链到状态实体，映射能否从散文实体准确落到预期的模拟层效果。
   - **幻觉率**——叙述声称的事实与 changes 的交集覆盖度（§4.3 后置校验器量化）。
   - **拒绝质量**——拒绝理由是否来自法则（可追溯，不得为实现术语）。
5. 通过后再扩展：法则网络（材质、组合、空间、时间）、忠实性约束松紧调参（§4.3）、自定义快捷键系统。

## 11. 待确认的开放问题

- 法则网络的表达力边界：创作者能在多大范围内声明法则？物理/化学法则易覆盖，社会/叙事状态（"国王在骗你""她记得你侮辱过她"）需自写法则网络。
- 空间建模：多房间 / 开放空间、位置的多种语义（手持 / 放置 / 装入 / 嵌住，C:DDA 式）如何落地。
- 时间驱动的持续世界：tick 法则的因果链覆盖度（法则完整性检查），时间本身是否成为决策压力（Lifeline 式在场感）。
- 跨回合指代如何落地（"把它放了"——它指什么？）——无选中时意图文本自带实体解析已有雏形（见 §4.2），候选方案：活动实体索引 / 澄清机制作为玩法的一部分。
- 拒绝是否写回状态痕迹（"你反复试了试，门缝里多了些铜粉"），让测绘有痕迹。
- 交互 UX：终端/前端任意文本选段 + 快捷键焦点如何落地（原型期实测）。
- 忠实性约束的松紧参数：接地字段粒度 vs 自由字段宽度（§4.3）。
- "新游戏类型"的最终定义由第一个伟大的游戏作品给出，而非引擎本身。

## 12. 参考与灵感来源

| 游戏/类型 | 贡献的正向参考 | 本方案拒绝的负面参考 |
|---|---|---|
| MUD（北大侠客行） | 命令解析复杂度即技能曲线；trigger/别名编程 = 玩家在编程，元游戏层 | 固定命令 DSL 让操作固化；社群穷举最优序列后游戏死亡 |
| NetHack | 穷举交互空间的探索模式；物态/物品/怪物互动的操作空间广度 | 输出端用 ASCII 字符而非自然语言，浪费了输入端的交互复杂度 |
| era 系 | 基于状态机的交互模拟器；所有操作映射到庞大数值系统；可重玩性来自系统深度 | 交互脚本是预编码的，操作被固化 |
| Degrees of Lewdity | 多资源轮耦合的风险决策设计（意志/疲劳/创伤/暴露度互相影响） | 回合制+固定选项制造决策节奏，但操作集被预设 |
| A Dark Room | 极简操作中"造火"式的 gameplay reveal（操作中发现而非 lore dump） | 放置式点击循环，可用鼠标连点器，太蠢 |
| Progress Quest | — | "零玩家游戏"极端：建号后角色全自动升级打怪，玩家零输入。无操作=无决策=无技能度量，连放置游戏的点击都省去，玩家沦为观众，纯数字膨胀的斯金纳箱；谱系上位于"玩家行为无重量"的最底端（玩家整个缺席）。仅作为恶搞/艺术装置成立（拷问"游戏是什么"），作为游戏是真空 |
| Dwarf Fortress | material system：交互规则降维为属性运算 | 仅覆盖物理/化学属性，覆盖不了社会/叙事状态 |
| Visual Novel | — | 固定分支 = 翻书，AI+随机分支只是加噪音，无法深挖 |
| Lifeline 系列 | 真实时间流逝制造"陪伴在场感"——时间节奏可剥离出来作为真实决策压力（接上状态机） | 本质是"带时钟的 VN"：选择落在固定分支，真实感靠拖延内容制造而非生成内容。LLM 已使该类别整体退役——实时应答/无限对话是其原生能力 |
| 文字遊戲 (Word Game) | "文字作为可交互实体"的直觉有吸引力 | 把 NetHack 的"字符构建视图"推向极端：世界 = 汉字瓦片（墙是一排"墙"字，玩家是"我"字），字形既是视图又是谜题机制；文字沦为退化版图形引擎，文学性与操作重量双失。且谜题为手工设计固定关卡、无状态机，属"固化操作"的另一形态 |
| AI chat/跑团/写小说 | — | 无系统约束的即时幻觉，玩家行为没有实际重量；"不是游戏" |
| 风之旅人 / What Remains of Edith Finch | 文本表达应向电影级渲染靠拢的标准 | — |
| Minecraft | 方块即原子：世界由可组合的最小元素构成，任何交互都在元素层面成立，玩家自由度来自系统组合而非预编码交互 | 3D 方块空间表达不了叙事/社会状态；"破坏-放置"覆盖一切，无因果链 |
| Terraria | 发现式合成：大量可交互物品 + 探索中揭晓组合配方，玩家考古游戏系统 | 配方仍是预编码集合，穷举后操作固化（同 MUD 死结） |
| Don't Starve | 生存资源耦合 + 时间压力：饥饱/理智/血量互锁，季节昼夜驱动决策节奏，饥饿把"时间"变成真实资源 | 行为树/预设配方使行为固定；"活着"本身有重量但可穷举 |
| RimWorld | AI 叙事生成：pawns 的性格/关系/事件与模拟耦合，故事从系统状态中涌现而非手写分支 | 事件是预设叙事碎片，触发式而非涌现；操作大多是菜单指派 |
| Kerbal Space Program | 真实规则即重量：玩家用物理零件自由组装，结果由物理模拟裁决，轨道力学本身成为技能曲线；系统组合产出意料之外的结果 | — |
| Mount & Blade | 开放世界沙盒：玩家在世界中自由定位自己，经济/战争/声望由系统模拟，无主线 | 战斗与治理是预设操作层，系统耦合浅 |
| Kenshi | 系统驱动世界状态：派系战争、城市兴衰、经济由模拟产生，玩家行动有真实后果且无角色预设 | 角色成长是"做事累积数值"的浅系统，无深交互 |
| Cataclysm: Dark Days Ahead | 极端深度的物品交互：一切皆可拆解/改造/组合（车辆系统），时间流逝是持续压力，纯文本的交互复杂度巅峰 | — |
