# 引擎设计

## 目的

一个引擎实例定义一个状态机。

AI 把多模态输入尝试映射为状态机的输入。

AI 基于状态机的输出与当前实例的状态，作出多模态输出。

现阶段只做文本输入与文本输出。

## 记号与契约

### 值

```
Scalar ::= String | Num | Bool      -- 标量
Seq    ::= Scalar · Seq             -- 非空有限标量序列
V      ::= one(Scalar) | many(Seq)  -- 状态值（存储形态）
V?     ::= some(V) | none           -- 可选状态值
-- Num ::= 有限实数，排除 NaN/±∞
```

### 世界

```
World  ::= (t, E, R)         -- t ∈ ℕ，单调
Entity ::= (id, props)       -- id ∈ String⁺，props : K ⇀ V
Edge   ::= (a, b, τ, v)      -- a, b 为实体 id，τ ∈ String⁺，v ∈ V；身份 (a, b, τ) 在 R 内两两相异
```

- 存储面无 none：`⇀` 的部分性即缺席——无引用、清空都是键缺席；none 只存在于格的状态表达（δ 的 next 与 𝒞 的 prev/next），不出现在世界存储；空序列不是值、也不是删除的拼写（δ 载荷里的空序列即违约），缺席是唯一的零
- `K` 是属性注册表键集，`κ : K → PropDef`，`PropDef = (type, many?, label?)`：`type ∈ {lit(string), lit(number), lit(boolean), ref}` 是值域与解释的一根轴——`ref` 的值即实体 id（须在世、过所指门、渲染为脸），`lit(·)` 的值即使等于某实体 id 也只是字面；`many` 声明重数（缺省 one，true 为非空 `many(Seq)`），存储形状 = type × 重数。槽的呈现轴是 `label?`（模型面名字，缺席即非槽）。身份键是 `GameDef.identity ∈ K`（可缺席）：它结构上全域至多一，是独立于槽的脸通道，不参与 label。`props` 必填（K 是 `κ` 的全定义域）。

  词汇闭合（仅属性）：`keys(e.props) ⊆ K`。`T` 是边类型注册表键集（`GameDef.relTypes?`，可缺席），注册是部分的：`τ ∈ T` 即获 `(type, many?, label?)` 的值域契约与模型面名，`type ∈ {lit(string), lit(number), lit(boolean)}`（边值是字面）；`τ ∉ T` 即开口 token（值按字面、以 token 原样示人）。边永无词汇闭合：注册是声明，不是义务；开口一元数据没有席位（边是 n 元格，不是动态 KV 的落点）。
- 模型面名字唯一且是世界语（属性与边共用同一名字空间）：属性槽取 `label`（非空且两两相异，状态视图与事件行同用），无 label 且非 identity 的属性不进呈现面；边类型的模型面名 = 注册 `label` ?? 开口 token（边以 token 示人是显式默认，不是漏网）。prop key 只在世界内部（词汇闭合与账本身份），不进模型面；`identity` 不是槽、不携出槽名（其值即身份），故不带 label。
- 指称槽的值是强引用：被指实体须在世，「无引用」即缺席；关系边是弱引用，端点在世由提交审查把门。边值是字面（端点已是引用）：注册即受 `type/many` 契约，未注册即开口 token、无值域契约。
- 无 label 且非 identity 的属性不进任何呈现面；`private` 动词不进广告面与意志尝试面（仍可由代码直连 apply；其步不产尝试行，后果由状态视图与新见段承接）。裁决侧照常读世界真相。
- 身份键即 `GameDef.identity`，可缺席；其属性恒为无 label 的非 many `lit(string)`，其行渲染取 `~` 形，未读侧出 `?`；缺席时实体以 id 示人。

### 变更与门

```
格 ::= ⟨e⟩(顶点) | ⟨e,k⟩(槽) | ⟨a,b,τ⟩(边)     -- 写与记录的共同坐标
δ  ::= set(e,k,v) | relSet(a,b,τ,v) | spawn(ê) | despawn(e)       v ∈ V?
𝒞  ::= (格, 前态, 后态)                        -- δ 是缺前态的写，记录是补全前态的 δ
```

槽格的身份是 (实体,键)／(端点,类型)，顶点格的身份就是实体的 id：格坐标不另存顶点身份，顶点格只有生（⊥→ê）与灭（ê→⊥）两种转移，不存在实体的替换；故顶点记录恰一侧为 ⊥，`id = id(next ?? prev)`。`set/relSet` 是槽格×后态的糖；`spawn(ê)` 的格即 ê 的顶点格，`despawn(e)` 的格即 ⟨e⟩。绝对写，后态自含；写 none 即删。幂等跳过与同址多写后者覆盖限于槽写（set/relSet）：`w ⊨ post(δ)` 即跳过（无边可删、无键可清的清除写是空操作）；顶点写不做跳过，重复 spawn（已在世）与缺世 despawn 是作者违约，拒绝而非覆盖。

```
G : Δ* × Source → 𝒞 ⊎ Denial      原子：拒绝 ⇒ w 不变
Decision ::= rule(⟨rule⟩) | gate(⟨law⟩)       -- 裁决出处
Ingest   ::= init | replay(⟨seq⟩)             -- 装载出处
Source   ::= Decision | Ingest                -- 审查 ctx 的出处
```

`𝒞` 是变更记录序列，后态的完整 diff：prev/next 自含；顶点记录为生 `(⊥, ê)` 或灭 `(ê, ⊥)`，不另存 id，槽记录前后态相异（⊥ 即缺席）

```
wₙ = w₀ ⊕ C₁ ⊕ … ⊕ Cₙ
```

despawn 级联删边，逐条入账且紧随 despawn 记录（弱引用随主消散）；强引用不清扫，悬空由 integrity 回滚。

提交审查先执行校验（逐条，后到的 δ 可引用先到的后果——指向而非读值：spawn 后可 set 其属性；δ 无读原语，先到的值不可读），后不变式：

```
ι : (w, ctx) → msg?      ctx = (genesis, changes, source)
```

genesis 是实际起点世界的冻结快照（存档恢复、变体开局同义）；changes 是本次提交的全部变更。integrity 恒挂：id 唯一、钟为非负整数、锚在世、词汇闭合、身份非空、存储值 ∈ V、类型契约（值与非空序列按 `type` 声明；`ref` 槽追加引用在世——标量与序列内逐项；注册边类型同受）、边形状与身份契约、三元组唯一。游戏不变式追加领域约束：只读 w 的为守恒类，读 changes 的为出处类。任一违反 ⇒ 整提交回滚并拒绝；游戏不变式的 message 即玩家文案，integrity 违反只有 debug（回落 noResponse）。

### 裁决

```
verb = (label, description, schema, cost ∈ ℕ, private?, clock?, rules)
a    = (verb, params)      params : P ⇀ V           一次尝试 = 一次裁决 = 一个时价 = 一个拒绝单位
ref(a) ⊆ P                指称参数键集（指称模式的参数）
Q   = (world, player, params, roll)           world 为深冻结快照，params 冻结——attempt 入界即不可变，越权写即抛
J   : Q → Grant ⊎ Deny ⊎ ⊥
Grant ::= (Δ*, voice?, facts?, ticks ∈ ℕ?)    voice/facts 是作者世界语，不过投影
Deny  ::= (law, fault, voice?, debug?)        fault ∈ {world, engine}
```

卫语句链 `rules`：首个非 ⊥ 表态即判决；全弃权由引擎闭合为 `action.unanswered`。指称参数先过所指门，非指称参数按字面径由法则裁决，门与渲染不解析其内容。

```
roll(addr, key, sides) = 1 + ⌊h(addr, key) · sides⌋      h : 确定性哈希 → [0,1)
```

骰子地址 `addr = (at, origin, verb, 序位)`——入账表态的账本位置；序位 = 同 (at, origin, verb) 的已入账表态数（本步之前）。地址对入账表态单射且由账本前缀唯一确定：trigger 与 action 随步入账，序位按入账表态计数。未入账的尝试（默）没有账本位置、不消耗序位；其若调用 roll，取「若入账则应有」的位置，同由账本前缀与自身 (at, origin, verb) 决定。不入 `params`：自由文本参数会让玩家成为命运的铸造者；同地址恒同值，sides 违约即抛。

### 步

```
Commit ::= (at, source, origin, action, price, ok, changes, 声)
origin ∈ { will, clock, code }        -- 触发类别，只分流行为
action ::= (verb, params)             -- 公共载荷；clock 的 params 恒空
price  =  origin = clock ? 0          -- 泵是价的铸造者
        : ok ? (granted ?? cost) : cost
声     ::= 授予带 voice?/facts? | 否决带 denial
```

入账判据：凡裁决时读出、不能由账本回算的，随步入账；凡由账本前缀可回算的（脸表、键截面、言默、近况、span），一律是投影，不入账。

序列按构造保序，at 为钟坐标。origin 由调用点决定（will：玩家经广告面；clock：泵逐刻；code：引擎直连 apply），随步入账——记录自含分类与价格，投影不重跑裁决、不据动词表反推（label/messages 等呈现词汇仍取自 def）：一切步一律携 action（action.verb 即动词；clock 的 params 恒空，是空参提案，不是另一种形状）。`source` 是裁决出处——法则表态 `rule(⟨rule⟩)`、门的自判 `gate(⟨law⟩)`（invisible/unanswered）；提交存在判据：变更 ∨ 事实 ∨ 否决 ∨ 应答义务，尝试提交恒因应答义务而在，时钟的空授予（无变更、无事实）即默、不留空步。步入账即冻结（构造后不可变）。呈现按 origin 分流：will 产尝试行（✓/✗ 动词与理由），code 不入事件流（不产尝试行，后果由状态视图与新见段承接），clock 归 ⏱（授予成块、否决为失败刻、全弃权即默）；时钟授予携刻 → `invariant.grant` 否决。

### 感知

投影钩子住 def 不住账本，签名统一收意志锚而非其推导值（如宿主）：锚的自见排除、按锚键控的知识都以锚为变元，体验者由钩子自行推导，反向不可；钩子可无视锚（上帝视角即常函数）：

```
naming : World × Player → (id → Bool)      所指域：意志能点名什么（指称参数门），缺省全见
sight  : World × Player → (格 → Bool)      所见域：呈现能显什么（顶点格成员即卡，边/槽格的键截面即事件披露前提），缺省顶点随所指、边/槽恒真
x   : World × Player → ViewValue          派生纹理，形态自由，无指称声明面
```

所指与所见是两根轴：`naming` 划门（意志能点名什么），`sight` 划呈现（能显什么）。缺省所见随所指，于是「知即在场」是缺省立场而非机制；两轴分别取值即得「可指名而不可见」与「可见而不可指名」。指称闭合由内核把门：值位指称与边端点同过所见域（卡内指称槽的目标不在域内则整槽遮蔽），与轴的取值无关：

```
D(w)   ::= { e : sight(w, ⟨e⟩) }                     -- 所见域（顶点格）
view(w) = ( t,
            entities  = { card(e) : id(e) ∈ D(w) },
            relations = { r ∈ R : {a, b} ⊆ D(w) ∧ sight(w, ⟨a,b,τ⟩) },
            extra     = x(w) )
refs(k) ::= k 非指称 ? ∅ : 值的指称集
card(e) = ( id(e), name(e)?, props ↾ { k ∈ K : k 有 label ∧ sight(w, ⟨e,k⟩) ∧ refs(k) ⊆ D(w) } )   -- props 以 label 为键
name(e) ::= 身份键缺席 ? ∅ : sight(w, ⟨e,身份键⟩) ? 身份值(e) : ∅
```

`sight` 对槽格求值时枚举 `E × K` 全集而非在场键：缺席槽可感（清空后的「→ null」可说）；其值域为 Bool，槽知觉按构造不铸造指称。判据取按锚键控的知识（知晓边即此形），不取槽值——以值为键，可见性即携带值的信息。谓词读物化真相：「知其有不知其值」的知识居所仍是账本，感知不物化为内容。

## 判定与对应

| 记号 | 符号 |
|---|---|
| `𝒞` | `Change` |
| `s` | `Commit` |
| `span(s)` | `FieldView`（投影时由重建的 `w⁻/w⁺` 即时求值） |
| `D(w)` | `SightView`（`sightView`：顶点域 `cards` ＋ 格谓词 `within`） |
| `naming` | `Simulation.domain`（所指域） |
| `ref(c, s)` | `referentsOf` |
| `可说(c, s)` / `存在(c, s)` | `spineLines` 的 changeLine |

## 协议

一切状态是 `w = (t, E, R)`，一切机械后果可说。

∀ 变异恰一入口 `G(Δ*, source)`；`G` 全函数——确定性代码的崩溃在门内代谢为否决（法则崩溃 → `rule.crash`，链终止：意志照价、时钟成失败刻，同拍余者照跑、时刻照走；审计崩溃 → `invariant.crash`）。

**提交全序**　`G` 以裁决读态 `w⁻` 为回滚锚与出处锚。（门的原子性；重放公式使 `𝒞` 可复原任意时刻的世界——档案是信息主侧：在线裁决的因果主侧是 w⁻。世界状态是档案的派生检查点）

**内容与坐标**　`E, R` 是内容，门的原子域；`t` 是坐标——不产 `𝒞`，写者唯一（落钟循环），谓词照常读钟。推导：钟的每次写入都必须归因于某次授予的落钟（刻账目），任何其他写入者都绕过账目；回滚的坐标边界由账目裁定——已入账的刻不可回滚；未入账的刻必须一并回滚，保留无账目的流逝违反凡刻必属某步。

**刻账目闭合**　授予区间的每刻或言或默：`言@at` ＝ 通过变更行判据的变更行、事实、拒绝之并；`言@at = ∅` 即默。言默是记录的函数（由重建边界重算恒同）；投影只可省略行文，不可重划言默——静默聚合 `timePassed ×n` 计 `|{at : 言@at = ∅}|`，是记录的函数，不是渲染的函数。

**凡不可说者不发生**　投影钩子与内核代码的异常不在 `G` 的输出域内代谢：apply 边界原子回滚后原样重抛；正常返回 ⇔ 步骤流与账本一致且授予刻数走完。

**随机是账本位置的纯函数**　同地址恒同值 ⇒ 事件重放与存档恢复得到同一命运。

**感知是注入的投影**　裁决谓词只读世界真相：`sight` 不进裁决，可达性等机制谓词是投影的原料。所指域按定义属于门——它划「意志能点名什么」；所见域划「呈现能显什么」，两者缺省同源而可分别取值。

## 可说性

**判定跨度**　`span(s) = (脸表, 边键截面, 属性键截面)(w⁻, w⁺)`——尝试提交跨其提交边界，时钟提交跨其拍的提交边界。跨度是投影而不得是证据：`w⁻/w⁺` 由 genesis ⊕ 𝒞 重建（逆推逐变更取 prev），脸表/截面由 `sight` 对重建世界即时求值。随步入账冻结会把呈现轴焊死在账本里；正确形状是冻结输入（账本）而非输入的函数。言默由重建边界重算，恒同——投影只可省略行文，不可重划。

**事件投影**　AI 所得 = `Π(窗口, def) ⊕ 感知变更行 ⊕ 作者 voice/facts`。近况 `Π` 是记录的纯函数且永不持久化（记录的提交边界由账本末世界逆推重建，脸表与截面即时求值，投影不回读活世界）：窗口内回合记录按与 act 结果相同的变更行判据投影（推论·刻账目闭合），体量由 `recentWindow` 独自调节；言默同源重算。act 结果的新见段是同一装配线的回合内增量：本回合新进所见域的实体以状态视图同形的实体卡承载。

**投影的消费分类**　进协议通道（约束裁决可行集）的投影，与真相的一致性机械保证；只服务工具与呈现的投影永不进协议通道，形态自由。豁免：`voice/facts` 进协议通道但属规则铸造的世界语，机械只保证送达——感知墙是缺省装配的纪律，不约束立法者。

## 主体性

**意志不在世界里**　`def.playerId` 是指向普通实体的锚引用

**缺省主语**　无主语动词的主语 = `hostOf(w, player)`：`in` 链上最近的宿主，缺省锚自身；视角锚定 hostOf 是 games 构件的取值。

**附身**　占据 = 锚实体 `in` 的迁移，一条 delta 过门。可生灭换角的游戏把锚指向不朽实体、宿主建为指称属性：宿主之死被指称契约拦成显式迁移（迁移可说，静默回退不可说），迁移合法性由过渡不变式钉住。

## 回合协议

**通道次序**　整批先过静态形态检查再占窗口，随后批次执行是 apply 的 fold——后一动作在后一世界态上裁决。静态违约不产生步骤：边裁边检会让中途违约在步流中留下无痕缺口；混批任一违约则整批不进裁决。

```
apply(a) = adjudicate(a) → G → 逐拍落钟（每拍各时钟动词独立过门）
price(s) = origin(s) ≠ clock ? (ok(s) ? (ticks(J) ?? cost(verb)) : cost(verb)) : 0
```

**拒绝恒价**　一切拒绝来源——门、法则、审查、崩溃——落同一 `cost(verb)`：`Deny` 无 ticks 字段，结构上不可改写；审查回滚整个授予时尝试仍耗尝试价。拒绝同样落钟：刻是世界的因，不是意志的果。时价的定义域是「入裁决」，不是结果；`cost = 0` 属作者定价——否决零价即对世界的免费查询，反穷举由此归于作者侧：提案有界（门闩封占用，校验拒绝的重提触不到世界）、域外指称统一文案（门）、理由世界腔（法则）。

**回合定稿**　`(seq, time, utterance, steps)` 入账；绕过引擎的裸 apply 不产生条目。定稿单点原子且前移到窗口关闭：回合的内容于裁决完成时已完备，叙述不在定义内，act 工具尾落条目即定稿——表达中断只降级呈现（回落确定性摘要），账目照记。落盘是定稿的完成条件：条目未落地即回合未定稿，档案上未曾发生。

**表达契约**　接缝是单只片段袋 PromptKit（recent 恒在，view/utterance/events/instruction 按相填充）；context 钩子在引擎裁剪之后改写外发消息阵列。散文对已锚定内容的转写与渲染自由。`spineLines` 是事件流的规范单行渲染：符号承担结构（✓/✗/⏱/×n），语言词全部来自 messages/label/规则文案。回合输入 = 状态视图 + 变更 + 近况。

**呈现服务**　narrate 无提案通道、无 act 通道、无时间流逝，不是回合；输出不入账、不进投影。用途：开场与仪器的场景重渲。

## 面向作者的契约

- **动词表**：`params` 声明（type×可选×重数×描述）派生宿主面、内核校验与规则参数的编译期类型；`type ∈ {lit(string), lit(number), lit(boolean), ref}`，`ref` 的值是实体 id（过所指门），`lit(·)` 是字面；`many` 令参数为非空序列，与世界值同形——一次尝试的操作数是裁决的一部分（指称逐项过门、整次原子），批次是多个尝试在世界态上的顺序 fold；操作数与顺序组合是两根轴，多重性不由批次承载。`private` 动词不进广告面（广告面 = 裁决面 ∖ private），由代码直接 apply——同一裁决边界与审查；其步不产尝试行，后果由状态视图与新见段承接。`clock` 动词每刻由泵以空参提案过同一扇门：无价、全弃权即默、授予禁携刻；其 deny 发声为失败刻。origin 由调用点决定：will（玩家经广告面）、clock（泵）、code（引擎直连）；private 只管广告面，与 origin 无关——公开动词亦可由 code 触发（世界因，不产尝试行），价对 code 通道照常计 cost。origin 随步入账，投影不再回查 def。
- **法则纪律**：键控形态（规则按属性组合键控、量级存于属性载荷，随新实体自动泛化）与 authored 形态（互动逐实体书写）共用同一裁决与审查；键控禁止特判实体 id。规则的读通道唯一在 w⁻：同一值地址（实体×键、边）的多次写只能是一条预计算轨迹，从 w⁻ 重复派生的叠加增量按序覆盖。
- **拒绝**：结构化 Denial，理由以世界腔内联在规则文本，缺省回落 noResponse；拒绝不携带涉及实体——指称落点在 action 参数（结构化）与法则理由（世界腔）。`fault: world`（卫语句链、门、unanswered 闭合、游戏不变式）有世界腔 voice（缺省 noResponse）；`fault: engine`（integrity、授予形状违约、commit 拒绝、`*.crash`）只有 debug，probe 报 bug。覆盖面没有机械判据。

## 非目标

- 语义校验
- 一次提交的动作数上界、刻数授予上界
- 多意志

## 架构决策
- `node_modules/@earendil-works/pi-coding-agent/docs/`，进程内集成
- **IPC 形态**：主进程 → 渲染层推送事件流（`narration_delta` / `narration_reset`），渲染层 → 主进程请求（invoke）
- **回合编排（单 pass）**：每回合一次 `session.prompt()`——模型先调 `act`，其后输出散文。相位 mapping（文本丢弃）→ narration（留作回合叙述，不入账）；重试作废在途生成（`narration_reset`），镜像 pi 语义。
- **上下文裁剪**：每次调用经 `context` 扩展裁剪为最后一条 user 消息起的后缀（工具结果存为独立 toolResult 角色、不并入 user 消息，回合内该锚恒为回合提示，叙述续行因此保住裁决前缀；context 事件的消息是深拷贝，就地改写不污会话文件）；会话文件累积全量消息作审计；持久化事实是回合记录，近况是其纯函数投影，只在窗口更新点（装载 / 回合边界）整体重算——回合协议使该点与映射消费点观察等价，进程重启由记录重建。装载纪要按消费判据（逐条试投影存活）定完好：损坏使近况截断至其后完好子后缀并告警显形，会话文件不动——纪要只喂投影与审计，门不读纪要；截断而非逐条剔除。档案是单一追加日志（会话文件），条目两种：回合（证据，每回合恰一，定稿写点）与检查点（缓存，声称已含前 n 条回合）——日志的任意前缀都是一致档案。装载即对账：取最后检查点为主侧锚（缺失或损坏则自变体开局全量重放），其后记录走 𝒞 重放——不重裁决、不掷骰（命运已入账；检查点覆盖的回合不重放世界，但骰子序位仍按账本补点：世界可跳、点数不可跳），逐变更 prev 校验加 integrity，authored 不变式不重审（历史由当时的法则裁判过）；链断（序位断裂、prev 不符、审查失败）则世界与近况同界截断并告警显形，检查点领先于证据即拒绝装载。检查点每回合随定稿追加，写失败仅告警（缓存可迟到，滞后由下一回合定稿治愈）。
- **缓存稳定性**：[tools+system] 放置于开头，系统提示与工具数组字节级稳定，不做 setActiveTools 相位切换；回合内（act 裁决后的描写续行）前缀 [tools+system+user] 逐字节稳定（近况头并入的 user 消息在回合内不变）。