# 语言游戏引擎设计方案

> 本文档回答「是什么、为什么」；技术决策依据与实证记录见 ARCHITECTURE.md；对本文件基础主张的质疑与裁决追踪见 CRITIQUE.md。

## 0. 公理

core 的全部承诺共六条。未来任何特性提案必须注明触碰了哪条公理、归入哪个声明面（§8）；无法归类的提案默认拒绝。

1. **世界即账本。** World = 实体（稳定 id + 属性包）× 关系边 × 一个整数时钟，外加 core 确定性维护的焦点/拒绝痕迹注记，此外无物。本体的唯一职责是让真相**可说、可 diff**：凡产生玩法后果者，其后果必可说——必能表达为叙述者可以合法说出的一条 Change 或 Fact；中间状态（隐藏的原因、调度、计数）可不可说，经 `internal` 属性合法存在。
2. **两扇门。** 一切状态变异经过提交硬墙（快照 → 提交 → 不变式校验 → 违反则原子回滚）；一切新事实断言经过声明契约（新事实 ⊆ 本回合状态可推导集）。这是本引擎区别于「AI 跑团」的全部身份，不可协商。
3. **语言不入账。** 语言只从两端进出：玩家的意图文本进（映射层翻译为动作提案），世界腔裁决文案与表达散文出。AI 产出的语言永不成为状态，后果只能由规则 / 系统产出——LLM 从不提案数值。
4. **随机是世界的纯函数。** 无隐藏变量：check / apply / dryTick / 存档恢复对同一状态天然一致。
5. **感知是注入的谓词。** 可见 / 可达 / 可持握由游戏声明（空槽），core 缺省仅全可见与全可达；core 不内嵌空间模型、物理或任何三维世界的语义。梦核等违反现实规则的世界由 authored 形态承载，不需要 core 让步。
6. **动力学三分。** 规则管此刻（动作的离散因果），系统管此后（tick 的连续因果），不变式管永远（一切提交的必要性过滤）。三者按时间轴干净分割，不得混装。

> **公理 3 的语言三分**：「core 不内嵌任何语言」管辖**世界语言**——玩家可见的一切文字（法则理由、不变式 message、回退摘要、表达散文）——由游戏注入或经声明契约约束，永不成为状态。**通道语言**——引擎↔模型的 prompt 脚手架、act/declare 工具描述与校验反馈——是协议基础设施，core 单一来源、从不进玩家视野；选用哪种自然语言是自由决策，不构成公理违反。**工具语言**——sim/loop 的研究者 CLI 输出——与本公理无关。叙述的输出语言由「跟随玩家意图所用语言」的硬约束锚定，开场渲染（尚无意图）跟随世界内容语言。

## 1. 身份：协议、基底、作者界面

「core」是三种不同身份的东西装在一起，混淆它们是一切设计困惑的来源：

| 层 | 内容 | 身份 |
|---|---|---|
| **协议** | `Action{verb,params}` 进 → `Delta / Change / Fact / Denial` 出；提交过不变式硬墙，断言过声明契约 | 引擎的身份所在，已完成且应冻结 |
| **基底** | Entity / props / relations / clock 这套 World 形状 | 实用主义的事实账本，可用但不可神圣化 |
| **作者界面** | verbs 集合 + 四个声明面（§8） | 受钩子准入原则约束的生长区 |

「本体论 + 规则代码」的结合不是事故，而是杀掉三个替代方案后的幸存点：纯数据法则已死（AST 解释器，ARCHITECTURE §4-11）；完全自由的代码沙盒丢掉可序列化、可 diff、可核验的边界；重本体（DF/CDDA 式专用结构）只在领域固定时成立，与谱系目标（§6）矛盾。「薄的可说基底 + 代码裁决」的代价是词汇看守问题，见 §9 开放问题。

**本质清单**——以下五件承载引擎身份，其余一切皆可替换：

> 双 pass 分离（切断幻觉路径）、声明契约（实体级接地边界）、卫语句规则（裁决即代码）、不变式原子回滚（重量的保证）、spawn/despawn（动态拓扑）。

## 2. 定位与核心主张

一种新的游戏类型引擎：**以确定性状态机为重量锚点、以 LLM 为映射与表达引擎**的交互式语言游戏。

- **操作比文字更加核心。** 玩家操作的「重量」由确定性裁决保证，AI 只负责让每次操作的反馈有文学生命力。重量住在裁决层，不住在解释层：同一动作提案的裁决确定可复现；意图→提案的映射是概率的（同文本跨 run 可命中不同动词），解释保真度是体验参数而非内核保证。
- **AI 不创造规则，AI 不产生系统后果。** 数值与后果只能由规则 / 系统产出（公理 2/3），映射层只提案、表达层只叙述。
- **表达接地。** 叙述的实体引用可从状态核验——这是实体级接地（声明契约的 touched 边界），不是命题级可证伪：谓词层一致性由法则、不变式、声明契约分层承担，散文正文无机械检查（实证选择，ARCHITECTURE §4-9~10）。幻觉被压缩为「对真实实体的虚假陈述」，残余率是待测量项，不是零承诺。约束是结构性的（schema + 声明契约），不是提示词请求。
- **自由度上限由作者协议决定。** 引擎每少承担一分职责，作者协议的可能性上限就高一寸——即使这增加创作负担；语言的开放性决定逼近上限的体验摩擦，不构成机制层面的无限自由。

## 3. 模拟层（确定性裁决内核）

接受结构化动作，返回新状态与结构化结果。动作空间由游戏声明的动词表驱动，后果由卫语句式规则产出。

### 3.1 规范面：动词 + 卫语句规则

- `GameDef.verbs` 是动词表：每个动词含 `schema`（TypeBox，生成 act 工具参数校验并推导规则参数编译期类型）、`entityParams`（实体参数，供可见性校验）、`candidates`（非实体参数候选值）、`rules`。动词集由各游戏声明（era 类声明 talk/travel/equip，开放世界声明 attack/trade/craft），引擎不硬编码任何动词。
- **规则即代码**：普通函数接收只读上下文 `Q`（world/actor/time/params + `rel/relNum/roll/canReach/name` 等引擎语义唯一入口），返回授予（Delta 列表 + 世界腔理由 + facts）、结构化拒绝（Denial）或 null（不表态交由后续规则）。按序裁决，首个表态即判决——优先序就是书写顺序，无隐式控制流。例：「采下浆果」→ 映射提案 `harvest(bush)` → harvest 规则裁决返回 `[berries+1, ripe→false]`。
- **承重墙**：法则网络形态下规则必须按属性组合键控，禁止特判实体 id——检查双方的 `material` 硬度来裁决，不关心具体是哪个实体。规则随新实体自动泛化，这是组合爆炸的唯一出路。
- **两种创作形态**，共用同一套裁决瓶颈、提交硬墙与表达契约，core 不感知形态差异：
  - **法则网络形态**（era/DoL 极）：规则按属性组合键控、随新实体自动泛化；
  - **authored 形态**（梦核极）：互动逐实体书写（每条一个卫语句子句 + 兜底），实体生灭、动态拓扑、持有物改写互动结果。先例：yume 以四动词、零领域不变式、两个氛围系统实现「发现即玩法」。
- **拒绝**：结构化 `Denial`（law/subject/object/prop/reason/debug/fallback），理由以世界腔内联在规则文本里，缺省回落 `messages.noResponse`。`deniedBy: "rule" | "denyAll" | "protocol"` 是探测与审计的语义基础，不依赖理由字符串匹配；协议性拒绝（映射层形态错误）不进玩家叙述。
- **前置门为封闭集**：未知动词 → schema → 施动工具前提 → 可见性 → 规则。施动工具前提（`instrumentParams`：工具须可持握且可达）之所以是门而非规则，只因 affordances 与 probe 机器需要复用；今后新的语义前提一律写成第一条卫语句规则，不再新增门。

### 3.2 动力学面：系统与时间

- `systems` 是按序执行的纯函数注册表（每 tick 运行，产出 deltas/facts），承载火蔓延、燃尽、日程等连续因果；**fact-only 纯氛围输出合法**——零状态变更的氛围事实（如梦中低语）同样进入表达输入；其认识论身份由 `Fact.kind` 区分：percept（缺省）授权状态断言（实体进声明契约 touched 集），utterance（世界之言）只许转述、不进 touched。
- `reactiveSystems`（可选）：granted 动作提交后立即跑一次 systems，「把火源放进易燃物当场引燃」的动作→响应因果当回合成立。
- `turnTicks`（可选）：每回合动作裁决后、表达前推进 n 刻——渴感 / 夜袭等持续压力无需显式 wait 即成立；无论动作成败时间都流逝（世界不因玩家停摆）。
- 随机经 `Q.roll(key, sides)`（公理 4；key 需同 tick 唯一，可含实体 id）；多时间尺度（回合/日/月）由游戏自持计数器 + systems 内判定，core 不内置历法。

### 3.3 必然性：不变式硬墙

- 提交后校验，违反即**原子回滚整个提交并拒绝**——法则与系统的 bug 都无法绕过。core 默认恒挂引用完整性（id 唯一、id 型属性 / 关系端点 / 焦点指向存在的实体）；游戏追加领域不变式（如「湿柴不得燃烧」「铜币总量守恒」，聚合助手 `sumProp`）；守恒类种子读 `InvariantCtx.genesis`（实际起点世界的冻结快照，而非 def.world——存档恢复与变体开局同样成立）。
- 游戏不变式的 message 是游戏撰写的世界腔，直接作玩家文案；core 完整性违反只留 debug 诊断，回落通用兜底文案。

### 3.4 生灭与拓扑

- Delta 原语：`set / inc / relSet / relInc / spawn / despawn`。生灭原语配合 relSet 建径让世界拓扑生长；despawn 级联清理核心结构（关系边/焦点/痕迹），悬空 id 引用由完整性硬墙回滚。
- 关系边表 `world.relations` 表达社会 / 叙事状态（信任、记忆、派系），表达层格式化为世界腔文本。

### 3.5 感知面：空槽与投影

core 不内嵌任何感知语义，全部为游戏注入的谓词（公理 5）：

- `grounding` 决定哪些实体进 LLM 序列化；`digest` 是序列化投影（裁剪冗余、格式化关系边、控制 prompt 体积）；`probeScope` 裁剪探测枚举域。
- `reach`/`reachReason` 是可达性空槽，`holdable` 是可持握空槽（core 不提供缺省，未声明即全不可持握）。容器包含树等空间语义是游戏侧自愿接入的构件（先例 `src/games/space.ts`），core 不强制。

### 3.6 优化件（非本质，可替换）

以下机制降低特定形态的作者负担或映射错误率，不属于引擎身份：affordances 动作空间接地（era 极收益；发现式世界应关闭——菜单即剧透）、focus/traces（指代锚点与拒绝痕迹）、dryTick 合法预言（「即将发生」进 touched 集）、`summarize` 回退摘要、`hint` 法则提示注入。

## 4. 映射层（LLM，双出口）

玩家输入自由文本；LLM 职责是解析为动作提案列表 `[{ verb, params }]`，实体命中可见索引，动词取自已声明动词表。

- **纯语义解析**：无关键词匹配、无别名表、无语言限制——动词与实体由 LLM 从 `name + attrs + 上下文` 纯语义接地；选中文本作为实体接地锚点。
- **双 pass 分离**（本质）：映射 pass 只产结构化结果（动作提案或仅 label 的结构化拒绝），不产散文；表达 pass 在状态变更后独立运行，是玩家的唯一信息来源。这切断两条幻觉路径：模型不调工具却叙述动作、模型叙述未发生的后果。
- **拒绝契约**：模型不撰写拒绝理由。无法解析时返回结构化拒绝（仅 label）；能解析的动作直接提交，由规则层裁决。合法性校验收敛于 `Simulation.adjudicateRaw` 单一瓶颈——场景 / CLI / probe / LLM 同一裁决口径。
- 未来可选优化：高频明确操作的确定性直通（规则捷径），降成本与延迟。

## 5. 表达层（LLM，实体级接地）

AI 读当前状态（digest 投影）+ 本回合变更（经 props 注册表 label 做语言无关线性化）+ 法则 facts，输出该状态的文字化。

- **声明契约**（本质）：新事实经第二个工具 `declare` 结构化提交（`{ entities, statement }`），校验为集合成员判断——声明实体 ⊆ 可见实体 ∩ 本回合**状态可推导集**（touched：动作主体、新见、变更、facts、授予动作参数；被拒动作不在内——它未改变任何状态）。本回合消逝实体（despawn）豁免可见性检查——叙述消失合法。校验失败逐条即时反馈，模型同一 turn 内自我纠正；回合末仍未通过 → 带错误反馈重生成一次 → 仍失败回落结构化摘要（确定性强接地兜底）。
- **正文自由**：散文正文无机械检查——一致性由两条结构性边界承担（声明契约 + 提交硬墙）；词表扫描器类机制已实证移除且永不回归（ARCHITECTURE §4-9~10）。润饰属性（`stylistic`）允许文学润饰。
- **内部属性隔离**：`internal: true` 的属性不进序列化 / 变更 / 校验——模型根本看不到（公理 1 的不可说逃生舱）。
- **叙述不回流**：表达输入只有当前状态 + 变更 + 近况窗口，无旧叙述全文——幻觉不固化；会话文件仍累积全量消息作审计。

## 6. 状态机谱系

创作者自选落点，引擎不预设复杂度：

```
完全无状态机 ←────────────────→ 极复杂状态机
(纯AI聊天)      (Era/DoL级别系统)
 灵活性最大                        确定性最强
 游戏性最弱                        游戏性最强
 AI全权负责                        AI只做表达
 玩家行为无重量                    玩家行为有精确重量
```

允许纯 AI 聊天端点存在（谱系最左端 = 自由度最大的普通模拟应用）。重量锚点的强度由游戏声明的法则与不变式密度决定，不来自 core 的原语集合——开放世界极的「一致」就是一小撮领域不变式，不是法则穷举。

## 7. 交互模式：文本即操作面

玩家用任意文字表达操作意图，可选中场景散文片段作实体接地锚点。快捷键 / 菜单只是「输入意图文本」的快捷方式，本质仍是自由文本。

- 玩家定义交互面：阅读深度直接转化为操作空间广度。
- 解析失败也是游戏的一部分：无法解析的意图获得世界腔平淡回应（模型不撰写理由），玩家通过「尝试 → 被世界拒绝」的落差学习边界。
- 裁决保持精确：意图与实体命中状态机边界后才产生后果（精确住在裁决层；意图解析层是概率的）。
- 与 AI chat 的本质区别：有系统约束；与 MUD/roguelike 的本质区别：操作集情境化、动态生成，不是预设热键。

## 8. 引擎接口：四个声明面

GameDef 的字段按四个声明面组织（概念分类）。**钩子准入原则：一个钩子要有资格存在，必须命名某种内核既无法推导、又无法在不破坏某类游戏的前提下取缺省的信息；且必须归入某一声明面、注明触碰的公理。跨栏的重新设计。**

**核心增长准入：只有「无法表达」（而非「表达痛苦」）有资格驱动 src/core 的机制增长；表达痛苦是游戏侧构件与库层的信号。侵蚀度量：每新增一个复杂场景记录 src/core 的 diff——预测为零，首个以「无法表达」立项的非零 diff 即设计警报。**

```ts
const game: GameDef = {
  // —— 规范面：何为可能、何为必然
  id, title,                 // 实例元数据
  verbs: { ... },            // 动词表：label/description/schema/entityParams/instrumentParams/candidates/rules
  invariants: [...],         // 提交后硬墙（core 默认恒挂引用完整性）
  playerId,
  world,

  // —— 动力学面：时间如何流逝
  systems: [...],            // 每 tick 的连续因果
  turnTicks,
  reactiveSystems,

  // —— 感知面：世界向各消费者的投影
  grounding,                 // 可见实体索引
  digest,                    // 序列化投影
  reach, reachReason, holdable, // 能力谓词空槽（公理 5）
  probeScope,                // 探测枚举域

  // —— 词汇面：世界的语言（core 不内嵌任何语言）
  messages,                  // 必填：core 产出的用户可见文案
  props: {...},              // 属性注册表：type/label/internal/stylistic
  hint,
  summarize,

  // —— 时序开关（优化件，非本质）
  affordances,               // 动作空间接地注入；发现式世界应关闭
};
```

引擎自动处理：act / declare 工具 schema 生成、双 pass 编排、结构化拒绝接线、declare 校验与回退摘要。拒绝理由一律由规则层产出，引擎不生成理由。

## 9. 开放问题与非目标

**开放问题**（按门控推进，未测量不动手）：

- **词汇看守**：「本体论 + 规则」模式的真实价格——属性语义散落在规则的读取行为里，靠字符串约定耦合，两条规则对同一属性的分歧无人报警。候选还债步骤（成本递增）：PropDef 值域（enum/range，`candidates` 可派生）→ kind → 属性结构 → 规则词表闭包检查（规则只准读注册表声明过的属性——代码可静态分析而数据法则做不到，是 §4-11 撤退的副产品）。触发点：village 规模增长暴露实际疼痛。**最廉价的闭包检查已落地：`sim lint` 静态扫描规则闭包的属性键对照注册表（ARCHITECTURE §4-12c），其余步骤仍按门控。**
- **规则遮蔽审计**：属性键控的规则同时匹配时，落败者静默（顺序耦合是法则网络形态的隐式状态）。候选对策：probe 增加「多条规则守卫命中、仅首条生效」的报告，把顺序耦合变成可审计信息。
- **社交记忆**：对话内容无法入账（公理 3），社交状态只剩裸数值——数值没有记忆。候选方案（记录通道：AI 写「世界听见了什么」，存入隔离命名空间，后果仍归规则裁决；记录 ≠ 后果，软通道的静默假授予 bug 在此结构上不可能复现）仅在对话密集对照实验证明空心感伤害体验后立项。

**非目标**（显式不做，让范围焦虑有去处）：

- 多 actor 离屏自治（NPC 由 LLM 驱动的 RimWorld 式野心）。
- 无限程序化梦境 / 无限意识流生成——那是谱系最左端的品类；Yume Nikki 本身就是手工有限世界，authored 有限怪诞 + 自由探索才是本引擎对梦核的正解。
- 词表式语言约束（扫描器 / forbiddenTerms 类）：语言是 LLM 的原生能力，词表硬约束已被实证移除且永不回归（ARCHITECTURE §4-9~10）。

## 附录：参考与灵感来源

| 游戏/类型 | 贡献的正向参考 | 本方案拒绝的负面参考 |
|---|---|---|
| MUD（北大侠客行） | 命令解析复杂度即技能曲线；trigger/别名编程 = 玩家在编程，元游戏层 | 固定命令 DSL 让操作固化；社群穷举最优序列后游戏死亡 |
| NetHack | 穷举交互空间的探索模式；物态/物品/怪物互动的操作空间广度 | 输出端用 ASCII 字符而非自然语言，浪费了输入端的交互复杂度 |
| era 系 | 基于状态机的交互模拟器；所有操作映射到庞大数值系统；可重玩性来自系统深度 | 交互脚本是预编码的，操作被固化 |
| Degrees of Lewdity | 多资源轮耦合的风险决策设计（意志/疲劳/创伤/暴露度互相影响） | 回合制+固定选项制造决策节奏，但操作集被预设 |
| A Dark Room | 极简操作中"造火"式的 gameplay reveal（操作中发现而非 lore dump） | 放置式点击循环，可用鼠标连点器，太蠢 |
| Progress Quest | — | "零玩家游戏"极端：建号后角色全自动升级打怪，玩家零输入。无操作=无决策=无技能度量，连放置游戏的点击都省去，玩家沦为观众，纯数字膨胀的斯金纳箱；谱系上位于"玩家行为无重量"的最底端（玩家整个缺席）。仅作为恶搞/艺术装置成立（拷问"游戏是什么"），作为游戏是真空 |
| Dwarf Fortress | material system：交互规则降维为属性运算 | 仅覆盖物理/化学属性，覆盖不了社会/叙事状态 |
| Visual Novel | — | 固定分支 = 翻书，AI+随机分支只是加噪音，无法深挖 |
| Lifeline 系列 | 真实时间流逝制造"陪伴在场感"——时间节奏可剥离出来作为真实决策压力（接上状态机） | 本质是"带时钟的 VN"：选择落在固定分支，真实感靠拖延内容制造而非生成内容。LLM 已使该类别整体退役——实时应答/无限对话是其原生能力 |
| 文字遊戲 (Word Game) | "文字作为可交互实体"的直觉有吸引力 | 把 NetHack 的"字符构建视图"推向极端：世界 = 汉字瓦片（墙是一排"墙"字，玩家是"我"字），字形既是视图又是谜题机制；文字沦为退化版图形引擎，文学性与操作重量双失。且谜题为手工设计固定关卡、无状态机，属"固化操作"的另一形态 |
| AI chat/跑团/写小说 | — | 无系统约束的即时幻觉，玩家行为没有实际重量；"不是游戏" |
| 风之旅人 / What Remains of Edith Finch | 文本表达应向电影级渲染靠拢的标准 | — |
| Minecraft | 方块即原子：世界由可组合的最小元素构成，任何交互都在元素层面成立，玩家自由度来自系统组合而非预编码交互 | 3D 方块空间表达不了叙事/社会状态；"破坏-放置"覆盖一切，无因果链 |
| Terraria | 发现式合成：大量可交互物品 + 探索中揭晓组合配方，玩家考古游戏系统 | 配方仍是预编码集合，穷举后操作固化（同 MUD 死结） |
| Don't Starve | 生存资源耦合 + 时间压力：饥饱/理智/血量互锁，季节昼夜驱动决策节奏，饥饿把"时间"变成真实资源 | 行为树/预设配方使行为固定；"活着"本身有重量但可穷举 |
| RimWorld | AI 叙事生成：pawns 的性格/关系/事件与模拟耦合，故事从系统状态中涌现而非手写分支 | 事件是预设叙事碎片，触发式而非涌现；操作大多是菜单指派 |
| Kerbal Space Program | 真实规则即重量：玩家用物理零件自由组装，结果由物理模拟裁决，轨道力学本身成为技能曲线；系统组合产出意料之外的结果 | — |
| Mount & Blade | 开放世界沙盒：玩家在世界中自由定位自己，经济/战争/声望由系统模拟，无主线 | 战斗与治理是预设操作层，系统耦合浅 |
| Kenshi | 系统驱动世界状态：派系战争、城市兴衰、经济由模拟产生，玩家行动有真实后果且无角色预设 | 角色成长是"做事累积数值"的浅系统，无深交互 |
| Cataclysm: Dark Days Ahead | 极端深度的物品交互：一切皆可拆解/改造/组合（车辆系统），时间流逝是持续压力，纯文本的交互复杂度巅峰 | — |
