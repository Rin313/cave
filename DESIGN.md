# 语言游戏引擎设计

## 主张

- 文本是操作面：意图以自由文本表达，不限定输入方式与交互 UI。
- 确定性状态机承担玩法后果；AI 只做映射（意图 → 提案）与表达（状态 → 散文）。
- 忠实是赌注：映射对意图、散文对状态的忠实不做判定。

## 记号与契约

### 值

```
S   ::= String ⊎ 有限数 ⊎ Bool      标量
V   ::= S ⊎ S*                      账本值（S* 为有限标量序列），存储形态
V⊥  ::= V ⊎ {null}                  值语言：null 只出现在读数、清除写与变更记录
```

null 不是值：缺席即键不在场；清除写 null 即删键、删边。

### 世界

```
w = (t, E, R)          t ∈ ℕ，单调
e = (id, name, props)  id, name ∈ String⁺，props : K ⇀ V
r = (a, b, τ, v)       τ ∈ String⁺，v ∈ V；边身份 (a, b, τ) 在 R 内两两相异
```

- `K` 是属性注册表键集，`κ : K → PropDef`，`PropDef = (type, label?, internal?)`，`type ∈ {string, number, boolean, id, tags, any}`。词汇闭合：`keys(e.props) ⊆ K`；动态键值对走关系边（关系类型与 tag 是开口 token，无注册表）。
- `type: id` 的属性值（标量或标量数组）是强引用：被指实体须在世，「无引用」由缺席表达。关系边是弱引用。
- `internal` 的属性与动词不进任何呈现面；裁决侧照常读世界真相。
- id 与 name 是实体的必备字段：id 承担协议指称（提案参数、键控），name 只承担呈现，是渲染解析的终点。

### 变更与门

```
δ ::= set(e,k,v) | relSet(a,b,τ,v) | rename(e,n) | spawn(ê) | despawn(e)       v ∈ V⊥
```

绝对写，后态自含；`set/relSet` 取 null 即删。幂等跳过限于状态写（set/relSet/rename）：`w ⊨ post(δ)` 即跳过，w 是应用该 delta 时的当前态而非 w⁻——无边可删、无键可清的清除写是空操作；同址多写后者覆盖。其余不可执行——目标缺失、端点缺失、spawn 的 id 占用——拒绝整提交；spawn/despawn 是存在事件，无跳过形态。

```
G : Δ* × Src → 𝒞 ⊎ Denial      原子：拒绝 ⇒ w 不变
Src ::= rule:⟨verb⟩.⟨rule⟩ | system:⟨id⟩ | def
```

`𝒞` 是变更记录序列，后态的完整 diff：prev/next 自含，spawn 携带完整后态实体，despawn 携带展示名（离场名的唯一来源）。

```
wₙ = w₀ ⊕ C₁ ⊕ … ⊕ Cₙ
```

despawn 级联删边，逐条入账且紧随 despawn 记录（弱引用随主消散，消散可说）；强引用不清扫，悬空由 integrity 回滚。

提交审查先执行校验（逐条，后到的 δ 可引用先到的后果——指向而非读值：spawn 后可 set 其属性；δ 无读原语，先到的值不可读），后不变式：

```
ι : (w, ctx) → msg?      ctx = (genesis, changes, src)
```

genesis 是实际起点世界的冻结快照（存档恢复、变体开局同义）；changes 是本次提交的全部变更。integrity 恒挂：id 唯一、必备字段非空、钟为非负整数、锚在世、词汇闭合、存储值 ∈ V、type 契约（id 引用在世——标量与数组；tags 为字符串阵列；其余按声明标量类型；any 豁免标量类型、不豁免账本值形状）、边形状与身份契约、三元组唯一。游戏不变式追加领域约束：只读 w 的为守恒类，读 changes 的为出处类。任一违反 ⇒ 整提交回滚并拒绝；游戏不变式的 message 即玩家文案，integrity 违反只有 debug（回落 noResponse）。

### 裁决

```
verb = (label, description, schema, cost ∈ ℕ, internal?, rules)
a    = (verb, params)      params : P ⇀ S      一次尝试 = 一次裁决 = 一个时价 = 一个拒绝单位
ref(a) ⊆ P                指称参数键集（ref 声明的字符串参数）
Q   = (world, player, t, params, roll)        world 为深冻结快照，params 冻结——attempt 入界即不可变，越权写即抛
J   : Q → Grant ⊎ Deny ⊎ ⊥
Grant ::= (Δ*, reason?, facts?, ticks ∈ ℕ?)
Deny  ::= (law, reason?, debug?)
```

卫语句链 `rules`：首个非 ⊥ 表态即判决；全弃权由引擎闭合为 `action.unanswered`。`ref` 参数先过可见性门，`free` 参数按字面径由法则裁决，门与渲染不解析其内容。

```
roll(t, src, key, sides) = 1 + ⌊h(t, src, key) · sides⌋      h : 确定性哈希 → [0,1)
```

骰子地址 `(t, src, key)` 元组编码并继承出处命名空间；同地址恒同值，sides 违约即抛。

### 步

```
Step ::= ActionStep | TickStep
ActionStep = (at, ok, reason, changes, field, action, ticks, deniedBy?, denial?, facts?)
TickStep   = (at, ok, changes, field, facts?) | (at, ok=false, denial, field)
```

序列按构造保序，at 为钟坐标。步是纯内容，不携带出处（归因的机器诊断在 `Denial.debug`，法则归因的观察处在 probe 的裁决地图）。步入账即冻结（构造后不可变）：记录是证据而非视图。刻步无应答通道：应答（reason）是动作协议的义务，系统不回应任何尝试。

### 感知

投影钩子住 def 不住账本，签名统一收意志锚而非其推导值（如宿主）：锚的自见排除、按锚键控的知识都以锚为变元，体验者由钩子自行推导，反向不可；钩子可无视锚（上帝视角即常函数）：

```
g   : W × Player → 𝒫(id)              参照域，缺省全见
πₑ  : W × Player → (R → Bool)         边感知，缺省恒真
πₚ  : W × Player → (E × K → Bool)     属性感知（槽谓词），缺省恒真
x   : W × Player → ViewValue          派生纹理，形态自由，无指称声明面
```

状态视图与可见性门同源（参照域判定 `refdom(w, player) = g(w, player) ∩ E`）：

```
view(w) = ( t,
            entities  = { card(e) : id(e) ∈ refdom(w) },
            relations = { r ∈ R : {a, b} ⊆ refdom(w) ∧ πₑ(w)(r) },
            extra     = x(w) )
card(e) = ( id(e), name(e), props ↾ { k ∈ K : k ∉ internal ∧ πₚ(w)(e, k) } )
```

πₚ 对 `E × K` 全集求值而非在场键：缺席槽可感（清空后的「→ null」可说）；其值域为 Bool，槽知觉按构造不铸造指称。判据取按锚键控的知识（知晓边即此形），不取槽值——以值为键，可见性即携带值的信息。谓词读物化真相：「知其有不知其值」的知识居所仍是账本，感知不物化为内容。

## 判定与对应

| 记号 | 符号 |
|---|---|
| `𝒞` | `Change` |
| `s` | `Step` |
| `span(s)` | `FieldSpan` |
| `refdom` | `visibleIn` |
| `ref(c, s)` | `referentsOf` |
| `可说(c, s)` / `存在(c, s)` | `spineLines` 的 speakableOf |

## 公理

**账本公理**　一切状态是 `w = (t, E, R)`，一切机械后果可说且只可说为变更 `𝒞`、否决 `Denial`、刻账目 `ticks`。状态侧无账本外隐藏状态，动态侧无裁决外隐藏变量。

**门公理**　∀ 变异恰一入口 `G(Δ*, src)`；`G` 全函数——确定性代码的崩溃在门内代谢为否决（法则崩溃 → `rule.crash`，链终止、时价照耗；系统崩溃 → `system.crash` 失败刻步，余系统照跑、时刻照走；审计崩溃 → `invariant.crash`）。

## 推论

**提交全序**　`G` 以裁决读态 `w⁻` 为回滚锚与出处锚。（门的原子性；重放公式使 `𝒞` 可复原任意时刻的世界）

**内容与坐标**　`E, R` 是内容，门的原子域；`t` 是坐标——不产 `𝒞`，写者唯一（落钟循环），谓词照常读钟。推导：钟的每次写入都必须归因于某次授予的落钟（刻账目），任何其他写入者都绕过账目；回滚的坐标边界由账目裁定——已入账的刻不可回滚；未入账的刻必须一并回滚，保留无账目的流逝违反凡刻必属某步。授予刻数走完才产生步骤（凡不可说者不发生），故回滚永不越过已入账坐标。

**刻账目闭合**　授予区间的每刻或言或默：`言@at` ＝ 通过变更行判据的变更行、事实、拒绝之并；`言@at = ∅` 即默。言默随步入账冻结；投影只可省略行文，不可重划言默——静默聚合 `timePassed ×n` 计 `|{at : 言@at = ∅}|`，是记录的函数，不是渲染的函数。

**凡不可说者不发生**　投影钩子与内核代码的异常不在 `G` 的输出域内代谢：apply 边界原子回滚后原样重抛（无记录的后果违反账本公理的可说性）；正常返回 ⇔ 步骤流与账本一致且授予刻数走完。

**随机是世界的纯函数**　同地址恒同值 ⇒ 事件重放与存档恢复得到同一命运。

**感知是注入的投影**　裁决谓词只读世界真相：πₑ、πₚ 不进裁决，可达性等机制谓词是投影的原料。参照域按定义属于门——它划「意志能点名什么」，感知划「呈现能显什么」。

**言语是惰性对象**　意图经映射译码为类型化参数，规则对参数裁决；原文以字面值存入 props/relations；言语的机械后果只能由 speech-act 动词物化。

**动力学时机**　确定性代码的可运行时机：动作裁决（rules）、刻内（systems）、提交审计（invariants）——同一扇门。时间条件属于规则/系统谓词，不属于坐标写与审查。

## 可说性

**判定跨度**　`span(s) = (refdom, 边键截面, 属性键截面)(w⁻, w⁺)`——动作步跨其提交边界，刻步跨所属系统的提交边界；边/属性截面仅于对应钩子声明时存在。随步入账冻结，提交后不可重算。

**变更行判据**　行 c 于步 s 的可说判定（internal 变更先滤）：

```
可说(c, s) ⟺ 存在(c, s) ∧ ref(c, s) ⊆ refdom(w⁻, player) ∪ refdom(w⁺, player)
存在(c, s) ⟺ c 非 rel/prop，或对应键截面未声明，或键(c) ∈ 截面⁻ ∪ 截面⁺
```

`ref(c, s)`——渲染指称集对渲染封闭，凡行铸出的同一性皆指称，与语法角色无关：rel 行为端点；prop 行为主语实体与 id 型值指称，未披露侧不数；spawn/despawn/rename 行为实体自身。键(c)：rel 为 (from, to, type)，prop 为 (entity, prop)，截面即 span(s) 的边键/属性键截面。键截面未声明则两侧均披露；一侧在场一侧缺席时行可说，缺席侧值以 `?` 占位（认知在提交内完成的形态：未读而誊 ⇒ 旧文为 `?`）。理由与 Fact 不过投影（规则铸造的世界语，作者特权）；跨度不含的离场者其 despawn 行恒沉默，跨度可说的历史行离场后仍以离场名渲染。沉默行不销毁信息：净效果由状态视图与新见段承接；视图与变更行都不可及的知觉只能由作者通道（Fact / extra）铸造。

**名字解析**　按位点分律：变更行由冻结跨度把门，尝试行恒可说而指称不经跨度门。变更行指称（prop 行主语、id 型属性值、关系端点）：在世取现值（改名连续），窗口内离场者取离场名表——行可说即指称在冻结的跨度内，名字于行被说出时已公开。尝试行 ref 参数：合法性由裁决读态的参照域判，过门者取现值（本动作焚毁者落离场名），出域者恒原样回显，在世名与离场名都不查——幻觉、隐藏、离场同一回显，尝试行不成为存在性 oracle。未声明为引用的字符串一律字面，与实体 id 碰撞不铸造指称；解析不出同回显。

**名字闭合**　记录在案的合法指称恒可解析。推导：ref 参数过门 ⇒ 裁决时在世；id 值与关系端点过审查 ⇒ 提交时在世；引用后于死亡的 delta 被审查回滚 ⇒ 失效至多发生在引用之后；窗口取时间序后缀 ⇒ despawn 记录与引用同窗 ⇒ 离场名可解析。例外：被拒尝试的出域引用恒原样回显——非合法指称，不解析。

**事件投影**　AI 所得 = `Π(窗口, w_now) ⊕ 感知变更行 ⊕ 作者 Fact`。近况 `Π` 是纯函数投影，永不持久化：窗口内回合记录按与 act 结果相同的变更行判据投影（推论·刻账目闭合），体量由 `recentWindow` 独自调节；言默随步入账冻结，唯名字取现值。act 结果的新见段是同一装配线的回合内增量：本回合新进 refdom 的实体以状态视图同形的实体卡承载。

**投影的消费分类**　进协议通道（约束裁决可行集）的投影，与真相的一致性机械保证；只服务工具与呈现的投影永不进协议通道，形态自由。豁免：`reason`/`facts` 进协议通道但属规则铸造的世界语，机械只保证送达。

## 主体性

**意志不在世界里**　`def.playerId` 是指向普通实体的锚引用，integrity 恒查其在世——锚在提交边界不可缺席；指针在 def 层不可重绑。账本无玩家。

**缺省主语**　无主语动词的主语 = `hostOf(w, player)`：`in` 链上最近的宿主，缺省锚自身；视角锚定 hostOf 是 games 构件的取值。

**附身**　占据 = 锚实体 `in` 的迁移，一条 delta 过门。可生灭换角的游戏把锚指向不朽实体、宿主建为 id 属性：宿主之死被 id 引用契约拦成显式迁移（迁移可说，静默回退不可说），迁移合法性由过渡不变式钉住。

## 回合协议

**单意志回合**　act 是唯一的提案通道，每回合恰一个裁决窗口；提案唯一形态是映射译码，被形态校验拒绝的调用未入裁决、不占窗口，门闩封的是占用后的再次调用；提案不得见自身结果，裁决先于叙述——消费结果的续提案使映射器升格为第二意志，单意志的成立压在窗口一次性上。

**通道次序**　整批先过静态形态检查再占窗口，随后批次执行是 apply 的 fold——后一动作在后一世界态上裁决。静态违约不产生步骤：边裁边检会让中途违约在步流中留下无痕缺口；混批任一违约则整批不进裁决。

```
apply(a) = adjudicate(a) → G → 逐刻落钟（每刻各系统独立过门）
ticks(s) = ok(s) ? (ticks(J) ?? cost(verb)) : cost(verb)
```

**拒绝恒价**　一切拒绝来源——可见性门、法则、审查、崩溃——落同一 `cost(verb)`（ticks 公式的拒绝支）：`Deny` 无 ticks 字段，结构上不可改写；审查回滚整个授予时尝试仍耗尝试价。拒绝同样落钟：刻是世界的因，不是意志的果。时价的定义域是「入裁决」，不是结果；`cost = 0` 属作者定价——否决零价即对世界的免费查询，反穷举由此归于作者侧：提案有界（门闩封占用，校验拒绝的重提触不到世界）、域外指称统一文案（门）、理由世界腔（法则）。

**静默刻**　多时间尺度由 systems 谓词从刻数取模派生，复杂历法可自持计数器。

**回合定稿**　`(time, intent, steps)` 入账，intent 逐字保留；绕过引擎的裸 apply 不产生条目。落盘是定稿的完成条件：条目未落地即回合未定稿，档案上未曾发生。

**映射契约**　纯语义解析：无关键词、正则、别名表、语言限制。构造不出合法提案时提交空提案，不写理由；「预计被拒」是提交的理由，合理性的判断属于世界。

**表达契约**　`voice` 是静态世界语言文档，原样注入 system prompt，缺省无契约。散文对已锚定内容的转写与渲染自由。`spineLines` 是事件流的规范单行渲染：符号承担结构（✓/✗/⏱/×n），语言词全部来自 messages/label/规则文案。`summarize` 是可选的声音覆写，崩溃回落缺省——呈现缺陷不得丢弃已发生的账目。叙述不回流：回合输入 = 状态视图 + 变更 + 近况。

**呈现服务**　narrate 无提案通道、无 act 通道、无时间流逝，不是回合；输出不入账、不进投影。用途：开场与仪器的场景重渲。

## 词汇面

- 世界语言：玩家可见的一切文字（法则理由、不变式 message、回退摘要）与表达契约 `voice`。core 不内嵌文案，`messages` 由游戏注入（noResponse / invisibleEntity / defaultReason / timePassed）。
- 通道语言：引擎与 prompt 脚手架之间的协议文本（工具描述、校验反馈、协议锚）。映射契约文案单源：系统提示、工具描述、回合提示、门闩与内核形态反馈共享同一措辞；宿主自产的 schema 报错在单源之外，协议只约束其语义（指名违约点、足以据以修正），不约束其措辞。
- 开口键按字面：关系类型与 tag token 无声明卡，机械行按字面言说，其世界腔由表达层承担。

## 面向作者的契约

- **动词表**：schema 生成 act 参数校验并推导规则参数的编译期类型；参数须为标量，多重性由批次承载；字符串参数须声明 ref/free，漏报在加载期失败。`internal` 动词不进广告面（广告面 = 裁决面 ∖ internal），由代码直接 apply——同一裁决边界与审查；其步不产尝试行，后果由状态视图与新见段承接。
- **法则纪律**：键控形态（规则按属性组合键控、量级存于属性载荷，随新实体自动泛化）与 authored 形态（互动逐实体书写）共用同一裁决与审查；键控禁止特判实体 id。规则的读通道唯一在 w⁻：同一值地址（实体×键、边）的多次写只能是一条预计算轨迹，从 w⁻ 重复派生的叠加增量按序覆盖。
- **拒绝**：结构化 Denial，理由以世界腔内联在规则文本，缺省回落 noResponse；拒绝不携带涉及实体——指称落点在 action 参数（结构化）与法则理由（世界腔）。`deniedBy: rule`（卫语句链、可见性门、unanswered 闭合）是玩法；`invariant`（审查否决、授予形状违约、`*.crash`）是必要性拦截，probe 报 bug。覆盖面没有机械判据：裁决地图供人判读拒绝的兜底落点与授予的后果形状，缺口判断属创作者。
- **前置站点**：静态形态违约（未知动词、schema 不符）错在裁决之外，拒绝不占窗口。「未知」相对通道而言：宿主 schema 与入口内预检按广告面（裁决面 ∖ internal）先后校验意志提案；内核形态检查的全集是裁决面，供代码直连 apply（internal 动词的合法通道）——内核收到违约动作即调用方违约，抛 `ProtocolViolation`（宿主路径不触发）。语义前提写成卫语句链首；接线判据：前提的否定情形落在参照域内（可指名而不可及）才接线法则，域外（不可指名）由门独任。grounding 以可达性谓词定义感知域是合法取值，代价是物理前提被门吸收——两者的分界是同一次决策，改其一必重审另一。
- **裁决地图**：probe 对每动词生成尝试空间的有限生成集——指称参数穷举可见域、必填自由参数取类型代表常量（每动作在独立 Simulation 上裁决）。probe 是法则裁决的唯一动态观察处（法则即代码，无法静态枚举），两面：活性矩阵（法则×动词的授予/拒绝/弃权/未达）只观察表态存在性，零表态是否死法则属作者判读（值条件法则之于常量，同状态条件之于初始世界）；逐 op 落行——拒绝行载法则与理由（兜底落点），授予行按授予法则×变更形状（kind＋键集；internal 不滤，裁决侧照常读）分组、载代表 op 与计数，归因经 probe 的插装而非步（步无出处）。核心级否决（invariant 且无世界腔理由）单列为 bug。

## 通道代数

core 的机制增长只由「无法表达」驱动。「无法表达」的判定：裁决面图灵完备（规则/系统/不变式是开放词汇上的任意代码），不可表达只可能栖居于定签通道——游戏意义必须穿过的、类型被 core 钉死的边界。每通道以载体（能持什么）、粒度（能对什么区分）、方向（状态/感知/事件/记录/入）刻画：

| 通道 | 载体 | 粒度 | 方向 |
|---|---|---|---|
| props / internal | V | 实体×键 | 状态/感知/事件 |
| relations | V | 边 | 状态 |
| grounding | id 集 | 锚×实体 | 感知 |
| edgePerception | Bool | 锚×边 | 感知 |
| propPerception | Bool | 锚×实体×键 | 感知 |
| digestExtra | ViewValue | 锚×自由 | 感知（仅状态面） |
| narratableChanges | 变更行 | internal 滤 | 事件 |
| viewCard | 实体卡 | internal ∧ πₚ | 感知 |
| span | 实体/边键/属性键集 | 锚×步·随步冻结 | 事件 |
| Verdict | Δ/reason/facts/ticks | 步 | 事件 |
| Action schema | 标量 | 尝试 | 入 |
| 回合记录 | intent + steps | 回合×窗口 | 记录 |

方法 = 覆盖矩阵：机制维度 × 通道粒度求覆盖，空格即候选；候选依次过证伪路线——换通道（边→属性、拒绝→零 delta 授予、提问→拒绝理由）、物化（历史/预期/信念 → 属性）、拆动词（成本不对称、条件 schema → 动词分裂）、键设计（命运共享、效果叠加 → 地址/数据归约）——皆不通者才是「无法表达」候选，且须由真实探针实例化方可立项。

可证伪预测：单意志游戏中通道矩阵闭合，未来 core 增长只可能来自多意志变体的批次同时性（批次执行是 fold，单意志下休眠）。

## 非目标

- 词表式语言约束（语言是 AI 的原生能力）
- 一次提交的动作数上界、刻数授予上界（不违反任何契约）
- 散文断言检查器（忠实是赌注）
- 数据法则（AST 解释器，无明显收益）
- 回合纪要（自述偏差经上下文跨回合放大）
- 直达提案通道与仪器摇钟（意志的提案必经映射译码；时间控制属于玩家动词与工具层夹具）

## 架构决策
- pi agent SDK：进程内集成，`node_modules/@earendil-works/pi-coding-agent/docs/`
- **IPC 形态**：主进程 → 渲染层推送事件流（`narration_delta` / `narration_reset`），渲染层 → 主进程请求（invoke）。原始 pi 流（映射期文本、thinking）不出 Engine。
- **回合编排（单 pass）**：每回合一次 `session.prompt()`——模型先调 `act`（one-shot 门闩），工具结果即世界回应（骨架行 + 未解析行 + 新见卡），其后输出散文。相位 mapping（文本丢弃）→ narration（入账）；重试作废在途生成（`narration_reset`），镜像 pi 语义。
- **上下文裁剪**：每次调用经 `context` 扩展（`core/context.ts` 为策略单一来源）裁剪为「近况投影 + 最后一条 user 消息起的后缀」（工具结果存为独立 toolResult 角色、不并入 user 消息，回合内该锚恒为回合提示，叙述续行因此保住裁决前缀；context 事件的消息是深拷贝，就地改写不污会话文件）；会话文件累积全量消息作审计；持久化事实是回合记录，近况是其纯函数投影，只在窗口更新点（create / recordTurn）整体重算——回合协议使该点与映射消费点观察等价，进程重启由记录重建。装载纪要按消费判据（逐条试投影存活）定完好：损坏使近况截断至其后完好子后缀并告警显形，会话文件不动——纪要只喂投影与审计，门不读纪要；截断而非逐条剔除，因名字闭合依赖完整时间后缀。修复只覆盖损坏——损坏可检（形状/投影失败），丢失不可检（缺席无痕），故档案完整性在写入侧闭环：回合落地是封装单元（编年史条目落盘 → 内存入账 → 近况重投影 → 状态存盘），条目落盘失败即 act 失败停止、state.json 不落，档案两侧同留上一回合——未能入账的回合在档案上未曾发生。
- **缓存稳定性**：状态每回合重注入使跨回合消息前缀不稳定，复用只剩 [tools+system] 头块——系统提示与工具数组字节级稳定，不做 setActiveTools 相位切换；回合内（act 裁决后的描写续行）前缀 [tools+system+user] 逐字节稳定（近况头并入的 user 消息在回合内不变）。compaction 保持关闭：pi 缺省摘要把旧叙述重新注入，与「模拟层唯一真相源」相悖。

## 验证

编译通过、场景通过、e2e 映射与表达准确都是伪信号，不证明设计正确；验证靠阅读 e2e 会话与分析源码。e2e 的 provider 用 `opencode-go`，model 用 `mimo-v2.5`。

## 暂时不做
- SQLite 持久化
- Electron 桌面壳