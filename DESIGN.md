# 语言游戏引擎设计方案

## 0. 公理

core 的全部承诺共三条公理：**公理一**陈述世界的形状，**公理二**陈述门的管辖，**公理三（回合律）**陈述通道的离散化（时间律随公理三一并陈述，但身份是公理一/二的导出形态）。公理之下没有并列的第二层承诺——随机、感知、动力学分类都是从公理推出的形态（推论），逐条注明推导来源；**指不出推导来源的引擎形态即设计警报**。未来任何特性提案必须注明触碰了哪条公理（或推论）、归入哪个声明面；无法归类的提案默认拒绝。

1. **世界即账本。** World = 实体（稳定 id + 身份卡 + 属性包）× 关系边 × 一个整数时钟，此外无物——这是玩法的全部真相。「此外无物」同时封死两侧：状态侧不得有账本外的隐藏状态，动态侧不得有裁决中的隐藏变量（随机推论由此得出）。账本的唯一职责是让真相**可说、可 diff**：凡产生玩法后果者，其后果必可说——必能表达为叙述者可以合法说出的一条 Change 或 Fact；中间状态（隐藏的原因、调度、计数）可不可说，经 `internal` 属性合法存在。身份卡（name/kind/tags）是引擎固定的第二词表：恒进序列化、恒可被规则键控。身份卡是协议锚而非时变真相——键控与指称赖以稳定的基准，无变更 delta；时变真相一律归属性包（属性包归注册表管辖），身份卡键集封闭故无注册需求、但同受承重墙纪律约束。
2. **一切后果过唯一的门。** 一切状态变异经过提交硬墙（快照 → 提交 → 不变式校验 → 违反则原子回滚）——本引擎区别于「AI 跑团」的身份所在，不可协商。门的管辖有二：**必要性**——墙审世界，任何 bug 无法绕过；**出处**——后果只能由确定性代码（规则 / 系统）产出，语言只从两端进出：意图文本进（映射层翻译为被 schema 收口的动作提案），世界腔裁决文案与表达散文出；模型产出只能作为被代码消费的类型化参数进入世界，永不作为裁决——「语言不入账」「LLM 从不提案数值」都是本款的昵称，精确形态是出处。门不审散文：表达侧无机械门，散文保真整体托付模型能力与提示约束，结构上的残余保证只有叙述不回流与未裁决/空散文回合的确定性摘要兜底——二者不拦截、只封顶（幻觉至多存活单回合视面）。
3. **回合律（协议性公理）。** 一回合、一意志、一裁决：act 是言语行为通道，每回合恰好一个说话人；one-shot 门闩封闭变异窗口，单 pass——一次会话运行内 act 一次性提交先行，叙述只能跟随裁决。本条陈述使公理一/二得以成立的通道离散化：意志不唯一则出处无主（不知替谁裁决），窗口不封闭则门被回合内续写击穿。多意志即协议变更（回合调度），属另一品类；实时同理。**时间律（导出形态，随本条一并陈述）**：世界时间只经裁决边界流逝，刻数由裁决授予；时长是行为的后果维度：动词声明尝试时价（成败皆消耗，协议性拒绝除外），规则可在授予中改写实际刻数，语言可经类型化参数提案时长、由规则裁决；无裁决即无流逝。推导：流逝的时间是玩法后果，凡后果必可说（公理一），钟必须可被因果层推动；时长只能出自裁决（公理二），模型永不直接提案时间数值。刻是世界的因果步（每刻 systems 照常运行），不是回合的别名。

> **语言二分**（公理二出处轴的词汇面）：「core 不内嵌任何语言」管辖**世界语言**——玩家可见的一切文字（法则理由、不变式 message、回退摘要、表达散文）——由游戏注入，永不成为状态。**通道语言**——引擎↔模型的 prompt 脚手架、act 工具描述与校验反馈——是协议基础设施，core 单一来源、从不进玩家视野；选用哪种自然语言是自由决策，不构成公理违反。

**推论**（形态被公理锁定，非独立承诺）：

- **随机是世界的纯函数**（由公理一）：无隐藏变量——apply / dryTick / 存档恢复对同一状态天然一致。机械形态：roll 是 `time#key` 的纯派生；key 需同 tick 唯一属工程纪律，非公理内容。
- **感知是注入的投影**（由公理一与 §2 极简立场）：感知谓词不是世界的真相，是账本向消费者的投影；投影是状态的函数，函数住 def 不住账本，core 不内嵌任何投影语义（内嵌即重本体），只留空槽：grounding / digestExtra / reach / holdable。缺省不对称是工程理由而非公理内容：全可见/全可达是不含属性语义的恒真谓词，缺省放开只扩大认知；holdable 缺省关闭，使「漏声明钩子」在规则的施动前提卫语句层变响。梦核等违反现实规则的世界由 authored 形态承载，不需要 core 让步。
- **动力学三分**（由公理二与公理三）：同一扇门、同一产出原语（Delta / Fact / Denial），确定性代码只有三个可运行的时机——动作裁决时（规则，管此刻的离散因果）、tick 内（系统，管此后的连续因果）、提交审计时（不变式，管永远）。三分是出处的时机编目，穷尽且互斥；不得混装——时机是因果的时间域标签，混装即出处失真。

## 1. 身份：协议、基底、作者界面

「core」是三种不同身份的东西装在一起，混淆它们是一切设计困惑的来源：

| 层 | 内容 | 身份 |
|---|---|---|
| **协议** | `Action{verb,params}` 进 → `Delta / Change / Fact / Denial` 出；提交过不变式硬墙 | 引擎的身份所在，已完成且应冻结 |
| **基底** | Entity / props / relations / clock 这套 World 形状 | 实用主义的事实账本，可用但不可神圣化 |
| **作者界面** | verbs 集合 + 四个声明面 | 受钩子准入原则约束的生长区 |

「本体论 + 规则代码」的结合不是事故，而是放弃三个替代方案后的幸存点：纯数据法则已死（AST 解释器）；完全自由的代码沙盒丢掉可序列化、可 diff、可核验的边界；重本体（DF/CDDA 式专用结构）只在领域固定时成立，与谱系目标（§6）矛盾。「薄的可说基底 + 代码裁决」的代价是词汇看守问题。

**本质清单**——以下六件承载引擎身份，其余一切皆可替换：

> 单 pass 回合 + 叙述不回流（公理三与公理二的执行机构：语言从入口进、出口出；act 一次性提交先行，叙述只能跟随 act 返回的裁决，出口的散文永不回流为输入——切断两条幻觉路径）；时间律随行：世界时间只经裁决边界流逝、刻数由裁决授予，时钟因此可说、卫语句规则（裁决即代码）、不变式原子回滚（重量的保证；可见性含状态与提交，§3.3）、spawn/despawn（动态拓扑，生长与收缩对称，§3.4）、属性注册表（可说性控制面：type/label/internal/stylistic 是公理一逃生舱、公理二出处隔离、变更线性化的共同底座）、感知空槽与缺省不对称（感知推论的机制本体：可见/可达缺省恒真，holdable 缺省关闭使遗漏变响）。

## 2. 定位与核心主张

一种新的游戏类型引擎：**以确定性状态机为重量锚点、以 LLM 为映射与表达引擎**的交互式语言游戏。

- **操作比文字更加核心。** 玩家操作的「重量」由确定性裁决保证，AI 只负责让每次操作的反馈有文学生命力。重量住在裁决层，不住在解释层：同一动作提案的裁决确定可复现；意图→提案的映射是概率的（同文本跨 run 可命中不同动词），解释保真度是体验参数而非内核保证。
- **AI 不创造规则，AI 不产生系统后果。** 数值与后果只能由规则 / 系统产出（公理二），映射层只提案、表达层只叙述。
- **表达保真是赌注。** 散文正文无机械检查，实体级与谓词级保真一并托付提示词与模型能力。这不是偷懒而是机械极限：申报义务不可机械强制（强制被空申报平凡满足）；「散文 ⊆ 状态」的覆盖差不可机械判定（需解析语义＝第二个被信任的语义引擎，循环信任）——任何表达侧机械校验都只能覆盖模型自选路径的窄面。表达侧的结构性残余只有叙述不回流与摘要兜底。
- **自由度上限由作者协议决定。** 引擎每少承担一分职责，作者协议的可能性上限就高一寸——即使这增加创作负担；语言的开放性决定逼近上限的体验摩擦，不构成机制层面的无限自由。

## 3. 模拟层（确定性裁决内核）

接受结构化动作，返回新状态与结构化结果。动作空间由游戏声明的动词表驱动，后果由卫语句式规则产出。

### 3.1 规范面：动词 + 卫语句规则

- `GameDef.verbs` 是动词表：每个动词含 `schema`（TypeBox，生成 act 工具参数校验并推导规则参数编译期类型）、`cost`（尝试时价，时间律的动词面）、`entityParams`（实体参数，供可见性校验）、`rules`。动词集由各游戏声明（era 类声明 talk/travel/equip，开放世界声明 attack/trade/craft），引擎不硬编码任何动词。
- **主体性两概念：意志与视角**：core 在主体性上只持有两件事，机械形态 = 一条协议不变量 + 一根引用。**意志**（玩家）：act 通道的说话人，每回合恰好一个（one-shot 门闩执行，无需 id）——意志不在世界里，账本里没有玩家，只有 def.playerId 这根指向普通实体的引用；它是意志在世界的全部足迹（居所），居所上的状态全是身体态，意志自身无状态。**视角**：感知函数的现值——游戏经 grounding/reach/holdable 注入函数（感知推论），世界态决定现值；grounding 圈定「意志能点名什么」，状态视图的实体索引与之构造等价（装配线保证；可感知而不可指名的纹理走 digestExtra 的无 id 通道），reach/holdable 经 Q 的具名入口交规则消费（施动前提等卫语句）。居所同时是四个缺省角色的共同缺省值：无主语动词的缺省主语（Q.player）、感知谓词的缺省视点、叙述「你」的缺省指称、墙的保护对象——前三者都是「现宿主」角色的缺省（体验者一侧：附身时「你」指器皿而非空壳），只有墙保护属引用自身的必要性（凡被解引用者必须可解）。二者之外皆非 core 概念：动词的语义主语是普通参数（entityParams）；身体/现宿主是游戏从世界态推导的取值（附身时居所退为空壳）。第一人称（感知锚定居所 + 无主语动词）、上帝视角（感知全开 + 主语显式入参，神力动词照常过提交硬墙——神是动词表更大的玩家，不是协议的越权者）、附身（钩子推导现宿主，视角与门自动跟随）都是这对概念的取值模式，零 core 改动。视角以居所为缺省锚点是惯例（第一人称的机制来源），不是必然——上帝视角与附身钩子无视该参数。人称与受话人是呈现投影的取值，非 core 概念（语言不入账，公理二）：core 只保证每回合把「谁在看」以可投影形态交给呈现层——账本 name 是协议锚而非时变真相（人称不是真相，name 无变更 delta），投影可动态呈现「你」；规则理由串内联人称，动态人称由规则从世界态计算或以无主语句式交表达层补足。切换扮演同理是取值而非协议：现宿主推导翻转即一条 delta、过提交硬墙（附身是动词，其 deltas 过门——够不着就附不了身）；def 重绑是不经裁决的旁路，永不提供；角色会死会换的游戏，居所须建模为魂/叙述者等 meta 锚（居所不朽，器皿可生灭）。引擎真正焊死的只有一件事（公理三）：**每回合恰好一个意志**——act 是言语行为通道，一次吟唱一个说话人；多意志即协议变更（回合调度），属另一品类。
- **规则即代码**：普通函数接收只读上下文 `Q`（world/player/time/params + `rel/relNum/roll/canReach/holdable/name` 等引擎语义唯一入口），返回授予（Delta 列表 + 世界腔理由 + facts）、结构化拒绝（Denial）或 null（不表态交由后续规则）。按序裁决，首个表态即判决——优先序就是书写顺序，无隐式控制流。例：「采下浆果」→ 映射提案 `harvest(bush)` → harvest 规则裁决返回 `[berries+1, ripe→false]`。
- **承重墙**：法则网络形态下规则必须按属性组合键控，禁止特判实体 id——检查双方的 `material` 硬度来裁决，不关心具体是哪个实体。键控面包括身份卡与属性包：`kind === "npc"` 与 `props.ripe` 同为合法键控，`p.entity === "x"` 同为越墙。泛化的另一半是数据载荷：量级与效果参数存于属性，一条规则覆盖一切可食之物——键控管「何时对谁」，载荷管「多少」，二者都是属性键控的合法形态。规则随新实体自动泛化，这是组合爆炸的唯一出路。
- **两种创作形态**，共用同一套裁决瓶颈与提交硬墙，core 不感知形态差异：
  - **法则网络形态**（era/DoL 极）：规则按属性组合键控、随新实体自动泛化；
  - **authored 形态**（梦核极）：互动逐实体书写（每条一个卫语句子句 + 兜底），实体生灭、动态拓扑、持有物改写互动结果。
- **拒绝**：结构化 `Denial`（law/subject/object/prop/reason/debug/fallback），理由以世界腔内联在规则文本里，缺省回落 `messages.noResponse`。`deniedBy: "rule" | "denyAll" | "protocol" | "invariant"` 是探测与审计的语义基础，不依赖理由字符串匹配；协议性拒绝（映射层形态错误）不进玩家叙述。
- **前置门为封闭集**：未知动词 → schema → 可见性 → 规则——门只执行协议必要性（动词存在、schema、可见指称），core 不执行任何语义前提；施动前提等语义前提一律写成第一条卫语句规则，经 `Q.canReach`/`Q.holdable` 消费感知槽位

### 3.2 动力学面：系统与时间

- `systems` 是按序执行的纯函数注册表（每 tick 运行，产出 deltas/facts），承载火蔓延、燃尽、日程等连续因果；**fact-only 纯氛围输出合法**——零状态变更的氛围事实同样进入表达输入，与机制事实一样只经结果视图供叙述跟随。
- **时长是裁决授予的后果维度（时间律的机制面）**：`VerbDef.cost`（缺省 0）是尝试的时价，无论裁决成败都消耗（协议性拒绝除外：映射层噪声不是尝试）；规则可在授予中以 `ticks` 改写实际流逝（睡一夜授四刻，期间 systems 照常运行，夜袭与饥饿在睡眠中真实涌现）；时长可经动词 schema 的类型化参数由语言提案、由规则裁决（「等到天亮」）。引擎按动作交织推进：每个动作裁决提交后按授予刻数逐刻运行 systems，产出并入表达输入，世界能在一次提交的多个行为之间反应；无裁决即无流逝（refusal 与未解析回合世界静止，这是定义不是缺陷）；刻是世界的因果步，不是回合的别名。
- 随机经 `Q.roll(key, sides)`（随机推论；key 需同 tick 唯一，可含实体 id；同 tick 内重试同一判定得同一命运——时间流逝是命运变化的唯一来源）；时钟由行为驱动（时间律），全零时价的游戏命运冻结；多时间尺度（刻/日/月）由游戏自持计数器 + systems 内判定，core 不内置历法。

### 3.3 必然性：不变式硬墙

- 提交受两翼审查，任一违反即**原子回滚整个提交并拒绝**——法则与系统的 bug 都无法绕过。**执行翼**审「裁决被如实执行」（见次条）；**状态翼**审「执行后的世界成立」：core 默认恒挂引用完整性与注册表类型契约（id 唯一、id 型属性——标量或引用数组——与关系端点指向存在的实体；注册属性值与声明类型一致，number 拒非有限值——NaN/Infinity 经 JSON 序列化即静默变 null，是账本腐蚀通道；null/缺席为缺省惯例放行，any 显式豁免）；游戏追加领域不变式；守恒类种子读 `InvariantCtx.genesis`（实际起点世界的冻结快照，而非 def.world——存档恢复与变体开局同样成立）。
- **执行校验（fidelity，公理一「后果必可说」的执行侧形态）**：每条 delta 在其应用时刻必须可执行（逐条校验而非提交前预检——同一授予内 spawn 后 set 是合法书写）——目标实体与关系端点必须存在、spawn 的 id 必须未占用、inc/relInc 的现值必须是有限数（缺席/null 按 0 的既定语义，这是承重墙泛化的承重约定；非数现值拒绝而非静默改写类型）、一切数值后果必须有限（NaN/Infinity 无法经 JSON 存活，序列化即静默变 null 的腐蚀通道在提交侧封死）。任一不可执行即整体回滚并拒绝（deniedBy 仍为 "invariant"，law `invariant.commit`，debug 承载诊断，不进玩家文案）；幂等跳过的唯一判据是「目标状态已成立」：relSet 删不存在的边成立（无边即状态，悬空端点之间本不容边）；set 同值与增量零效果以目标存在为前提——主语不存在的「已成立」不可判定，存在性拒绝在前，永不回落为跳过。推导：裁决理由与变更流必须互证——静默丢弃 delta 即叙述对账本说谎；probe 将核心级不变拒绝（无世界腔理由）报为 bug。
- **不变式可见提交（两种形态，同一接口）**：`InvariantCtx` 除 `genesis` 外携带 `changes`（本次提交的全部变更，含 spawn/despawn 与 `src` 产出方标识）。**状态不变式**只读世界——守恒类，防总量漂移；**过渡不变式**读提交——provenance 类，防错误再分配：「把玩家的铜币改判给浆果丛」总量守恒而再分配非法，状态不变式不可见，过渡不变式以 src 白名单闭案。每条规则/系统的提交独立过墙，过渡不变式的粒度即单次提交。类型契约管词汇（注册属性值的形状），过渡不变式管语义（这一次提交是否必要）。
- **`deniedBy` 增 `"invariant"`**：墙的否决与法则否决是不同的语义来源——法则否决是玩法（世界性理由），墙否决是必要性拦截（规格违反信号或戏剧性必然），审计与探测应能区分。
- 游戏不变式的 message 是游戏撰写的世界腔，直接作玩家文案；core 完整性违反只留 debug 诊断，回落通用兜底文案。
- **意志的居所在墙的管辖内**：`playerId` 是 def 指向世界的唯一数据引用（意志的居所，四个缺省角色的缺省值，见 §3.1；感知钩子以它为缺省首参但可无视——缺省全见全达即上帝视角），integrity 不变式恒查其可解；初始世界同样过墙（构造期校验，genesis = 自身、changes = 空）——「不变式管永远」包括 t=0。由此显式声明两个立场：**指针不可重绑**（居所引用在 def 层恒定；附身/换躯不改指针——现宿主由游戏从世界态推导）；**居所不朽，器皿可生灭**——墙保护的是意志的居所而非任何身体：终局是状态不是删除；附身的器皿可 despawn，意志按游戏的推导回退到居所。

### 3.4 生灭与拓扑

- Delta 原语：`set / inc / relSet / relInc / spawn / despawn`（relSet 值 null 即删边——拓扑收缩与生长对称，存储边永不持 null）。提交产出按基底类别同构分形的变更记录 `Change`（kind: prop/rel/spawn/despawn——rel 携带完整边端点，next null 即删边）。生灭原语配合 relSet 建径/删径让世界拓扑生长与收缩；despawn 级联清理关系边，悬空 id 引用由完整性硬墙回滚。
- **生长与收缩对称**：硬墙对悬空引用 fail-closed 是必要性，不是 despawn 的缺陷；缺的是机械清点——core 提供引用清点原语 `refsTo(def, world, id)`（按注册表 `type:"id"` 枚举指向该实体的 (entity, prop)，标量与引用数组同覆盖，语义无关的机械操作），清理策略（置空、转移、级联生灭）是游戏语义，由规则决定；容器级 despawn（房间塌陷）同理由此表达（先迁散子女再 despawn）。
- 关系边表 `world.relations` 表达社会 / 叙事状态（信任、记忆、派系），表达层格式化为世界腔文本。

### 3.5 感知面：空槽与投影

core 不内嵌任何感知语义，全部为游戏注入的谓词（感知推论）：

- **状态视图是唯一装配线**：prompt 的状态视图由 core 组装——可见实体（grounding）× 注册表过滤（internal 不进 prompt，隔离由 core 机械保证、不依赖作者记忆）× 关系端点可见过滤，顶层并入 `digestExtra`（游戏派生纹理：出口、随身清单等）。参照域契约由构造保证：视图实体索引 ≡ 可见性门的权威集——模型看得见的才可指名，可指名的必看得见；`digestExtra` 无 id 承诺，携带可指名 id 时配合非 entityParams 参数消费（地点恒可指名，由法则层回答）。自由字符串的 digest 投影钩子不设——它会让 internal 隔离与参照域契约退化为作者记忆。
- **投影按消费者分类**：进协议通道（约束裁决可行集）的投影，与真相的一致性必须机械保证；只服务工具与呈现的投影（探测域、意图菜单）留在工具层（probe 的 per-game 探测域配置），形态自由，裁决与协议从不消费。
- `reach`/`reachReason` 是可达性空槽，`holdable` 是可持握空槽（core 不提供缺省，未声明即全不可持握）。缺省的不对称有明确理由：全可见/全可达是不含任何属性语义的恒真谓词，其消费者（枚举、规则）缺省放开只扩大认知；holdable 的消费者是规则内的否定性施动前提卫语句（`Q.holdable`，与 `Q.canReach` 同一模式）——缺省放开会让「漏声明 holdable」的规则静默放行一切工具前提，缺省关闭使遗漏在规则层变响。容器包含树等空间语义是游戏侧自愿接入的构件，core 不强制。

### 3.6 优化件（非本质，可替换）

以下机制降低特定形态的作者负担或映射错误率，不属于引擎身份：dryTick 合法预言（「即将发生」进结果视图）、`summarize` 回退摘要、`hint` 法则提示注入。

## 4. 映射层

玩家输入自由文本；LLM 职责是解析为动作提案列表 `[{ verb, params }]`，实体命中可见索引，动词取自已声明动词表。

- **纯语义解析**：无关键词匹配、无别名表、无语言限制——动词与实体由 LLM 从 `name + attrs + 上下文` 纯语义接地；选中文本作为实体接地锚点。
- **单 pass 回合**（本质）：每回合一次会话运行——模型先调 `act` 一次性提交动作提案（或仅 label 的结构化拒绝），one-shot 门闩封闭变异窗口；`act` 工具结果是世界回应的唯一载体（尝试行、时间流逝、法则事实、即将发生、新见，经 label 线性化、协议性拒绝过滤），叙述只能跟随它。这切断两条幻觉路径：模型不调工具却叙述动作（未调 act 的回合回落确定性摘要，其文本不作为叙述）、模型叙述未发生的后果（裁决就在上下文中）。时间分离不是本质——结构边界（门闩、硬墙）才是。
- **拒绝契约**：模型不撰写拒绝理由。refusal 的唯一判据是**构造性**：构造不出合法提案（动词表承载不了意图，或意图指称落不到可见实体）时返回结构化拒绝（仅 label）；能构造的动作一律直接提交，由规则层裁决——「预计被拒」不是拒绝的理由而是提交的理由（成败皆消耗的尝试时价正是「尝试 → 被世界拒绝」落差学习的材料），「是否合理」的判断属于世界、不属于映射。构造性是窄判据（枚举动词 × schema 适配 × 可见实体，无需世界模拟），把同类意图的跨 run 落点方差收窄到机械检查附近。合法性校验收敛于 `Simulation.adjudicateRaw` 单一瓶颈——场景 / CLI / probe / LLM 同一裁决口径。

## 5. 表达层（LLM，表达保真赌注）

AI 读状态视图（core 装配，回合 prompt 注入）+ 本回合变更（act 工具结果承载，经 props 注册表 label 做语言无关线性化）+ 法则 facts，输出该状态的文字化。

- **表达保真是赌注**：散文正文无机械检查，实体级与谓词级保真一并归提示词层。机械后果恰两条：散文为空或回合未裁决 → 确定性摘要整体替换（强接地兜底）、叙述不固化（残余至多存活单回合视面）。润饰属性（`stylistic`）允许文学润饰。
- **内部属性隔离**：`internal: true` 的属性不进序列化 / 变更线性化——模型根本看不到（公理一的不可说逃生舱）。
- **叙述不回流**：回合输入只有当前状态 + 变更（act 工具结果）+ 近况窗口，无旧叙述全文——幻觉不固化；会话文件仍累积全量消息作审计。

## 6. 状态机谱系

创作者自选落点，引擎不预设复杂度：

```
完全无状态机 ←────────────────→ 极复杂状态机
(纯AI聊天)      (Era/DoL级别系统)
 灵活性最大                        确定性最强
 游戏性最弱                        游戏性最强
 AI全权负责                        AI只做表达
 玩家行为无重量                    玩家行为有裁决重量
```

这张图是品类史的修辞，不是本引擎的参数空间。真实自由度至少是三个不共轴的旋钮：**裁决重量**（法则与不变式密度，决定玩家行为的可复现约束）、**动作空间广度**（动词 × 实体 × 规则授予面，决定可尝试的操作集）、**叙事依赖度**（须物化为 props/relations 才能存续的连续性，决定作者为故事连贯所付的税）。

身份下界：门的**在场**是身份，门的**通过量**是分级参数。纯 AI 聊天无门可过，不在本引擎身份之内——协议形态未被禁止，但引擎不为其设计、不承诺支持，它属于另一品类。重量锚点的强度由游戏声明的法则与不变式密度决定，不来自 core 的原语集合——开放世界极的「一致」就是一小撮领域不变式，不是法则穷举。

## 7. 交互模式：文本即操作面

玩家用任意文字表达操作意图，可选中场景散文片段作实体接地锚点。快捷键 / 菜单只是「输入意图文本」的快捷方式，本质仍是自由文本。

- 玩家定义交互面：阅读深度直接转化为操作空间广度。
- 解析失败也是游戏的一部分：无法解析的意图获得世界腔平淡回应（模型不撰写理由），玩家通过「尝试 → 被世界拒绝」的落差学习边界。
- 裁决保持精确：意图与实体命中状态机边界后才产生后果（精确住在裁决层；意图解析层是概率的）。
- 与 AI chat 的本质区别：有系统约束；与 MUD/roguelike 的本质区别：操作集情境化、动态生成，不是预设热键。

## 8. 引擎接口：四个声明面

GameDef 的字段按四个声明面组织（概念分类）。**钩子准入原则：一个钩子要有资格存在，必须命名某种内核既无法推导、又无法在不破坏某类游戏的前提下取缺省的信息；且必须归入某一声明面、注明触碰的公理或推论。跨栏的重新设计。** 

**核心增长准入：只有「无法表达」（而非「表达痛苦」）有资格驱动 src/core 的机制增长；表达痛苦是游戏侧构件与库层的信号。侵蚀度量：每新增一个复杂场景记录 src/core 的 diff——预测为零，首个以「无法表达」立项的非零 diff 即设计警报。**

```ts
const game: GameDef = {
  // —— 规范面：何为可能、何为必然
  id, title,                 // 实例元数据
  verbs: { ... },            // 动词表：label/description/schema/cost/entityParams/rules
  invariants: [...],         // 提交后硬墙（core 默认恒挂引用完整性）
  playerId,                  // 意志的居所：def 指向世界的唯一数据引用（四个缺省角色的缺省值，§3.1；integrity 墙管辖；现宿主不在此钉死）
  world,

  // —— 动力学面：时间如何流逝
  systems: [...],            // 每 tick 的连续因果（时长由裁决授予：VerbDef.cost / Verdict.ticks）

  // —— 感知面：世界向各消费者的投影
  grounding,                 // 可见实体索引（状态视图实体索引 ≡ 此集，装配线保证）
  digestExtra,               // 状态视图派生纹理（无 id 承诺）
  reach, reachReason, holdable, // 能力谓词空槽（感知推论）

  // —— 词汇面：世界的语言（core 不内嵌任何语言）
  messages,                  // 必填：core 产出的用户可见文案
  props: {...},              // 属性注册表：type/label/internal/stylistic
  hint,
  summarize,
};
```

引擎自动处理：act 工具 schema 生成、状态视图装配（参照域 ≡ 可见性门）、单 pass 回合编排（one-shot 门闩）、结构化拒绝接线、回退摘要。拒绝理由一律由规则层产出，引擎不生成理由。

## 9. 开放问题与非目标

**开放问题**（按门控推进，未测量不动手）：

- **社交记忆**：对话内容无法入账（公理二出处），社交状态只剩裸数值——数值没有记忆。候选方案（记录通道：AI 写「世界听见了什么」，存入隔离命名空间，后果仍归规则裁决；记录 ≠ 后果，软通道的静默假授予 bug 在此结构上不可能复现）仅在对话密集对照实验证明空心感伤害体验后立项。
- **现宿主推导**：附身形态的「现宿主」由每个钩子/规则各自从世界态推导（游戏侧义务，可写成 games/ 构件，同 space.ts 模式）。候选 `def.host` 钩子（缺省恒为 playerId，一次推导、可见性门/Q 全部跟随）。当前不准入：表达痛苦 ≠ 无法表达。

**非目标**（显式不做，让范围焦虑有去处）：

- 多 actor 离屏自治（NPC 由 LLM 驱动的 RimWorld 式野心）。
- 词表式语言约束（扫描器 / forbiddenTerms 类）：语言是 LLM 的原生能力。

## 附录：参考与灵感来源

| 游戏/类型 | 贡献的正向参考 | 本方案拒绝的负面参考 |
|---|---|---|
| MUD（北大侠客行） | 命令解析复杂度即技能曲线；trigger/别名编程 = 玩家在编程，元游戏层 | 固定命令 DSL 让操作固化；社群穷举最优序列后游戏死亡 |
| NetHack | 穷举交互空间的探索模式；物态/物品/怪物互动的操作空间广度 | 输出端用 ASCII 字符而非自然语言，浪费了输入端的交互复杂度 |
| era 系 | 基于状态机的交互模拟器；所有操作映射到庞大数值系统；可重玩性来自系统深度 | 交互脚本是预编码的，操作被固化 |
| Degrees of Lewdity | 多资源轮耦合的风险决策设计（意志/疲劳/创伤/暴露度互相影响） | 回合制+固定选项制造决策节奏，但操作集被预设 |
| A Dark Room | 极简操作中"造火"式的 gameplay reveal（操作中发现而非 lore dump） | 放置式点击循环，可用鼠标连点器，太蠢 |
| Progress Quest | — | "零玩家游戏"极端：建号后角色全自动升级打怪，玩家零输入。无操作=无决策=无技能度量，连放置游戏的点击都省去，玩家沦为观众，纯数字膨胀的斯金纳箱；谱系上位于"玩家行为无重量"的最底端（玩家整个缺席）。仅作为恶搞/艺术装置成立（拷问"游戏是什么"），作为游戏是真空 |
| Dwarf Fortress | material system：交互规则降维为属性运算 | 仅覆盖物理/化学属性，覆盖不了社会/叙事状态 |
| Visual Novel | — | 固定分支 = 翻书，AI+随机分支只是加噪音，无法深挖 |
| Lifeline 系列 | 真实时间流逝制造"陪伴在场感"——时间节奏可剥离出来作为真实决策压力（接上状态机） | 本质是"带时钟的 VN"：选择落在固定分支，真实感靠拖延内容制造而非生成内容。LLM 已使该类别整体退役——实时应答/无限对话是其原生能力 |
| 文字遊戲 (Word Game) | "文字作为可交互实体"的直觉有吸引力 | 把 NetHack 的"字符构建视图"推向极端：世界 = 汉字瓦片（墙是一排"墙"字，玩家是"我"字），字形既是视图又是谜题机制；文字沦为退化版图形引擎，文学性与操作重量双失。且谜题为手工设计固定关卡、无状态机，属"固化操作"的另一形态 |
| AI chat/跑团/写小说 | — | 无系统约束的即时幻觉，玩家行为没有实际重量；"不是游戏" |
| 风之旅人 / What Remains of Edith Finch | 文本表达应向电影级渲染靠拢的标准 | — |
| Minecraft | 方块即原子：世界由可组合的最小元素构成，任何交互都在元素层面成立，玩家自由度来自系统组合而非预编码交互 | 3D 方块空间表达不了叙事/社会状态；"破坏-放置"覆盖一切，无因果链 |
| Terraria | 发现式合成：大量可交互物品 + 探索中揭晓组合配方，玩家考古游戏系统 | 配方仍是预编码集合，穷举后操作固化（同 MUD 死结） |
| Don't Starve | 生存资源耦合 + 时间压力：饥饱/理智/血量互锁，季节昼夜驱动决策节奏，饥饿把"时间"变成真实资源 | 行为树/预设配方使行为固定；"活着"本身有重量但可穷举 |
| RimWorld | AI 叙事生成：pawns 的性格/关系/事件与模拟耦合，故事从系统状态中涌现而非手写分支 | 事件是预设叙事碎片，触发式而非涌现；操作大多是菜单指派 |
| Kerbal Space Program | 真实规则即重量：玩家用物理零件自由组装，结果由物理模拟裁决，轨道力学本身成为技能曲线；系统组合产出意料之外的结果 | — |
| Mount & Blade | 开放世界沙盒：玩家在世界中自由定位自己，经济/战争/声望由系统模拟，无主线 | 战斗与治理是预设操作层，系统耦合浅 |
| Kenshi | 系统驱动世界状态：派系战争、城市兴衰、经济由模拟产生，玩家行动有真实后果且无角色预设 | 角色成长是"做事累积数值"的浅系统，无深交互 |
| Cataclysm: Dark Days Ahead | 极端深度的物品交互：一切皆可拆解/改造/组合（车辆系统），时间流逝是持续压力，纯文本的交互复杂度巅峰 | — |
