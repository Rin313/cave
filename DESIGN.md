# 语言游戏引擎设计

## 定位

- 文本是操作面：意图以自由文本表达，不限定输入方式与交互 UI。
- 确定性状态机承担全部玩法后果；AI 只做映射（意图 → 提案）与表达（状态 → 散文）。
- 忠实是赌注：映射对意图、散文对状态的忠实不做判定。
- core 的机制增长只由「无法表达」驱动（而非「表达痛苦」）；games 层允许任意写法，多游戏收敛出的相同形态不得提升为 core 机制。

## 记号与契约

### 值

```
S   ::= String ⊎ 有限数 ⊎ Bool      标量
V   ::= S ⊎ S*                      账本值（S* 为有限标量序列），存储形态
V⊥  ::= V ⊎ {null}                  值语言：null 只出现在读数、清除写与变更记录
```

null 不是值：缺席即键不在场；清除写 null 即删键、删边。

### 世界

```
w = (t, E, R)          t ∈ ℕ，单调
e = (id, name, props)  id, name ∈ String⁺，props : K ⇀ V
r = (a, b, τ, v)       τ ∈ String⁺，v ∈ V；边身份 (a, b, τ) 在 R 内两两相异
```

- `K` 是属性注册表键集，`κ : K → PropDef`，`PropDef = (type, label?, internal?)`，`type ∈ {string, number, boolean, id, tags, any}`。词汇闭合：`keys(e.props) ⊆ K`；动态键值对走关系边（关系类型与 tag 是开口 token，无注册表）。
- `type: id` 的属性值（标量或标量数组）是强引用：被指实体须在世，「无引用」由缺席表达。关系边是弱引用（见 despawn）。
- `internal` 的属性与动词不进任何呈现面；裁决侧照常读世界真相。
- id 与 name 是实体的必备字段：id 承担协议指称（提案参数、键控），name 只承担呈现，是渲染解析的终点。

### 变更与门

```
δ ::= set(e,k,v) | relSet(a,b,τ,v) | rename(e,n) | spawn(ê) | despawn(e)       v ∈ V⊥
```

绝对写，后态自含；`set/relSet` 取 null 即删。幂等跳过限于状态写（set/relSet/rename）：`w ⊨ post(δ)` 即跳过，w 是应用该 delta 时的当前态而非 w⁻——无边可删、无键可清的清除写是空操作；同址多写后者覆盖。其余不可执行——目标缺失、端点缺失、spawn 的 id 占用——拒绝整提交；spawn/despawn 是存在事件，无跳过形态。

```
G : Δ* × Src → 𝒞 ⊎ Denial      原子：拒绝 ⇒ w 不变
Src ::= rule:⟨verb⟩.⟨rule⟩ | system:⟨id⟩ | def
```

`𝒞` 是变更记录序列，后态的完整 diff：prev/next 自含，spawn 携带完整后态实体，despawn 携带展示名（离场名的唯一来源）。

```
wₙ = w₀ ⊕ C₁ ⊕ … ⊕ Cₙ
```

despawn 级联删边，逐条入账且紧随 despawn 记录（弱引用随主消散，消散可说）；强引用不清扫，悬空由 integrity 回滚。

提交审查先执行校验（逐条，后到的 δ 可引用先到的后果——指向而非读值：spawn 后可 set 其属性；δ 无读原语，先到的值不可读），后不变式：

```
ι : (w, ctx) → msg?      ctx = (genesis, changes, src)
```

genesis 是实际起点世界的冻结快照（存档恢复、变体开局同义）；changes 是本次提交的全部变更。integrity 恒挂：id 唯一、必备字段非空、钟为非负整数、锚在世、词汇闭合、存储值 ∈ V、type 契约（id 引用在世——标量与数组；tags 为字符串阵列；其余按声明标量类型；any 豁免标量类型、不豁免账本值形状）、边形状与身份契约、三元组唯一。游戏不变式追加领域约束：只读 w 的为守恒类，读 changes 的为出处类。任一违反 ⇒ 整提交回滚并拒绝；游戏不变式的 message 即玩家文案，integrity 违反只有 debug（回落 noResponse）。

### 裁决

```
verb = (label, description, schema, cost ∈ ℕ, internal?, rules)
a    = (verb, params)      params : P ⇀ S      一次尝试 = 一次裁决 = 一个时价 = 一个拒绝单位
ref(a) ⊆ P                指称参数键集（schema 上 ref 标记的字符串参数）
Q   = (world, player, t, params, roll)        world 为深冻结快照，越权写即抛
J   : Q → Grant ⊎ Deny ⊎ ⊥
Grant ::= (Δ*, reason?, facts?, ticks ∈ ℕ?)
Deny  ::= (law, reason?, debug?)
```

卫语句链 `rules`：首个非 ⊥ 表态即判决；全弃权由引擎闭合为 `action.unanswered`。`ref` 参数先过可见性门，`free` 参数按字面径由法则裁决，门与渲染不解析其内容。

```
roll(t, src, key, sides) = 1 + ⌊h(t, src, key) · sides⌋      h : 确定性哈希 → [0,1)
```

骰子地址 `(t, src, key)` 元组编码并继承出处命名空间；同地址恒同值，sides 违约即抛。

### 步

```
Step ::= ActionStep | TickStep
ActionStep = (at, ok, reason, changes, field, action, ticks, deniedBy?, denial?, facts?)
TickStep   = (at, ok, changes, field, facts?) | (at, ok=false, denial, field)
```

序列按构造保序，at 为钟坐标。步是纯内容，不携带出处（归因的机器诊断在 `Denial.debug`，法则归因的观察处在 probe 的裁决地图）。刻步无应答通道：应答（reason）是动作协议的义务，系统不回应任何尝试。

### 感知

投影钩子住 def 不住账本，签名统一收意志锚而非其推导值（如宿主）：锚的自见排除、按锚键控的知识都以锚为变元，体验者由钩子自行推导，反向不可；钩子可无视锚（上帝视角即常函数）：

```
g   : W × Player → 𝒫(id)              参照域，缺省全见
πₑ  : W × Player → (R → Bool)         边感知，缺省恒真
πₚ  : W × Player → (E × K → Bool)     属性感知（槽谓词），缺省恒真
x   : W × Player → ViewValue          派生纹理，形态自由，无指称声明面
```

状态视图与可见性门同源（`refdom(w) = g(w, player) ∩ E`）：

```
view(w) = ( t,
            entities  = { card(e) : id(e) ∈ refdom(w) },
            relations = { r ∈ R : {a, b} ⊆ refdom(w) ∧ πₑ(w)(r) },
            extra     = x(w) )
card(e) = ( id(e), name(e), props ↾ { k ∈ K : k ∉ internal ∧ πₚ(w)(e, k) } )
```

πₚ 对 `E × K` 全集求值而非在场键：缺席槽可感（清空后的「→ null」可说）；其变元不含值，槽知觉按构造不铸造指称。谓词读物化真相：「知其有不知其值」的知识居所仍是账本，感知不物化为内容。

## 公理

**账本公理**　一切状态是 `w = (t, E, R)`，一切机械后果可说且只可说为变更 `𝒞`、否决 `Denial`、刻账目 `ticks`。状态侧无账本外隐藏状态，动态侧无裁决外隐藏变量。

**门公理**　∀ 变异恰一入口 `G(Δ*, src)`；`G` 全函数——确定性代码的崩溃在门内代谢为否决（法则崩溃 → `rule.crash`，链终止、时价照耗；系统崩溃 → `system.crash` 失败刻步，余系统照跑、时刻照走；审计崩溃 → `invariant.crash`）。审查 fail-closed：任何 bug 无法绕过；后果只能由确定性代码产出。

## 推论

**提交全序**　`G` 以裁决读态 `w⁻` 为回滚锚与出处锚。（门的原子性；重放公式使 `𝒞` 可复原任意时刻的世界）

**内容与坐标**　`E, R` 是内容，门的原子域；`t` 是坐标——不产 `𝒞`，写者唯一（落钟循环），谓词照常读钟。推导：钟的每次写入都必须归因于某次授予的落钟（刻账目），任何其他写入者都绕过账目；回滚的坐标边界由账目裁定——已入账的刻不可回滚；未入账的刻必须一并回滚，保留无账目的流逝违反凡刻必属某步。授予刻数走完才产生步骤（凡不可说者不发生），故回滚永不越过已入账坐标。时间条件属于规则/系统谓词，不属于坐标写与审查。

**凡不可说者不发生**　投影钩子与内核代码的异常不在 `G` 的输出域内代谢：apply 边界原子回滚后原样重抛（无记录的后果违反账本公理的可说性）；正常返回 ⇔ 步骤流与账本一致且授予刻数走完。

**随机是世界的纯函数**　同地址恒同值 ⇒ 事件重放与存档恢复得到同一命运。

**感知是注入的投影**　钩子缺席取最少销毁信息的形状（全见、恒真）。裁决谓词只读世界真相：πₑ、πₚ 不进裁决，可达性等机制谓词是投影的原料。参照域按定义属于门——它划「意志能点名什么」，是认识论边界，不是感知判据。

**言语是惰性对象**　意图经映射译码为类型化参数，规则对参数裁决；原文以字面值存入 props/relations；言语的机械后果只能由 speech-act 动词物化。

**动力学时机**　确定性代码的可运行时机：动作裁决（rules，离散因果）、tick 内（systems，连续因果）、提交审计（invariants，永远）；同一扇门、同一产出原语（Δ / facts / Denial）。

## 可说性

**判定跨度**　`span(s) = (refdom, 边键截面, 属性键截面)(w⁻, w⁺)`——动作步跨其提交边界，刻步跨所属系统的提交边界；边/属性截面仅于对应钩子声明时存在。随步冻结，提交后不可重算——记录须是证据而非视图。

**变更行判据**　行 c 于步 s 可说 ⟺ `ref(c) ⊆ refdom(w⁻) ∪ refdom(w⁺)`。渲染指称集：rel 行为端点，prop 行为值指称（未披露侧不数），spawn/despawn/rename 行为实体自身。rel/prop 行另受键截面约束：存在性按两侧并集；值侧分侧披露，未采样侧以 `?` 占位（认知在提交内完成的形态：未读而誊 ⇒ 旧文为 `?`）。理由与 Fact 不过投影（规则铸造的世界语，作者特权）；internal 变更恒滤；跨度不含的离场者其 despawn 行恒沉默，跨度可说的历史行离场后仍以离场名渲染。沉默行不销毁信息：净效果由状态视图与新见段承接；视图与变更行都不可及的知觉只能由作者通道（Fact / extra）铸造。

**名字解析**　按位点分律：变更行由冻结跨度把门，尝试行恒可说而指称不经跨度门。id 型属性值与关系端点解析为名字：在世取现值（改名连续），窗口内离场者取离场名表——行可说即指称在冻结的跨度内，名字于行被说出时已公开。ref 参数的合法性由裁决读态的参照域判：过门者取现值（本动作焚毁者落离场名），出域者恒原样回显，在世名与离场名都不查——幻觉、隐藏、离场同一回显，尝试行不成为存在性 oracle。解析不出原样回显；未声明为引用的字符串一律字面，与实体 id 碰撞不铸造指称。prop 行的指称集只含值：占据迁移行的可说性系于宿主指称，不系于锚的自见性。

**名字闭合**　记录在案的合法指称恒可解析。推导：ref 参数过门 ⇒ 裁决时在世；id 值与关系端点过审查 ⇒ 提交时在世；引用后于死亡的 delta 被审查回滚 ⇒ 失效至多发生在引用之后；窗口取时间序后缀 ⇒ despawn 记录与引用同窗 ⇒ 离场名可解析。例外：被拒尝试的出域引用恒原样回显——非合法指称，不解析。

**事件投影**　AI 所得 = `Π(窗口, w_now) ⊕ 感知变更行 ⊕ 作者 Fact`。近况 `Π` 是纯函数投影，永不持久化；可说性随步冻结，活输入唯名字。act 结果的新见段是同一装配线的回合内增量：本回合新进 refdom 的实体以状态视图同形的实体卡承载。

**投影的消费分类**　进协议通道（约束裁决可行集）的投影，与真相的一致性机械保证；只服务工具与呈现的投影（裁决地图、意图菜单）永不进协议通道，形态自由。豁免：`reason`/`facts` 进协议通道但属规则铸造的世界语，机械只保证送达。

## 主体性

**意志不在世界里**　`def.playerId` 是指向普通实体的锚引用，integrity 恒查其在世——锚在提交边界不可缺席；指针在 def 层不可重绑。账本无玩家。

**缺省主语**　无主语动词的主语 = `hostOf(w, player)`：`in` 链上最近的宿主，缺省锚自身；视角锚定 hostOf 是 games 构件的取值（seals）。

**附身**　占据 = 锚实体 `in` 的迁移，一条 delta 过门。可生灭换角的游戏把锚指向不朽实体、宿主建为 id 属性：宿主之死被 id 引用契约拦成显式迁移（迁移可说，静默回退不可说），迁移合法性由过渡不变式钉住。

人称与受话人是呈现投影的取值：每回合把「谁在看」以可投影形态交给呈现层；规则理由串内联人称，动态人称由规则从世界态计算或以无主语句式交表达层。

## 回合协议

**单意志回合**　act 是唯一的提案通道，每回合恰一个提案窗口；提案唯一形态是映射译码，门闩封本回合的第二次调用；提案不得见自身结果，裁决先于叙述。

**通道次序**　整批先过静态形态检查（否则已裁决动作失去记录），随后逐动作裁决与落钟：后一动作在后一世界态上裁决。

```
apply(a) = adjudicate(a) → G → 逐刻落钟（每刻各系统独立过门）
ticks(s) = ok(s) ? (ticks(J) ?? cost(verb)) : cost(verb)
```

**拒绝恒价**　拒绝分支恒为 `cost(verb)`：可见性门、法则、审查、崩溃同价——Deny 无 ticks 字段，法则结构上不可改写；授予分支的刻数是法则的 authored 输出，缺省 `cost(verb)`，裁决入口校验形状；审查回滚整个授予时尝试仍耗 `cost(verb)`。拒绝同样落钟：刻是世界的因，不是意志的果。推导：若否决零价，入裁决试探即对世界的免费查询，钟沦为免费预言机；时价取常函数使测量与行动同价。时价的定义域是「入裁决」，不是结果；`cost = 0` 属作者定价，反穷举由提案有界（门闩）、域外指称统一文案（门）、理由世界腔（法则）承载。

**静默刻**　零产出的刻仍须表达时间流逝（`timePassed ×n`）；多时间尺度由 systems 谓词从刻数取模派生，复杂历法可自持计数器。

**回合定稿**　`(time, intent, steps)` 入账，intent 逐字保留；绕过引擎的裸 apply 不产生条目。凡刻必属某步的授予区间（刻只发生在授予的执行内）。

**映射契约**　纯语义解析：无关键词、正则、别名表、语言限制。构造不出合法提案时提交空提案，不写理由；「预计被拒」是提交的理由，合理性的判断属于世界。

**表达契约**　`voice` 是静态世界语言文档，原样注入 system prompt，缺省无契约。散文对已锚定内容的转写与渲染自由，以不与状态矛盾为界。`spineLines` 是事件流的规范单行渲染：符号承担结构（✓/✗/⏱/×n），语言词全部来自 messages/label/规则文案。`summarize` 是可选的声音覆写，崩溃回落缺省——呈现缺陷不得丢弃已发生的账目。叙述不回流：回合输入 = 状态视图 + 变更 + 近况。

**呈现服务**　narrate 无提案通道、无 act 通道、无时间流逝，不是回合；正当调用判据：账本中存在可说而未被说过的状态内容。

## 词汇面

- 世界语言：玩家可见的一切文字（法则理由、不变式 message、回退摘要）与表达契约 `voice`。core 不内嵌文案，`messages` 由游戏注入（noResponse / invisibleEntity / defaultReason / timePassed）。
- 通道语言：引擎与 prompt 脚手架之间的协议文本（工具描述、校验反馈、协议锚）。映射契约文案单源：系统提示、工具描述、回合提示、门闩共享同一措辞。
- 开口键按字面：关系类型与 tag token 无声明卡，机械行按字面言说，其世界腔由表达层承担。

世界腔：以世界口吻写就的玩家可见文案；机器诊断（debug）不在此列，不进玩家视野。

## 面向作者的契约

- **动词表**：schema 生成 act 参数校验并推导规则参数的编译期类型；参数须为标量，多重性由批次承载；字符串参数须声明 ref/free，漏报在加载期失败。`internal` 动词不进广告面（广告面 = 裁决面 ∖ internal），由代码直接 apply——同一裁决边界与审查。
- **法则纪律**：键控形态（规则按属性组合键控、量级存于属性载荷，随新实体自动泛化）与 authored 形态（互动逐实体书写）共用同一裁决与审查；键控禁止特判实体 id；规则的读通道唯一在 w⁻——同一值地址（实体×键、边）多次写只能是一条预计算轨迹，后续值计入同提交的先序写（清单逐信销账即此形），从 w⁻ 重复派生的叠加增量按序覆盖、意图静默丢失。分辨率即链：顺序即优先级，弃权是唯一组合算子。
- **拒绝**：结构化 Denial，理由以世界腔内联在规则文本，缺省回落 noResponse；拒绝不携带涉及实体——指称落点在 action 参数（结构化）与法则理由（世界腔）。`deniedBy: rule`（卫语句链、可见性门、unanswered 闭合）是玩法；`invariant`（审查否决、授予形状违约、`*.crash`）是必要性拦截，probe 报 bug。覆盖面没有机械判据：裁决地图供人判读兜底落点，缺口判断属创作者。
- **前置站点**：静态形态违约（未知动词、schema 不符）错在裁决之外——工具边界由校验拒绝；内核收到同类动作即调用方违约，抛 `ProtocolViolation`。语义前提写成卫语句链首；接线判据：前提的否定情形落在参照域内（可指名而不可及）才接线法则，域外（不可指名）由门独任，重复接线即死法则。grounding 以可达性谓词定义感知域是合法取值，代价是物理前提被门吸收——grounding 与前提法则是同一次认识的决策，改其一必重审另一。
- **裁决地图**：probe 对每动词穷举指称参数 × 可见域（每动作在独立 Simulation 上裁决）；法则×动词活性矩阵（授予/拒绝/弃权/未达）是键控泛化拓扑的唯一观察处，零表态是否死法则属作者判读；核心级否决（invariant 且无世界腔理由）单列为 bug。

## 通道代数

「无法表达」的判定：裁决面图灵完备（规则/系统/不变式是开放词汇上的任意代码），不可表达只可能栖居于定签通道——游戏意义必须穿过的、类型被 core 钉死的边界。每通道以载体（能持什么）、粒度（能对什么区分）、方向（状态/感知/事件/记录/入）刻画：

| 通道 | 载体 | 粒度 | 方向 |
|---|---|---|---|
| props / internal | V | 实体×键 | 状态/感知/事件 |
| relations | V | 边 | 状态 |
| grounding | id 集 | 锚×实体 | 感知 |
| edgePerception | Bool | 锚×边 | 感知 |
| propPerception | Bool | 锚×实体×键 | 感知 |
| digestExtra | ViewValue | 锚×自由 | 感知（仅状态面） |
| narratableChanges | 变更行 | internal 滤 | 事件 |
| viewCard | 实体卡 | internal ∧ πₚ | 感知 |
| span | 实体/边键/属性键集 | 锚×步·随步冻结 | 事件 |
| Verdict | Δ/reason/facts/ticks | 步 | 事件 |
| Action schema | 标量 | 尝试 | 入 |
| 回合记录 | intent + steps | 回合×窗口 | 记录 |

方法 = 覆盖矩阵：机制维度 × 通道粒度求覆盖，空格即候选；候选依次过证伪路线——换通道（边→属性、拒绝→零 delta 授予、提问→拒绝理由）、物化（历史/预期/信念 → 属性）、拆动词（成本不对称、条件 schema → 动词分裂）、键设计（命运共享、效果叠加 → 地址/数据归约）——皆不通者才是「无法表达」候选，且须由真实探针实例化方可立项。

可证伪预测：单意志游戏中通道矩阵闭合，未来 core 增长只可能来自多意志变体的批次同时性（批次执行是 fold，单意志下休眠）。

## 研究章程

games 层是研究仪器不是产品。流水线：推导（覆盖矩阵）→ 实例化（探针）→ 判定。判定单位是格子（矩阵坐标），归因隔离由场景锁承载：每场景以初始世界独立起跑。单个最小探针即可承载全部研究需求。

- 展品准入：探针里每个动词/系统/机制具名它实例化的格子，无格子的展品不入馆；极小性按关切计，不按器物计。
- 场景先于世界：新需求先试「在既有世界上写场景锁」，格子无法实例化时才扩展世界。
- 体量外迁：事件体量的压测（投影噪声、近况可读性）属于场景/夹具层，游戏保持极小。

单探针的决定性收益是复合格：通道×通道的交互（改名×known 的跨室指称连续性、知晓×附身的知识随魂、边的消散与属性的强引用对照）只在通道共存的单一世界可实例化。覆盖不了的残余：多意志同时性是协议变体（引擎层），任何 GameDef 都不覆盖；批次多动作压力与 e2e 会话规模属于 loop 阅读与夹具。

统一探针 `seals`（封缄·宅邸夜），展品 → 格子：

| 展品 | 格子 |
|---|---|
| 信文 propPerception（知晓边为判据） | 属性感知槽 |
| 全边隐藏＋亲和/疑心桶级披露 | 知其有不知其值（换通道） |
| 誊写自由字符串 | 言语惰性物化 |
| 攀谈（heard 物化＋introduced 内部标记） | 对话获名的引用生命周期 |
| 揭面（rename）×known | 复合格：改名连续性 |
| 收发清单（id 数组）＋掷火系连检查 | 强引用挡 despawn |
| 魂锚＋附身迁移＋掷火器皿 | 主体性：迁移过门、宿主可生灭、锚不朽 |
| 掷火：边随主消散 vs 属性须先解 | 弱/强引用二分 |
| 交割/流言/漂移 systems | 离屏因果、NPC 边动力学 |
| 走动（邻接前提法则、尝试时价、视角随居所） | 语义前提的法则站点、时价、grounding 注入 |
| 夜笺送达（system spawn → 书案） | 离屏生灭入账、新见卡 |
| 占问（同刻同签） | 随机是世界的纯函数 |
| grounding 锚定 hostOf、魂不可自见 | 无主语动词缺省主语 |

## 非目标

- 词表式语言约束（语言是 AI 的原生能力）
- 一次提交的动作数上界、刻数授予上界（不违反任何契约）
- 散文断言检查器（忠实是赌注）
- 数据法则（AST 解释器，无明显收益）
- 回合纪要（自述偏差经上下文跨回合放大）
- 直达提案通道与仪器摇钟（意志的提案必经映射译码；时间控制属于玩家动词与工具层夹具）
- 多 actor 离屏自治（NPC 由 AI 驱动）
