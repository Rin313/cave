# 语言游戏引擎设计方案

## 1. 定位

一种新的游戏类型引擎：**以确定性状态机为重量锚点、以 LLM 为映射与表达引擎**的交互式语言游戏。

核心主张：

- 操作比文字更加核心。
- AI 不创造规则，AI 不产生系统后果。
- 玩家操作的"重量"由状态机保证，AI 只负责让每次操作的反馈有文学生命力。

## 2. 设计依据（对既有文字游戏的分析结论）

1. **信息提取本身是游戏性**——从文字噪音中定位关键信息的能力替代图形游戏的"反应时间"成为技能度量。
2. **操作成本 = 决策权重**——文字操作比点击慢，每个操作更重，玩家决策更审慎。
3. **系统透明度差异**——文字能描述图形游戏无法表现的复杂度，代价是玩家必须心理建模整个系统。
4. **元游戏层**——MUD 的 trigger 编程、NetHack 的 wiki 考古是文字游戏独有的 meta 玩法；本方案中对应物是"重量边界测绘"（§3 原则 4）。

## 3. 核心原则

1. **模拟层是唯一真相源。** 一切游戏状态、后果、数值由确定性状态机决定，不接受 AI 篡改。
2. **状态机之外的一切，AI 可以自由发挥，但不产生系统层面的重量。**
3. **拒绝也是游戏的一部分，且理由必须来自世界法则。** 玩家通过试错测绘"什么行为改变世界"，这本身就是元玩法；拒绝的理由是线索（「太软，撬不动」告诉你材质有意义），不允许用实现术语（"不是容器""不可见"）回应。
4. **没有通关公式。** 状态空间由模拟层保证复杂度，AI 不制造数值不平衡。对既有文字游戏，"固化操作"（社群穷举出最优序列后游戏死亡）是真正的死结；本方案通过情境化、随状态动态变化的操作集与表达层的修辞新鲜度破局。
5. **表达可证伪。** 玩家看到的一切文字都必须能从状态真相核验：叙述只引用状态派生的实体与变更，幻觉无处藏身。表达层是强制约束（schema + 后置校验），不是提示词请求。
6. **无限的自由度，无限的想象力，无限的生命力。** 尽可能减少引擎承担的职责来突破可能性的上限，即使这会增加 mod 创作者的负担。

## 4. 三层架构

```
模拟层 → 映射层 → 表达层
```

### 4.1 模拟层（确定性）

- NetHack / DoL / C:DDA 级别的法则网络：实体、属性、关系、法则组成的约束网络。
- 接受**结构化动作**（action），返回新状态与结构化结果。
- 统一模式：**动作动词表 + 卫语句式规则裁决**
  - 动作空间由游戏声明：`GameDef.verbs` 是动词表（`{ verb: { schema, entityParams, candidates, rules } }`），引擎据此生成 act 工具的 schema，并在每回合以 `Simulation.affordances()`（枚举 动词×可见实体×候选值 的只读裁决 `check()`，预算按动词均分）把**当前世界会授予的动作**注入映射 prompt——动作空间接地，映射不再冷猜测参数。不再硬编码 apply/move/set——era 类游戏可声明 `talk/travel/equip`，开放世界可声明 `attack/trade/craft`。当前参考游戏按两极各自声明动词集并内置法则集：era 极 village（`gather/eat/rest/talk/buy/sell/clear/fix/seal/subdue/scout`）、开放世界极 waste（`travel/move/open/use/harvest/mark/examine`）。环境响应（刻痕、勘察等）由实体不可知法则承担，不再需要 core 级"AI 提后果"通道（软通道已移除，研究结论见 ARCHITECTURE §5-8~11）。
  - 每个动词挂一组**卫语句式规则**（v4 取代 Expr/Law 数据法则 + 解释器，依据见 ARCHITECTURE §5-13）：普通函数接收只读上下文 `Q`（world/actor/time/params + rel/roll/reach/name 等引擎语义入口），返回授予（delta 变更列表 + 世界腔理由 + facts）或结构化拒绝（Denial），null = 不表态；按序裁决，首个表态即判决——拒绝/授予优先序就是书写顺序，不存在跨法则的隐式吞没。数值与后果由规则产出，LLM 不提案数值。
  - 每个否决结果标记 `deniedBy: "rule" | "denyAll"`：具体规则给了世界性理由（rule）还是落到通用兜底（Denial 的 fallback 标记 / runner 兜底）。这是"法则无洞"探测与拒绝质量审计的语义基础，不依赖理由字符串匹配。
  - **动作的组合由 LLM 自由提出（提案），世界的回应由规则层确定完成，LLM 不参与任何状态变更，也不提案数值**——数值与后果由规则产出。
  - `采下浆果` → LLM 提案 `harvest(bush)` → 动词 `harvest` 的卫语句规则裁决返回变更 `[berries+1, ripe→false]`。
  - 代价：创作者负担从"写交互"转为"声明动词表 + 规则网络"；未覆盖的动作由世界性兜底回应。
  - **环境响应（声明式，取代"AI 提后果"通道）**：非预设的"自由动作"由游戏声明动词 + 实体不可知法则承担（如 waste 的 `mark`/`examine`：规则按 `canReach` 键控，一条规则覆盖全部可达实体）。结构性属性（位置/材质/明火/燃烧/钱币…）仍只能由法则/系统变更；语义一致性由领域不变式兜底（如「湿柴不得燃烧」一条声明式取代逐条守卫）。**研究结论**：软通道（`fallback:"soft"` + `access:"soft"`，AI 直接写期望后果）与实体不可知法则在 e2e 上等价、可写面相同，且带授予理由退化与静默假授予 bug，已移除（ARCHITECTURE §5-8~11）。「AI 不产生系统后果」（§1）因此重新成立——后果只能由法则/系统产出，经提交硬墙（引用完整性 + 领域不变式）原子裁决。
  - **规则必须按属性组合键控，不能按实体键控**（承重墙）：规则只读实体的属性/关系来裁决，禁止特判实体 id。这让规则随新实体加入自动泛化（检查双方的 `material` 硬度来裁决，不关心具体是哪个实体），也是组合爆炸的唯一可行出路。属性组合系统（DF material system 风格：实体属性集 + 动作作为属性解析器）是天然实现手段。
  - **时间系统**：`GameDef.systems` 是按序执行的系统注册表（每 tick 运行的纯函数规则，产出 deltas），承载火蔓延、燃尽、日程等时间驱动逻辑。**`reactiveSystems`（GameDef 可选，默认 false）开启后，granted 动作提交即按序跑一次 systems**——把火源放入易燃物当场引燃、开箱触发陷阱，"动作→世界响应"的因果链当回合成立，不依赖显式 wait。reactive 产出并入动作 StepResult，不重复入日志。
  - **守恒与数值（era/DoL 强一致系统）**：提交后不变式硬墙（core 默认引用完整性 + 游戏声明）违反即**原子回滚整个提交并拒绝**——法则、系统 bug 都无法凭空铸币/灭币。era 类游戏以聚合助手 `sumProp`（core 通用原语）声明守恒不变式（如「铜币总量 == 种子值」），数值由规则产出的 Delta（`inc/relInc`）表达，条件比较在规则代码里直书；随机是 World 的纯函数——`roll` 是 `Q.roll(key, sides)`（key 需同 tick 唯一，可含实体 id），check/apply/dryTick/存档天然一致；多时间尺度（回合/日/月）由游戏自持计数器 + systems 内按 `q.time` 判定，core 不内置历法。
  - **接地钩子**：`grounding` 决定哪些实体进 LLM 序列化；可达性是 core 的**空槽**（`reach`/`reachReason` 谓词，缺省全可达），core 不内嵌任何空间模型——容器包含树语义（`space`/`openable`/`open`/`in`）降为游戏侧构件 `src/games/space.ts`（`inTreeReach`/`inTreeVisible`），需要空间语义的游戏自选接入，不强制；关容器对实体的放行（如楔住的门缝）由构件 opts 的 `containerAccess` 谓词在 game 层裁决。**`digest`（GameDef 可选）是序列化投影**：决定状态以什么形态进 prompt（裁剪冗余、格式化关系边、聚焦点置顶），缺省全量 JSON。
  - **施动工具前提**：动词可声明 `instrumentParams`（哪些实体参数是"挥动的工具"，如 use 的 source）。核心在规则前跑共享检查——必须可持握（`holdable` 空槽，游戏必须声明，core 不提供缺省属性）且可达（`reach` 空槽，与 core 空槽同一模式）；不满足直接拒绝（`instrument.*` 法则），不进入规则。这消除了"用搬不动的重物施力"式的荒谬授予，且让 affordances 枚举自动跳过不可持握工具、`sim probe` 可审计"规则会在不可持握工具上授予"的潜在洞。
  - **关系边与焦点/痕迹**：`world.relations`（`{ from, to, type, value }` 边表）表达社会/叙事状态（信任、记忆、派系），规则以 `relSet/relInc` 变更。`world.focus`（本回合显著实体，跨回合指代锚点）与 `world.traces`（实体累计被拒次数，拒绝痕迹）由核心确定性维护，二者进序列化。
  - **法则无洞**：完整性检查工具（`sim probe`）穷举可见实体的动词/参数组合，报告落到 `deniedBy === "denyAll"` 的法则缺口，作为作者预警——世界法则的洞就是模型幻觉的诱因。**新增 `latent` 审计**：对 `instrumentParams` 拦截的动作，用 `probeGrant()`（只读、跳过工具前提）探测"法则本身是否会在不可持握工具上授予"。fallback 兜底是必要的终端兜底（未覆盖的操作用世界性理由回应，理由内联在规则文本里，不泄漏实现术语），不追求零洞；对模型**真会试**的操作给高信号理由。probe 盲区：仅覆盖已声明 `instrumentParams` 的动词，未声明者需作者自查 rules。
  - 意图表降级为可选"规则捷径"（高频明确操作的确定性直通），不再作为交互的主要出口。

### 4.2 映射层（LLM，双出口 + 结构化拒绝）

玩家输入是两段自由文本：**意图文本**（任意语言、任意表述，可含完整句子）与可选**选中文本**（场景散文里选中的片段）。LLM 的职责是把它们解析为**动作提案列表**（`[{ verb, params }]`），实体必须命中可见实体索引，动词必须取自已声明的动词表。

可靠性实现手段：

- **纯语义解析**：动作与实体完全由 LLM 基于自由文本 + `name + attrs + 上下文` 判断，**无关键词匹配、无别名表、无语言枚举限制**。选中文本作为实体接地锚点；无选中时从意图文本本身解析实体（如「采下浆果」「把书放进木盒」）。
- **Function calling**：把 `act(actions)` 暴露为 tool schema，参数由 `GameDef.verbs` 生成（TypeBox union）。合法性校验（可见性、条件、可访问性）发生在 tool call 内部，由规则层逐条裁决。
- **双 pass 分离（与表达层彻底分离）**：
  - 映射 pass 只产结构化结果：`(动作提案列表)` 或 `(结构化拒绝：label)`，**不产散文**。
  - 表达 pass 在状态变更后独立运行（见 §4.3），面向玩家；玩家的唯一信息来源是表达 pass。
  - 这切断两条幻觉路径：模型不调工具却叙述动作、模型叙述未发生的后果。
- **规则捷径**（未实现，未来可选优化）：`take X` / `open door` 等高频明确动作可用确定性解析直通，不绕 LLM，降成本与延迟。
- **拒绝契约**：模型不撰写任何拒绝理由。无法解析时返回结构化拒绝（仅 `label`）。设计上模型可把预判被拒的动作填入 `refusal.considered` 交规则层裁决（否认 → 规则 denyReason，授予 → 直接执行，纠正模型误判）——**当前未实现**（act 工具 `refusal` 仅含 `label`）；现有路径是模型直接提交 action，由规则层裁决，状态不变。**施动工具前提是规则层之前的共享拒绝**（`instrument.*` 法则，见 §4.1）：`use` 类动作的施动工具不可持握/不可达时，核心直接拒绝并渲染世界腔理由，规则不参与——这是少数几个"核心产出的法则"，理由经 `messages.instrumentUnholdable`/`instrumentUnreachable` 由游戏注入语言。

### 4.3 表达层（LLM，可证伪的文本生成）

AI 读结构化状态 + 本回合变更（changes）+ 涉及集，输出该状态的文字化表达。变更进 prompt 时经语言无关线性化（`name.label: from → to`，实体名 + props 注册表 `label`，core 只做符号连接、不内嵌语言词），不向模型输出原始 `entity.prop`——与「不得写出 id/属性名」约束自洽；`props[].label` 因此是可见属性的维护项（缺 label 回退 raw 属性名）。

- **忠实性约束（强制，非提示词）**：
  - 输出契约：**唯一契约 structured（工具化）**——新事实经第二个自定义工具 `declare` 以结构化参数提交（每条 `{ entities: string[], statement }`），按实体 id 精确集合校验（无名字回退，模型须自列提及的实体）；校验失败逐条返回，模型在同一回合内自我纠正。涉及集推导与校验集中在 `core/declare.ts`（单一来源），engine/工具不重复实现。散文正文即纯文本，**无 `[facts:]` 首行格式约定**（格式 + 正则已移除，实证见 ARCHITECTURE §5-12；取代 §5-11 的文本契约）。
  - **declare 工具校验**：声明实体必须 ⊆ 可见实体，且命中**本回合状态可推导集**——动作主体（玩家）、本回合新可见实体（如打开容器后露出的物品）、变更（from/to）、法则 facts、「即将发生」、**授予动作的实体参数**（被拒动作参数不在内——被拒动作未改变任何状态）。散文正文不再有机械检查（`leakageCheck` 已移除——与 `forbiddenTerms` 同类：词表式匹配 + 语言假设，交由 prompt 设计与模型合规）；**新事实一律走 declare 工具**，`text` 不得断言状态中不存在的实体/属性、不得与状态矛盾——状态蕴含的旧事实（陈设、属性、位置）允许描述。
  - **一致性归属（已修订，取代词表断言扫描器）**：散文正文不再有词表扫描器（`assertionRules`/`negationWords`/`sentencePunct`/`assertionPunct` 与 `scanClaims`/`checkAssertions` 已移除，实证见 ARCHITECTURE §5-10/§5-11）——物理属性与状态的一致性由两条边界承担：**声明契约**（新事实 ⊆ 状态可推导集，语言无关的集合成员判断）与**提交硬墙**（法则 + 不变式原子回滚）。正文可自由文学表达，仅受泄漏检查约束。**润饰属性**（`props` 注册表 `stylistic: true`，如 `marked` 刻痕）允许文学润饰（「刻痕斑驳」），系统提示注入 `[可润饰属性]` 提示。
  - **合法预言**：`Simulation.dryTick()` 克隆世界模拟下一 tick，把「即将发生」的变更作为合法新事实注入表达 prompt——把火源放入易燃容器后「它将燃」是合法预言而非幻觉，不再误伤。**引擎无状态化随机**：随机是 games 层的世界状态（纯函数派生，如 `hashStr(world 计数器)`），世界即完整真相源——dryTick 克隆世界即完整预言，与真实 tick 天然一致；check/apply/存档/恢复也因随机是 World 的纯函数而一致（见 ARCHITECTURE §4-4）。**`pending` 同时进声明校验的 touched 集**——「即将发生」实体可被合法声明（如「将燃」），预言级叙述与已发生事实在声明侧可区分。原 `checkAssertions` 的 pending 豁免逻辑（weak/strong 断言词区分）随扫描器一并移除。
  - **内部属性隔离**：模拟层内部属性（如 `burnTicks`、`actor` 标记）通过属性注册表 `GameDef.props` 的 `internal: true` 标记声明，不进 LLM 序列化、不进 changes、不进表达校验——模型根本看不到，杜绝泄漏。
  - declare 校验失败 → 工具逐条反馈，模型回合内自我纠正；回合结束仍未通过 → 带错误反馈重生成一次；仍失败 → 回退为结构化摘要（强制可证伪）。摘要可由 GameDef 的 `summarize` 钩子提供（游戏腔调、可读），缺省用引擎的通用 JSON 序列化。
- **叙述不回流**：表达 pass 的输入只有当前状态 + changes，无旧叙述全文（幻觉不固化）；上下文裁剪已落地（ARCHITECTURE §2-6），映射 pass 同样只见近况窗口而非旧叙述。
- 生成即时、可流式输出，覆盖表达层延迟。

## 5. 状态机谱系

创作者自选落点：

```
完全无状态机 ←────────────────→ 极复杂状态机
(纯AI聊天)      (Era/DoL级别系统)
 灵活性最大                        确定性最强
 游戏性最弱                        游戏性最强
 AI全权负责                        AI只做表达
 玩家行为无重量                    玩家行为有精确重量
```

- 允许纯 AI 聊天端点存在（完全无状态机 = 自由度最大的普通模拟应用）。
- 引擎不预设状态机复杂度，由每个游戏自己声明。

## 6. 交互模式：文本即操作面

玩家用任意文字表达操作意图（§4.2），可选中场景散文片段作实体接地锚点。快捷键/菜单只是"输入意图文本"的快捷方式，本质仍是自由文本。

关键特性：

- 玩家定义交互面——阅读深度直接转化为操作空间广度。
- 解析失败也是游戏的一部分：无法解析的意图获得世界腔的平淡回应（模型不撰写理由），玩家通过「尝试→被世界拒绝」的落差学习边界。
- 操作始终保持精确——意图与实体命中状态机边界后才产生后果，LLM 不创造系统后果。
- 与 AI chat 的本质区别：无系统约束；与 MUD/roguelike 的本质区别：操作集是情境化、动态生成的，不是预设热键。

## 7. 引擎接口（创作者只需实现状态机）

创作者通过声明 `GameDef` 接入引擎，核心两件事：**声明动词表 + 声明规则**。

```ts
const game: GameDef = {
  verbs: {
    move: { label, description, schema, entityParams, candidates, rules },
    // 每个动词：schema 约束参数（TypeBox）；candidates 给非实体参数候选值（动作空间接地/探测共用）；rules 为卫语句式规则（按序裁决首个表态即判决，末尾可挂 fallback 兜底）
  },
  systems: [SystemRule],           // 时间系统（可选，world→deltas 的纯函数规则）
  messages,                       // 必填：core 产出的用户可见文案（时间流逝/施动工具/不可见实体等），只收解析后的 referent（名字），core 不内嵌任何语言
  grounding: (w, a) => [...],     // 可见实体索引（可选）
  hint, summarize, props, // props：属性注册表（type/label/internal/stylistic）；表达一致性由声明契约 + 提交硬墙承担，无词表断言钩子
};
```

引擎自动处理：act/declare 工具 schema 生成、每回合动作空间接地注入、双 pass（映射/表达）编排、结构化拒绝接线、declare 事实校验与回退摘要。拒绝理由一律由规则层产出，引擎不生成理由。选中文本作为软接地锚点注入映射 prompt；快捷键绑定属桌面壳阶段。

## 8. 原型验证计划

先验证完整闭环（表达→选取→映射→执行→再表达），核心指标是**接地一致性**——不测映射层孤立准确率，因为映射成功率依赖表达层接地质量：

1. 最小种子状态机：一个场景、若干带属性实体 + 基础法则网络（基线场景已覆盖：village 的数值互锁 / 经济守恒 / 顺序过程 / 随机判定，waste 的路径探索 / 拾取放置 / 环境响应（mark/examine）/ 时间系统）。
2. Function-calling 映射层（`act(actions)`，schema 从动词表生成）+ 实体接地 + JSON schema。
3. 跑 50 条刁钻交互用例：否定、跨句指代、隐含宾语、怪异措辞、歧义实体。
4. 量出指标：
   - **接地一致性**——表达 pass 散文中的实体能否 100% 回链到状态实体，映射能否从散文实体准确落到预期的模拟层效果。
   - **幻觉率**——表达声明实体命中本回合涉及集的比例（§4.3 声明校验量化）。
   - **拒绝质量**——拒绝理由是否来自法则（可追溯，不得为实现术语）。
5. 通过后再扩展：法则网络（材质、组合、空间、时间）、忠实性约束松紧调参（§4.3）、自定义快捷键系统。

## 9. 待确认的开放问题

- 法则网络的表达力边界：创作者能在多大范围内声明法则？物理/化学法则易覆盖；社会/叙事状态（"国王在骗你""她记得你侮辱过她"）已由关系边表 `world.relations` 提供原语（信任/记忆/派系以 `{from,to,type,value}` 表达），但表达力边界（复杂关系演化、矛盾记忆）待验证。
- 空间建模：多房间 / 开放空间、位置的多种语义（手持 / 放置 / 装入 / 嵌住，C:DDA 式）如何落地。
- 时间驱动的持续世界：tick 法则的因果链覆盖度（法则完整性检查），时间本身是否成为决策压力（Lifeline 式在场感）。`reactiveSystems` 已让"动作→世界响应"当回合成立，但持续世界的时间压力节奏仍待设计。
- 跨回合指代已落地为确定性 `focus`（见 §4.1、ARCHITECTURE §4-5）：无回流下「打开容器 → 把里面的东西拿起来」正确解析；「把它关上」被动词约束正确回落。**残余问题**：focus 是优先候选而非硬绑定，多实体歧义场景仍可能失败；候选方案：活动实体索引增强 / 澄清机制作为玩法的一部分。
- 拒绝痕迹已落地（`world.traces`：实体累计被拒次数，进序列化）：表达层可引述"反复试过"的累积感。**但"痕迹具象化"未实现**——"反复撬过的门缝留下磨损"需要规则在 traces 达阈值时产出痕迹实体/变更，属后续玩法扩展。
- 忠实性约束的松紧参数：接地字段粒度 vs 自由字段宽度（§4.3）。预言/已发生已在钩子侧可区分（pending 传入），但"预言深度"（如「再过不久它会怎么样」这类对后续进程的预测）的边界仍靠词表启发式。

## 10. 参考与灵感来源

| 游戏/类型 | 贡献的正向参考 | 本方案拒绝的负面参考 |
|---|---|---|
| MUD（北大侠客行） | 命令解析复杂度即技能曲线；trigger/别名编程 = 玩家在编程，元游戏层 | 固定命令 DSL 让操作固化；社群穷举最优序列后游戏死亡 |
| NetHack | 穷举交互空间的探索模式；物态/物品/怪物互动的操作空间广度 | 输出端用 ASCII 字符而非自然语言，浪费了输入端的交互复杂度 |
| era 系 | 基于状态机的交互模拟器；所有操作映射到庞大数值系统；可重玩性来自系统深度 | 交互脚本是预编码的，操作被固化 |
| Degrees of Lewdity | 多资源轮耦合的风险决策设计（意志/疲劳/创伤/暴露度互相影响） | 回合制+固定选项制造决策节奏，但操作集被预设 |
| A Dark Room | 极简操作中"造火"式的 gameplay reveal（操作中发现而非 lore dump） | 放置式点击循环，可用鼠标连点器，太蠢 |
| Progress Quest | — | "零玩家游戏"极端：建号后角色全自动升级打怪，玩家零输入。无操作=无决策=无技能度量，连放置游戏的点击都省去，玩家沦为观众，纯数字膨胀的斯金纳箱；谱系上位于"玩家行为无重量"的最底端（玩家整个缺席）。仅作为恶搞/艺术装置成立（拷问"游戏是什么"），作为游戏是真空 |
| Dwarf Fortress | material system：交互规则降维为属性运算 | 仅覆盖物理/化学属性，覆盖不了社会/叙事状态 |
| Visual Novel | — | 固定分支 = 翻书，AI+随机分支只是加噪音，无法深挖 |
| Lifeline 系列 | 真实时间流逝制造"陪伴在场感"——时间节奏可剥离出来作为真实决策压力（接上状态机） | 本质是"带时钟的 VN"：选择落在固定分支，真实感靠拖延内容制造而非生成内容。LLM 已使该类别整体退役——实时应答/无限对话是其原生能力 |
| 文字遊戲 (Word Game) | "文字作为可交互实体"的直觉有吸引力 | 把 NetHack 的"字符构建视图"推向极端：世界 = 汉字瓦片（墙是一排"墙"字，玩家是"我"字），字形既是视图又是谜题机制；文字沦为退化版图形引擎，文学性与操作重量双失。且谜题为手工设计固定关卡、无状态机，属"固化操作"的另一形态 |
| AI chat/跑团/写小说 | — | 无系统约束的即时幻觉，玩家行为没有实际重量；"不是游戏" |
| 风之旅人 / What Remains of Edith Finch | 文本表达应向电影级渲染靠拢的标准 | — |
| Minecraft | 方块即原子：世界由可组合的最小元素构成，任何交互都在元素层面成立，玩家自由度来自系统组合而非预编码交互 | 3D 方块空间表达不了叙事/社会状态；"破坏-放置"覆盖一切，无因果链 |
| Terraria | 发现式合成：大量可交互物品 + 探索中揭晓组合配方，玩家考古游戏系统 | 配方仍是预编码集合，穷举后操作固化（同 MUD 死结） |
| Don't Starve | 生存资源耦合 + 时间压力：饥饱/理智/血量互锁，季节昼夜驱动决策节奏，饥饿把"时间"变成真实资源 | 行为树/预设配方使行为固定；"活着"本身有重量但可穷举 |
| RimWorld | AI 叙事生成：pawns 的性格/关系/事件与模拟耦合，故事从系统状态中涌现而非手写分支 | 事件是预设叙事碎片，触发式而非涌现；操作大多是菜单指派 |
| Kerbal Space Program | 真实规则即重量：玩家用物理零件自由组装，结果由物理模拟裁决，轨道力学本身成为技能曲线；系统组合产出意料之外的结果 | — |
| Mount & Blade | 开放世界沙盒：玩家在世界中自由定位自己，经济/战争/声望由系统模拟，无主线 | 战斗与治理是预设操作层，系统耦合浅 |
| Kenshi | 系统驱动世界状态：派系战争、城市兴衰、经济由模拟产生，玩家行动有真实后果且无角色预设 | 角色成长是"做事累积数值"的浅系统，无深交互 |
| Cataclysm: Dark Days Ahead | 极端深度的物品交互：一切皆可拆解/改造/组合（车辆系统），时间流逝是持续压力，纯文本的交互复杂度巅峰 | — |
